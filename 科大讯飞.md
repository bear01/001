# 科大讯飞测试开发工程师面试题整理

生成时间：2026-05-31  
岗位方向：测试开发工程师 / AI 测试开发 / 算法测试 / 语音产品测试 / 教育产品质量 / 自动化测试 / 大模型质量评测  
适用场景：讯飞星火、讯飞开放平台、语音识别、语音合成、机器翻译、讯飞输入法、学习机、智慧教育、智慧医疗、车载语音、办公 AI 和行业大模型应用。

## 一、面试画像

科大讯飞测试开发面试通常会结合传统测试开发基础和 AI 产品质量能力。公开面经中常见问题包括 UI 自动化适用场景、接口自动化、Bug 流程、测试用例设计、测试基础、项目经历、算法与编程、AI 能力如何引入测试流程等。岗位资料中也能看到性能测试、大数据平台自动化测试、AI 算法测试开发、C++/Python 脚本能力、语音信号处理、自然语言处理和模式识别基础等要求。讯飞开放平台本身提供语音识别、语音合成、翻译、OCR、声纹识别和星火大模型等 AI 能力，因此面试准备要突出“测试工程化 + AI 算法评测 + 业务质量闭环”。

注意：本文不是官方题库，而是基于公开面经、招聘信息、讯飞开放平台资料和测试开发高频考点整理出的复习材料。

复习优先级建议：

1. 基础能力：Linux、HTTP/TCP、MySQL、Redis、消息队列、并发、Python/Java/C++、数据结构。
2. 自动化能力：接口自动化、UI 自动化、测试平台、CI/CD、Mock、数据构造、失败归因。
3. AI 业务：ASR、TTS、翻译、OCR、声纹、星火大模型、教育学习机、车载语音。
4. 算法测试：数据集、标注质量、准确率、召回率、WER/CER、延迟、鲁棒性、公平性。
5. AI 能力：AI 用例生成、LLM/RAG 测试、Agent 工具调用、模型安全、AI 代码测试。

## 二、计算机基础

### 1. HTTP 和 HTTPS 有什么区别？AI 开放平台要关注什么？

**参考答案：**  
HTTP 是明文传输，HTTPS 在 HTTP 与 TCP 之间加入 TLS 加密层，提供身份认证、加密传输和完整性保护。AI 开放平台涉及 AppID、APIKey、APISecret、用户语音、文本、图片和行业数据，测试时要关注证书校验、密钥保护、请求签名、重放攻击、日志脱敏、限流和错误码。

**AI 相关加分点：**  
语音、文本和图片可能包含隐私内容，接入 LLM 日志分析时要先脱敏，并设置数据留存和访问权限。

### 2. WebSocket 和 HTTP 普通请求有什么区别？语音识别为什么常用长连接？

**参考答案：**  
HTTP 普通请求适合一次性请求响应，WebSocket 建立长连接后可以双向持续传输数据。实时语音识别需要不断上传音频帧并接收中间识别结果，长连接可以降低重复建连开销并支持流式返回。测试要覆盖连接建立、鉴权、分片上传、心跳、断线重连、中间结果、最终结果和弱网。

**AI 相关加分点：**  
AI 生成接口用例时容易只测最终响应，要补充流式过程中的分片、乱序、超时和重连断言。

### 3. TCP 如何保证可靠传输？

**参考答案：**  
TCP 通过序列号、确认应答、超时重传、滑动窗口、拥塞控制、流量控制和校验和保证可靠传输。语音、翻译和大模型 API 测试要关注网络延迟、丢包、重试、连接池、超时配置和请求幂等。实时交互类业务还要关注延迟和用户体验。

**AI 相关加分点：**  
语音识别的质量不仅取决于模型，还受网络分片、音频编码和端侧采集影响，测试要端到端看。

### 4. MySQL 索引为什么能加速查询？索引失效有哪些原因？

**参考答案：**  
B+Tree 索引通过有序结构减少扫描行数，支持等值、范围、排序和最左前缀匹配。索引可能因函数操作、隐式类型转换、前置通配符、违反最左前缀原则、低区分度、统计信息不准等失效。测试开发要会用 explain、慢查询日志和压测验证索引效果。

**AI 相关加分点：**  
AI 可以解释 explain 结果，但优化建议要结合真实数据量、查询频率和业务 SLA。

### 5. Redis 缓存穿透、击穿、雪崩是什么？讯飞场景怎么测？

**参考答案：**  
穿透是大量查询不存在数据打到数据库；击穿是热点 key 过期后大量请求同时回源；雪崩是大量 key 同时过期或缓存集群故障导致数据库压力暴增。讯飞场景可围绕模型配置、服务额度、用户会话、知识库索引、教育题库、设备状态、热点语音资源测试非法 ID、高并发热点、批量过期、Redis 故障和限流降级。

**AI 相关加分点：**  
AI 应用的配置和知识库缓存要关注权限隔离，避免跨用户、跨租户读取缓存。

### 6. 什么是幂等？AI 平台哪些接口需要幂等？

**参考答案：**  
幂等是同一请求执行一次和多次的最终业务结果一致。订单支付、额度扣减、模型调用计费、训练任务提交、数据集上传、知识库导入、作业批改、消息消费都需要幂等。常见方案包括唯一请求号、幂等 token、状态机、数据库唯一索引、去重表和分布式锁。

**AI 相关加分点：**  
模型调用计费链路要重点测试重复请求和超时重试，避免重复扣费或漏计费。

### 7. 进程、线程和协程有什么区别？

**参考答案：**  
进程是资源分配单位，线程是 CPU 调度单位，同一进程内线程共享内存；协程是用户态轻量级执行单元，适合高并发 IO。自动化执行器、音频批量评测、模型压测、任务调度平台需要根据 CPU 密集、IO 密集和隔离性选择并发模型。

**AI 相关加分点：**  
AI 批量评测要控制并发、token 成本、音频文件句柄和模型服务限流，避免压垮评测环境。

## 三、编码与算法

### 8. 写一个函数，判断字符串括号是否合法。

**参考答案：**  
使用栈。遇到左括号入栈，遇到右括号时检查栈顶是否匹配；遍历结束后栈为空才合法。时间复杂度 O(n)，空间复杂度 O(n)。

```python
def valid_brackets(s: str) -> bool:
    pairs = {')': '(', ']': '[', '}': '{'}
    stack = []
    for ch in s:
        if ch in pairs.values():
            stack.append(ch)
        elif ch in pairs:
            if not stack or stack.pop() != pairs[ch]:
                return False
    return not stack
```

**AI 相关加分点：**  
AI 生成代码后，要补空串、普通字符、只有右括号、嵌套括号和连续括号测试。

### 9. 如何计算语音识别 WER/CER？

**参考答案：**  
WER 是词错误率，CER 是字错误率，核心都基于编辑距离。计算参考文本和识别文本之间的替换、插入、删除次数，再除以参考文本长度。中文语音识别常用 CER，英文或分词后的文本常用 WER。测试时要明确归一化规则，例如大小写、标点、数字、同音词、空格和繁简转换。

**AI 相关加分点：**  
模型评测不能只看整体 CER，要分场景看方言、噪声、远场、儿童声、专业术语和长音频。

### 10. Top K 高频元素怎么做？

**参考答案：**  
先用哈希表统计频次，再用小顶堆维护 K 个高频元素，时间复杂度 O(n log k)。海量日志可先分片统计，再合并 Top K。讯飞场景可用于识别错误热词、合成异常、API 错误码、教育题目错误类型和用户反馈热点分析。

**AI 相关加分点：**  
Top K 可做规则粗聚类，再用向量检索和 LLM 做语义合并。

### 11. 如何实现 LRU 缓存？

**参考答案：**  
使用哈希表加双向链表。哈希表支持 O(1) 查找，双向链表维护访问顺序。读取或写入时把节点移动到头部，容量超限时淘汰尾部节点。要处理更新已有 key、容量为 0、删除映射和并发访问。

**AI 相关加分点：**  
AI 写 LRU 时常漏掉更新已有 key 后移动节点或删除旧映射，测试用例要覆盖。

### 12. 如何判断链表是否有环？

**参考答案：**  
使用快慢指针。慢指针每次一步，快指针每次两步；如果有环二者最终会相遇，如果快指针到达空节点则无环。时间复杂度 O(n)，空间复杂度 O(1)。

**AI 相关加分点：**  
AI 生成用例时要覆盖空链表、单节点自环、尾节点成环和无环链表。

### 13. 如何实现音频批量评测任务调度？

**参考答案：**  
可以把音频文件、参考文本、场景标签和模型版本作为任务元数据，写入任务队列；执行器按并发限制拉取任务，调用 ASR 服务，记录结果、耗时、错误码和指标。要支持重试、超时、失败隔离、断点续跑、结果聚合和版本对比。

**AI 相关加分点：**  
评测平台要保存模型版本、数据版本和提示词版本，否则结果不可复现。

### 14. 如何测试一个排序函数？

**参考答案：**  
不仅检查输出有序，还要检查元素数量不变、频次不变、没有丢失和新增。如果要求稳定排序，还要验证相等元素相对顺序。用例包括空数组、单元素、已排序、逆序、重复元素、负数、极值、大量随机数和局部有序。

**AI 相关加分点：**  
可用 property-based testing，让 AI 辅助生成属性断言，再由程序自动验证。

## 四、AI 语音与开放平台场景

### 15. 如何测试实时语音识别 ASR？

**参考答案：**  
覆盖音频格式、采样率、声道、分片大小、流式上传、中间结果、最终结果、静音、长音频、断线重连、弱网、噪声、方言、儿童声、远场和专业词。指标包括 CER/WER、首包延迟、最终延迟、实时率、错误码、稳定性和资源消耗。

**AI 相关加分点：**  
ASR 测试要建立分场景评测集，不能只用普通话安静环境样本。

### 16. 如何测试语音合成 TTS？

**参考答案：**  
覆盖短文本、长文本、多语言、数字、日期、英文、标点、停顿、语速、音色、情感、断句、特殊字符和超长输入。指标包括发音准确性、自然度、稳定性、首包延迟、合成耗时、音频完整性和失败率。还要验证文本切分、敏感内容、缓存和权限。

**AI 相关加分点：**  
TTS 质量需要主观 MOS 和客观指标结合，AI 可辅助发现发音异常但不能完全替代人工听测。

### 17. 如何测试机器翻译？

**参考答案：**  
覆盖中英互译、多语言、短句、长文、口语、专业术语、数字、日期、单位、人名地名、上下文和格式保留。指标包括准确性、流畅性、术语一致性、BLEU/COMET 等自动指标和人工评分。异常包括空输入、超长输入、混合语言和敏感内容。

**AI 相关加分点：**  
LLM 翻译要测试幻觉和擅自补充信息，尤其是法律、医疗、教育和政务文本。

### 18. 如何测试声纹识别？

**参考答案：**  
覆盖注册、验证、噪声、变声、录音回放、不同设备、距离、方言、年龄变化、多人混音和攻击样本。指标包括通过率、拒识率、误识率、等错误率 EER、响应时间和安全性。还要验证隐私保护、样本加密和权限控制。

**AI 相关加分点：**  
声纹属于敏感生物特征，测试数据必须严格脱敏和授权，不能随意上传给大模型。

### 19. 如何测试 OCR？

**参考答案：**  
覆盖清晰图片、模糊、倾斜、低光、反光、复杂背景、表格、手写、票据、证件、多语言和竖排文本。指标包括字符准确率、字段准确率、版面还原、耗时和失败率。要验证隐私、证件脱敏、异常输入和大图处理。

**AI 相关加分点：**  
OCR + LLM 信息抽取要拆开评测，避免 OCR 错误被 LLM“合理化”后掩盖。

### 20. 如何测试讯飞输入法语音输入？

**参考答案：**  
覆盖普通话、方言、中英混输、标点、表情、长句、断句、离线/在线切换、弱网、隐私授权、候选词和纠错。性能关注识别延迟、耗电、流量、崩溃率和卡顿。还要验证键盘权限、剪贴板、云端同步和敏感词处理。

**AI 相关加分点：**  
语音输入的 AI 纠错要测试是否擅自改写用户原意，尤其是人名、地名和专业术语。

### 21. 如何测试开放平台 API 鉴权？

**参考答案：**  
覆盖 AppID、APIKey、APISecret、签名、时间戳、nonce、防重放、额度、限流、IP 白名单、权限范围和错误码。异常包括密钥错误、签名过期、参数篡改、额度用尽、跨应用调用、服务未开通和高并发请求。

**AI 相关加分点：**  
大模型 API 还要测试 token 计费、上下文长度、模型版本权限和内容安全策略。

### 22. 如何测试 API 限流和计费？

**参考答案：**  
覆盖 QPS 限制、日/月额度、并发限制、突发流量、付费套餐、超额提示、重试、降级和账单。断言包括接口返回、调用日志、计费记录、控制台展示和告警。要特别测试超时重试是否重复计费。

**AI 相关加分点：**  
模型服务成本高，测试要记录 token、音频时长、图片张数、计费单位和失败请求计费策略。

## 五、教育、硬件与行业场景

### 23. 如何测试 AI 学习机作文批改？

**参考答案：**  
覆盖不同年级、题材、错别字、语法、结构、立意、跑题、抄袭、敏感内容、极短作文和超长作文。结果要验证评分、评语、修改建议、示例、可解释性和安全合规。需要人工标注样本校准，避免模型输出不当价值观或错误知识。

**AI 相关加分点：**  
教育场景要特别关注未成年人保护、内容安全、公平性和可解释性。

### 24. 如何测试拍照搜题？

**参考答案：**  
覆盖清晰图片、模糊、倾斜、手写、印刷体、多题同图、题干缺失、公式、图形、英语、物理化学等学科。链路包括图像采集、OCR、题目检索、答案匹配、解析展示和错题收藏。指标包括识别准确率、命中率、解析质量和响应时间。

**AI 相关加分点：**  
RAG 或题库检索要验证是否引用真实题目解析，不能编造答案。

### 25. 如何测试口语评测？

**参考答案：**  
覆盖单词、句子、段落、跟读、自由表达、噪声、方言、儿童声、发音不标准、漏读、多读和停顿。指标包括发音准确度、流利度、完整度、韵律、评分一致性和反馈可解释性。要用人工标注样本验证模型评分。

**AI 相关加分点：**  
口语评测要关注不同口音和年龄段的公平性，避免模型对某些群体系统性偏低。

### 26. 如何测试车载语音助手？

**参考答案：**  
覆盖唤醒、识别、语义理解、多轮对话、导航、音乐、空调、车窗、座椅、电话、打断、噪声、多人说话、离线降级和弱网。安全场景要验证行驶中禁止危险操作、误唤醒、误识别和权限控制。

**AI 相关加分点：**  
大模型车载助手要测试越权控制、幻觉指令、危险动作拒绝和工具调用审计。

### 27. 如何测试会议转写和摘要？

**参考答案：**  
覆盖多人说话、说话人分离、噪声、专业术语、中英混说、长会议、标点、时间戳、关键词、摘要、待办事项和导出。指标包括转写准确率、说话人识别准确率、摘要准确性、遗漏率、幻觉率和隐私保护。

**AI 相关加分点：**  
摘要要能追溯原文时间戳，避免模型生成会议中没有出现过的结论。

### 28. 如何测试智慧医疗语音录入？

**参考答案：**  
覆盖医学术语、药品名、疾病名、医生口音、嘈杂门诊、长句、模板字段、结构化病历和隐私权限。指标包括术语准确率、字段准确率、延迟、可编辑性和审计日志。医疗场景要有强隐私、合规和人工复核。

**AI 相关加分点：**  
LLM 不能擅自给诊断建议或修改医生原意，医疗文本生成必须可追溯。

## 六、自动化测试与质量平台

### 29. UI 自动化适合什么场景？什么时候不适合？

**参考答案：**  
UI 自动化适合需求稳定、流程高频、人工回归成本高、收益明确的核心链路，例如登录、购买、搜索、提交、核心配置。不适合频繁变化页面、一次性活动、视觉细节频繁调整和难以稳定定位的低价值场景。业务规则应尽量下沉到接口或服务层测试。

**AI 相关加分点：**  
AI 可辅助修复定位器和生成脚本，但要经过 review，避免掩盖真实产品缺陷。

### 30. 如何设计接口自动化测试框架？

**参考答案：**  
底层封装请求、鉴权、环境、日志、重试；中间层封装业务 API、测试数据和依赖提取；上层编排场景、断言和报告。框架要支持参数化、Mock、并发执行、失败重跑、CI 集成、历史趋势和质量看板。

**AI 相关加分点：**  
AI 可根据 OpenAPI 生成基础用例和 client，但业务断言、计费、权限和幂等校验需要测试开发把关。

### 31. 如何做 AI 算法自动化评测平台？

**参考答案：**  
平台应支持数据集管理、标注版本、模型版本、任务调度、指标计算、错误样本归因、报告、可视化、回归对比和权限。不同算法要支持不同指标，例如 ASR 的 CER/WER、TTS 的 MOS、OCR 的字段准确率、LLM 的准确性和安全性。

**AI 相关加分点：**  
算法评测必须可复现：保存数据版本、模型版本、prompt 版本、采样参数和评测脚本版本。

### 32. 如何把自动化测试接入 CI/CD？

**参考答案：**  
主干流水线跑单元测试、静态扫描、接口冒烟、契约测试和核心自动化；夜间任务跑完整回归、性能基线和算法评测。AI 产品发布前还要增加模型回归、安全评测、数据集评测、灰度监控和回滚策略。

**AI 相关加分点：**  
模型上线门禁不能只看构建通过，还要看关键指标是否退化、是否引入高风险安全问题。

### 33. 如何治理 flaky case？

**参考答案：**  
先统计失败率、失败原因、影响流水线次数和修复时长。常见治理包括稳定等待、独立测试数据、Mock、失败重试、截图/日志/音频留存、环境隔离、用例隔离和低价值用例下线。流程上要有责任人、SLA 和定期复盘。

**AI 相关加分点：**  
LLM 可做失败归因，但分类必须附带错误栈、接口响应、音频样本、模型版本和环境信息。

### 34. 如何设计质量看板？

**参考答案：**  
包括需求进度、用例通过率、自动化覆盖率、构建失败率、缺陷趋势、线上缺陷、回归耗时、接口错误率、P95/P99、模型指标、识别准确率、合成失败率、API 成功率、计费异常、灰度异常和用户反馈。

**AI 相关加分点：**  
LLM 可生成质量日报，但原始指标和计算口径必须可追溯。

## 七、性能、稳定性与安全

### 35. 如何做语音识别服务性能测试？

**参考答案：**  
先定义音频时长、并发用户、分片大小、音频格式、网络模型和 SLA。监控 QPS、实时率、首包延迟、最终延迟、错误率、CPU、GPU、内存、队列堆积和下游依赖。还要按短音频、长音频、流式、离线批处理分别压测。

**AI 相关加分点：**  
ASR 性能测试要同时看质量指标和性能指标，不能为了低延迟牺牲准确率。

### 36. 如何测试限流和降级？

**参考答案：**  
限流验证阈值、维度、返回码、提示、恢复和误伤；降级验证核心链路可用、非核心能力关闭、缓存兜底、默认值和用户体验。可模拟模型服务超时、GPU 资源不足、鉴权服务异常、计费服务异常和知识库服务不可用。

**AI 相关加分点：**  
AI 可根据依赖图生成故障注入清单，但执行要在可控环境逐步扩大。

### 37. 如何测试模型安全？

**参考答案：**  
覆盖违法违规、隐私泄露、歧视偏见、医疗金融建议、未成年人保护、越狱攻击、提示注入、多轮诱导和工具调用越权。要建立红队样本、正常样本、边界样本和多语言样本，评估拦截率、误杀率、漏放率、延迟和可解释性。

**AI 相关加分点：**  
教育和医疗场景中，安全漏放比普通问答更高风险，要按场景分层评估。

### 38. 线上 P0 故障时，测试开发应该做什么？

**参考答案：**  
先协助止血，确认影响范围、模型版本、服务版本、时间、用户群和核心指标，推动降级、回滚或配置关闭。然后保留日志、trace、音频/文本样本、监控、配置和变更记录。恢复后参与复盘，补充自动化用例、模型评测样本、监控告警和故障演练。

**AI 相关加分点：**  
AI 可快速摘要日志和告警，但故障现场高风险操作必须人工确认。

## 八、AI + 测试专项

### 39. 如何测试讯飞星火这类大模型？

**参考答案：**  
覆盖通用问答、数学推理、代码、写作、知识问答、工具调用、多轮对话、长上下文、拒答、安全、稳定性、延迟和成本。需要建立黄金集、红队集、行业场景集和线上 badcase 回流机制。指标包括准确率、幻觉率、一致性、拒答正确率、工具调用成功率、延迟和用户满意度。

**AI 相关加分点：**  
大模型评测要版本化管理 prompt、采样参数、模型版本和评委模型，避免结果不可复现。

### 40. 如何测试 RAG 应用？

**参考答案：**  
RAG 测试要拆成检索质量和生成质量。检索侧关注文档切分、向量召回、重排、权限隔离和知识更新；生成侧关注答案正确性、引用准确性、无答案拒答、幻觉和一致性。指标包括召回率、答案准确率、引用正确率、延迟和成本。

**AI 相关加分点：**  
教育、医疗、政企知识库要特别验证权限隔离，避免跨用户、跨租户泄露知识。

### 41. 如何测试 AI Agent 的工具调用？

**参考答案：**  
覆盖工具选择、参数生成、权限控制、失败重试、超时、结果校验、多工具编排和审计日志。Agent 不应在未授权情况下执行删除文件、发送消息、修改配置、扣费、导出数据或控制设备等高风险动作。

**AI 相关加分点：**  
建立工具调用黄金集和红队集，量化成功率、误调用率、越权率和任务完成率。

### 42. 如何用 AI 辅助生成测试用例？

**参考答案：**  
把 PRD、接口文档、历史缺陷、业务规则、模型指标、数据集说明和代码 diff 输入 AI，让它生成场景清单、边界值、异常路径、决策表和脚本草案。风险包括幻觉、遗漏规则、重复低价值用例、泄露敏感数据和断言不准确。要做脱敏、人工评审、规则校验和执行反馈闭环。

**AI 相关加分点：**  
AI 是初稿生成器和覆盖提醒器，最终测试责任仍在测试开发。

### 43. 如何评估 AI 生成代码的质量？

**参考答案：**  
通过单元测试、契约测试、集成测试、静态扫描、代码评审、覆盖率、边界用例和灰度验证评估。AI 代码容易出现看似合理但边界不完整、风格不一致、重复逻辑和隐藏安全风险。团队应把规范固化到工具链中，在提交前自动检查。

**AI 相关加分点：**  
可以让 AI 先生成测试，再生成实现，并通过 CI 验证；但不能跳过人工 review。

## 九、项目与行为面试

### 44. 介绍一个你做过的自动化测试项目。

**参考答案：**  
用 STAR 结构回答：背景、痛点、职责、方案、难点、收益。要讲覆盖率、执行时长、发现缺陷、稳定性、接入流水线和团队使用情况。不要只说写脚本，要体现平台化和业务收益。

**AI 相关加分点：**  
可以补充如何用 AI 生成初版用例、分析失败日志或总结报告，并说明人工校验机制。

### 45. 如果模型版本上线时间被压缩怎么办？

**参考答案：**  
先确认变更范围、影响场景、风险等级、回滚方案和灰度策略。按风险优先级保留核心业务集、模型指标回归、安全红队集、性能基线和线上监控。明确未覆盖风险并同步相关方，上线后加强 badcase 回流和快速回滚准备。

**AI 相关加分点：**  
AI 可推荐最小回归集，但最终选择要结合业务影响、历史缺陷和模型安全风险。

### 46. 开发或算法同学不认可你提的 bug，怎么办？

**参考答案：**  
补充复现步骤、环境、版本、输入样本、模型输出、日志、指标变化和预期依据。再对齐需求、标注规则或算法设计，确认是产品缺陷、模型边界、数据问题、标注问题还是测试误判。争议仍在时拉产品、算法负责人或业务专家决策。

**AI 相关加分点：**  
AI 可以整理缺陷描述和影响范围，但不能替代真实样本、指标和业务规则。

### 47. 你如何理解 AI 测试开发岗位？

**参考答案：**  
AI 测试开发既要有传统测开的工程能力，也要理解模型指标、数据质量、评测集、标注、灰度、监控和安全。它不是只跑用例，而是通过平台化和自动化方法持续评估模型效果、工程稳定性和业务风险。优秀 AI 测开要能把需求、数据、模型、服务、端侧体验和线上反馈串成闭环。

**AI 相关加分点：**  
AI 时代的测开要把重复分析、初稿生成和报告总结交给 AI，把风险判断、评测体系和关键验证掌握在自己手里。

## 十、反问面试官

1. 团队测试开发更偏 AI 算法评测、业务质量保障，还是测试平台和质量效能建设？
2. 当前最影响质量效率的问题是什么：自动化覆盖、数据集管理、标注质量、失败归因，还是模型上线风险？
3. 团队是否已经在使用 AI 辅助测试、智能回归、LLM/RAG 评测或 Agent 工具调用测试？
4. 语音识别、语音合成、教育硬件或星火大模型业务中，当前最核心的质量指标有哪些？
5. 对新人前三个月的期待是什么？希望先补业务，还是先交付工具能力？

## 十一、复习建议

1. 准备两个项目故事：一个接口自动化/测试平台项目，一个 AI 算法评测或语音产品质量项目。
2. 每天刷 2 道算法题，重点是哈希、栈、链表、堆、LRU、编辑距离和字符串处理。
3. 熟悉讯飞典型业务：ASR、TTS、翻译、OCR、声纹、输入法、学习机、口语评测、车载语音、会议转写。
4. 准备 AI 测试指标：CER/WER、MOS、字段准确率、召回率、误报漏报、幻觉率、工具调用成功率。
5. 准备 AI + 测试方案：AI 用例生成、RAG 测试、Agent 工具调用、大模型安全、模型版本回归和 badcase 闭环。

## 十二、参考来源

- 牛客网：科大讯飞测开一面面经，涉及 UI 自动化适用场景、AI 能力引入测试流程等：https://www.nowcoder.com/feed/main/detail/e1a76e39355747b680d3c633b9437eee
- 测试之家：科大讯飞高级测试工程师/硬件测试工程师招聘，涉及测试流程优化、技术创新、测试工具开发：https://testerhome.com/topics/39099
- 测试之家：科大讯飞性能测试、大数据、AI 测试工程师招聘，涉及性能测试、大数据平台自动化测试框架和 AI 测试：https://testerhome.com/topics/38480
- 牛客网：科大讯飞 AI 算法测试开发测试工程师岗位，涉及 C++、Python、算法、NLP、语音信号处理和模式识别：https://www.nowcoder.com/jobs/detail/340555
- 爱测-测试人社区：科大讯飞 AI 测试面经，涉及测试知识、AI 测试和项目问题：https://ceshiren.com/t/topic/18028
- CSDN：科大讯飞测试开发工程师面试，涉及笔试、编程题和岗位技术问题：https://blog.csdn.net/weixin_44831924/article/details/110314510
- 讯飞开放平台文档中心，覆盖语音识别、语音合成、翻译、OCR、声纹和大模型等产品文档：https://www.xfyun.cn/doc/
- 讯飞开放平台平台简介，提到服务状态监控、自动化部署、自动化测试和日志体系：https://www.xfyun.cn/doc/platform/xfyunreadme.html
- 讯飞开放平台长文本语音合成 API 文档，涉及密钥、接口调用和长文本合成限制：https://www.xfyun.cn/doc/tts/long_text_tts/API.html
- 科大讯飞春招补录招聘简章，包含测试开发工程师、AI 算法测试开发工程师、测试工程师等岗位：https://jobcareer.sdu.edu.cn/eweb/servlet/resdownload?id=KwcBpyDaKkFXW2UWhn5Lns&type=getfile

