# Shopee 测试开发工程师面试题整理

生成时间：2026-05-31  
岗位方向：测试开发工程师 / SDET / QA Automation / 电商质量 / 金融科技质量 / 自动化测试 / AI 测试  
适用场景：Shopee/虾皮电商、ShopeePay、数字银行、跨境交易、卖家中心、物流履约、广告推荐、东南亚多语言多币种业务。

## 一、面试画像

Shopee 测试开发面试常见特点是基础面较广，既问计算机网络、数据库、操作系统、并发、SQL，也会深挖项目、接口自动化、Mock、性能测试、测试工具和测试用例设计。公开面经中高频出现 HTTP 报文、B+ 树索引、事务、乐观锁悲观锁、TCP 可靠传输、进程线程、URL 到页面呈现、双十一性能测试、Web 登录测试、cookie/session、死锁、GET/POST、SQL 查询、字符串处理等。岗位描述中也强调 Java/Python、持续集成、白盒测试、接口测试、自动化框架和测试工具二次开发能力。

注意：本文不是官方题库，而是基于公开面经、招聘信息、Shopee 电商/金融业务特点和测试开发高频考点整理出的复习材料。

复习优先级建议：

1. 基础能力：HTTP/TCP、操作系统、MySQL、Redis、并发、锁、SQL、数据结构与算法。
2. 自动化能力：接口自动化、UI 自动化、Mock、测试框架、CI/CD、报告和失败归因。
3. 电商业务：商品、搜索、购物车、下单、库存、优惠、支付、退款、物流、卖家后台。
4. 跨境业务：多语言、多币种、税费、汇率、海外支付、合规、时区和地址格式。
5. AI 能力：AI 用例生成、智能回归、用户评论分析、LLM 客服、RAG、Agent 工具调用测试。

## 二、计算机基础

### 1. Cookie 和 Session 有什么区别？

**参考答案：**  
Cookie 存储在客户端，通常用于保存会话标识、偏好设置等；Session 存储在服务端，通过 session id 与客户端关联。Cookie 容量有限且容易被篡改，所以敏感信息不应直接存储在 Cookie 中。Session 更安全但需要服务端存储和分布式会话管理。电商登录、购物车、结算和支付链路都要测试登录态过期、跨端同步、退出登录、cookie 被清除和 session 失效。

**AI 相关加分点：**  
AI 生成登录测试用例时，要覆盖认证、授权、会话过期和敏感信息泄露，不要只测登录成功。

### 2. 进程和线程有什么区别？

**参考答案：**  
进程是资源分配单位，有独立地址空间；线程是 CPU 调度单位，同一进程内线程共享内存。线程切换成本较低，但共享数据需要考虑线程安全。测试开发做并发执行器、压测工具、自动化任务调度和 CI/CD 平台时，需要理解进程隔离、线程池、队列、锁和资源限制。

**AI 相关加分点：**  
AI 测试 Agent 并发执行任务时，要限制浏览器实例、模型请求、文件句柄和测试环境资源。

### 3. 什么是死锁？死锁的四个必要条件是什么？

**参考答案：**  
死锁是多个线程或进程相互等待对方释放资源，导致无法继续执行。四个必要条件是互斥、占有且等待、不可抢占、循环等待。排查死锁可以查看线程 dump、锁等待日志、数据库锁、连接池和超时。预防方式包括固定加锁顺序、减少锁粒度、设置超时、避免嵌套锁和使用无锁结构。

**AI 相关加分点：**  
AI 可以辅助阅读线程 dump，但必须输出锁对象、等待线程、持有线程和证据链。

### 4. MySQL 事务是什么？ACID 分别代表什么？

**参考答案：**  
事务是一组数据库操作的逻辑单元，要么全部成功，要么全部失败。ACID 分别是原子性、一致性、隔离性、持久性。电商业务中下单、扣库存、支付、发券、退款、卖家结算都需要事务或最终一致性保障。测试时要覆盖失败回滚、并发、重复请求、MQ 重投和对账。

**AI 相关加分点：**  
AI 可以生成事务异常路径，但库存、金额和订单状态必须用确定性断言和对账验证。

### 5. 乐观锁和悲观锁有什么区别？

**参考答案：**  
悲观锁假设冲突频繁，操作前先加锁，例如数据库行锁；乐观锁假设冲突较少，更新时通过版本号或 CAS 判断是否被修改。悲观锁适合强一致和冲突多的场景，但并发性能较低；乐观锁适合读多写少场景，但需要处理更新失败重试。秒杀库存、优惠券库存、订单状态流转都要设计并发测试。

**AI 相关加分点：**  
AI 可枚举并发路径，但是否超卖、重复领取、状态错乱要通过压测和最终数据验证。

### 6. HTTP GET 和 POST 有什么区别？

**参考答案：**  
GET 通常用于读取资源，参数常放在 URL 中，具备幂等语义；POST 常用于提交或创建资源，请求体承载数据，通常不保证幂等。接口测试不能只看方法名，还要看业务语义、权限、幂等、参数校验和响应断言。对于支付、下单、发券等写操作，要重点测重复提交和网络重试。

**AI 相关加分点：**  
AI 生成接口用例时容易只断言 200，需要补充业务状态、数据库、缓存、消息和幂等断言。

### 7. TCP 如何保证可靠传输？

**参考答案：**  
TCP 通过序列号、确认应答、超时重传、滑动窗口、拥塞控制、流量控制和校验和保证可靠传输。测试开发在压测和弱网测试中要关注超时、重试、连接池、丢包、乱序、网络切换和请求幂等。跨境电商还要关注海外网络延迟和区域链路质量。

**AI 相关加分点：**  
AI 生成弱网测试方案时，要明确延迟、丢包、抖动、限速、网络切换和重试策略。

### 8. 从输入 URL 到页面展示发生了什么？

**参考答案：**  
大致包括 DNS 解析、建立 TCP 连接、TLS 握手、发送 HTTP 请求、服务端处理、返回响应、浏览器解析 HTML/CSS/JS、构建 DOM/CSSOM、布局、绘制和执行脚本。测试时可以从网络、缓存、CDN、接口耗时、首屏性能、前端错误、资源加载和兼容性排查页面问题。

**AI 相关加分点：**  
AI 可以分析前端性能报告，但要结合真实指标，如 FCP、LCP、TTI、接口耗时和错误日志。

## 三、编码、SQL 与算法

### 9. 写 SQL：一个成绩表中查找成绩最高和最低的学生。

**参考答案：**  
如果表结构为 `score(student_id, student_name, score)`，可以使用子查询：

```sql
select *
from score
where score = (select max(score) from score)
   or score = (select min(score) from score);
```

如果要分别标记最高和最低，可加一列类型。要考虑并列最高/最低、空表、NULL 分数和索引。

**AI 相关加分点：**  
AI 生成 SQL 后，要检查并列值、NULL、排序和性能，不要只返回一条。

### 10. 写 SQL：输出所有成绩大于 80 的学生。

**参考答案：**  
假设表为 `score(student_id, student_name, score)`：

```sql
select student_id, student_name, score
from score
where score > 80;
```

如果涉及多张表，例如学生表和成绩表，需要 join 并明确科目、考试批次和是否去重。

**AI 相关加分点：**  
回答时主动澄清表结构和业务语义，是面试中非常加分的测试思维。

### 11. 如何统计字符串中的数字、英文字符、中文字符数量？

**参考答案：**  
遍历字符串，分别判断字符类型。Python 中可以用 `isdigit`、`isalpha` 和 Unicode 范围判断中文。要注意英文和中文都可能被 `isalpha` 识别，因此中文判断应优先或单独处理。

```python
def count_chars(s: str):
    nums = letters = chinese = others = 0
    for ch in s:
        if '\u4e00' <= ch <= '\u9fff':
            chinese += 1
        elif ch.isdigit():
            nums += 1
        elif ('a' <= ch <= 'z') or ('A' <= ch <= 'Z'):
            letters += 1
        else:
            others += 1
    return nums, letters, chinese, others
```

**AI 相关加分点：**  
AI 生成代码后，要补 emoji、空格、标点、全角数字和多语言字符测试。

### 12. 如何判断回文数或回文字符串？

**参考答案：**  
可以使用双指针，从两端向中间比较。若忽略大小写、空格和标点，需要先做规范化处理。测试用例包括空串、单字符、奇偶长度、大小写、中文、标点、负数和末尾为 0 的数字。

**AI 相关加分点：**  
AI 生成实现前要明确需求：是否忽略大小写、是否忽略标点、是否支持 Unicode。

### 13. 如何实现 LRU 缓存？

**参考答案：**  
使用哈希表加双向链表。哈希表支持 O(1) 查找，双向链表维护访问顺序。读取或写入时把节点移动到头部，容量超限时淘汰尾部节点。要处理更新已有 key、容量为 0、删除映射和并发访问。

**AI 相关加分点：**  
AI 写 LRU 时常漏掉更新已有 key 后移动节点或删除旧映射，测试用例要覆盖。

### 14. 如何测试一个排序函数？

**参考答案：**  
不仅检查输出有序，还要检查元素数量不变、频次不变、没有丢失和新增。如果要求稳定排序，还要验证相等元素相对顺序。用例包括空数组、单元素、已排序、逆序、重复元素、负数、极值、大量随机数和局部有序。

**AI 相关加分点：**  
可用 property-based testing，让 AI 辅助生成属性断言，再由程序自动验证。

## 四、电商与金融业务场景

### 15. 如何测试 Web 登录页面？

**参考答案：**  
覆盖账号密码登录、验证码、记住登录、忘记密码、第三方登录、输入校验、错误提示、登录态、退出登录、权限跳转和安全风控。异常包括账号不存在、密码错误、频繁失败、验证码过期、账号冻结、Cookie 禁用、Session 过期、弱网和浏览器兼容。安全上要覆盖 SQL 注入、XSS、CSRF、暴力破解和敏感信息脱敏。

**AI 相关加分点：**  
AI 可以生成基础用例，但安全、风控和登录态边界要人工补充。

### 16. 如何测试 Shopee 商品详情页？

**参考答案：**  
覆盖商品标题、图片、视频、价格、库存、规格、评价、店铺、优惠、配送、售后、收藏、分享、推荐和埋点。异常包括商品下架、库存不足、价格变更、规格失效、图片加载失败、缓存不一致、弱网和多语言展示。断言要覆盖前端展示、商品中心、价格中心、库存中心和缓存。

**AI 相关加分点：**  
AI 可生成商品类型矩阵，但价格和库存必须做确定性断言。

### 17. 如何测试购物车？

**参考答案：**  
覆盖添加、删除、修改数量、勾选、批量操作、跨店铺、价格变化、库存变化、优惠计算、失效商品、登录态同步、多端同步和结算跳转。异常包括重复点击、网络重试、商品下架、活动结束、库存不足和跨境不可配送。

**AI 相关加分点：**  
AI 可生成组合用例，但优惠、库存、价格和跨境配送规则要精确校验。

### 18. 如何测试下单链路？

**参考答案：**  
覆盖地址、商品、库存、价格、优惠、运费、税费、发票、支付、订单创建、消息通知、订单状态和售后入口。异常包括库存不足、优惠失效、地址不可达、支付失败、重复提交、超时、风控拦截和服务降级。

**AI 相关加分点：**  
AI 可生成链路清单，但交易链路必须做数据一致性和资金对账。

### 19. 如何测试优惠券和促销活动？

**参考答案：**  
覆盖领券、用券、过期、退回、叠加规则、门槛、用户资格、商品范围、店铺范围、活动库存、风控、防刷和财务对账。并发场景验证重复领取、库存扣减、事务回滚和 MQ 重投。配置场景验证活动开始结束时间、灰度人群和异常配置拦截。

**AI 相关加分点：**  
可以让 AI 把复杂规则转成决策表，但金额、库存和财务断言必须精确。

### 20. 如何测试支付和退款？

**参考答案：**  
支付覆盖下单、支付渠道、支付回调、支付超时、重复支付、取消、补单和对账。退款覆盖全额、部分、优惠退回、库存恢复、责任方、原路退回和延迟到账。跨境场景还要关注多币种、汇率、税费和本地支付渠道差异。

**AI 相关加分点：**  
AI 可以枚举支付异常路径，但资金链路要保留确定性断言和对账。

### 21. 如何测试卖家中心商品发布？

**参考答案：**  
覆盖类目、标题、图片、视频、规格、价格、库存、运费模板、跨境物流、资质、审核、上架、下架和编辑。异常包括图片违规、价格越界、库存为 0、资质缺失、审核拒绝、重复提交和权限越权。

**AI 相关加分点：**  
AI 可生成类目和规格组合，但资质与内容合规要结合规则引擎和人工审核样本。

### 22. 如何测试跨境物流？

**参考答案：**  
覆盖揽收、仓库入库、国际干线、清关、末端派送、签收、拒收、退货和异常件。验证轨迹顺序、时间、节点、地图展示、推送通知和多端一致性。异常包括重复轨迹、轨迹丢失、时间倒挂、包裹拆分、清关失败和延迟更新。

**AI 相关加分点：**  
AI 可从异常轨迹中聚类问题类型，但轨迹状态机和 SLA 要用规则校验。

### 23. 如何测试 ShopeePay 或数字银行转账？

**参考答案：**  
覆盖绑卡、充值、付款、转账、提现、退款、限额、实名、风控、账单和通知。异常包括余额不足、重复提交、支付回调重复、账号冻结、超限、网络重试和合规拦截。重点验证资金一致性、幂等、审计日志和对账。

**AI 相关加分点：**  
AI 可以生成风险矩阵，但资金链路要保留人工复核和资金对账。

### 24. 如何测试多语言多币种？

**参考答案：**  
覆盖语言切换、币种换算、汇率、税费、地址格式、手机号格式、当地支付、客服文案、隐私政策和本地法规。异常包括汇率更新、翻译缺失、金额四舍五入、跨时区订单、地址不可达和当地合规限制。

**AI 相关加分点：**  
AI 可生成多语言文案和地址数据，但汇率、税费和合规规则必须按当地规则校验。

## 五、自动化测试与质量平台

### 25. 如何设计接口自动化测试框架？

**参考答案：**  
底层封装请求、鉴权、环境、日志、重试；中间层封装业务 API、测试数据和依赖提取；上层编排场景、断言和报告。框架要支持参数化、Mock、并发执行、失败重跑、CI 集成、历史趋势和质量看板。

**AI 相关加分点：**  
AI 可根据 OpenAPI 生成基础用例和 client，但业务断言、数据准备和幂等校验需要测试开发把关。

### 26. 服务端坏了但前端需要取数据，如何测试？

**参考答案：**  
可以使用 Mock 服务、契约文档、静态数据、本地接口或录制回放来测试前端展示、交互、输入校验、错误提示和埋点。等真实服务恢复后，再补充接口联调、数据一致性和端到端回归。关键是明确 Mock 覆盖范围和后续真实链路验证计划。

**AI 相关加分点：**  
AI 可根据接口契约生成 Mock 数据，但业务边界、异常码和权限规则需要人工确认。

### 27. 自动化测试为什么会不稳定？如何治理？

**参考答案：**  
原因包括环境波动、数据依赖、异步等待、元素定位脆弱、接口限流、第三方依赖、用例顺序耦合和断言不合理。治理手段包括独立测试数据、显式等待、稳定定位、Mock、失败截图/日志/trace、重试分级、失败归因、用例隔离和稳定性看板。

**AI 相关加分点：**  
LLM 可做失败归因，但分类必须附带错误栈、截图、接口响应和环境信息。

### 28. 如何把自动化测试接入 CI/CD？

**参考答案：**  
主干流水线跑单元测试、静态扫描、接口冒烟、契约测试和少量核心 UI；夜间任务跑完整回归、兼容性和性能基线。发布前增加核心交易链路、支付退款、库存、优惠、跨境物流和灰度验证。

**AI 相关加分点：**  
AI 可根据代码 diff 和接口变更推荐回归范围，但发布阻断规则要明确可解释。

### 29. 如何设计质量看板？

**参考答案：**  
包括需求进度、用例通过率、自动化覆盖率、构建失败率、缺陷趋势、线上缺陷、回归耗时、接口错误率、P95/P99、下单成功率、支付成功率、退款异常、物流异常、卖家投诉、灰度异常和用户反馈。

**AI 相关加分点：**  
LLM 可生成质量日报，但原始指标和计算口径必须可追溯。

### 30. 如何做测试数据管理？

**参考答案：**  
要解决数据构造、隔离、复用、清理、脱敏和一致性。Shopee 场景需要支持买家、卖家、商品、库存、优惠、订单、支付、退款、物流、国家/地区、币种和风控标签的可追踪构造，并保证用例可重复。

**AI 相关加分点：**  
AI 生成数据时要遵守隐私和业务约束，尤其是账号、支付、地址、手机号和身份信息。

## 六、性能、稳定性与安全

### 31. 双十一或大促如何测试服务器性能？

**参考答案：**  
先梳理核心链路：搜索、商品详情、购物车、下单、库存、优惠、支付、物流、卖家中心。再建立真实流量模型，做容量评估、全链路压测、限流降级、预案演练、监控告警、灰度验证和应急值守。指标包括 QPS、P95/P99、错误率、下单成功率、支付成功率、库存一致性和用户投诉。

**AI 相关加分点：**  
AI 可从历史故障和压测报告中提取风险清单，但应急动作必须绑定预案和人工确认。

### 32. 如何测试限流和降级？

**参考答案：**  
限流验证阈值、维度、返回码、提示、恢复和误伤；降级验证核心链路可用、非核心功能关闭、缓存兜底、默认值和用户体验。可模拟价格服务异常、库存服务慢、支付回调延迟、物流服务超时和广告服务不可用。

**AI 相关加分点：**  
AI 可根据依赖图生成故障注入清单，但执行要在可控环境逐步扩大。

### 33. 如何测试黑灰产攻击防护？

**参考答案：**  
覆盖批量注册、刷单、薅券、恶意退款、虚假评价、爬虫、撞库、机器下单、作弊点击和卖家违规。验证风控规则、限流、验证码、设备指纹、账号画像、异常行为检测、处置策略和误伤率。

**AI 相关加分点：**  
AI 可生成攻击变体和异常样本，但风控策略要防泄露并关注误杀正常用户。

### 34. 如何测试数据准确性？

**参考答案：**  
从采集、清洗、计算、存储、查询和展示全链路验证。常用方法包括源表与结果表对账、行数校验、金额汇总、空值/重复值、分区、延迟和血缘分析。电商场景重点对订单金额、优惠金额、税费、库存、支付、退款、物流时效和卖家结算做对账。

**AI 相关加分点：**  
AI 可生成 SQL 和异常摘要，但关键金额与库存指标必须用确定性规则对账。

### 35. 线上 P0 故障时，测试开发应该做什么？

**参考答案：**  
先协助止血，确认影响范围、版本、时间、国家/地区、用户群和核心指标，推动降级、回滚或配置关闭。然后保留日志、trace、监控、配置和变更记录。恢复后参与复盘，补充自动化用例、监控告警、发布门禁和故障演练。

**AI 相关加分点：**  
AI 可快速摘要日志和告警，但故障现场高风险操作必须人工确认。

## 七、AI + 测试专项

### 36. 如何用 AI 辅助生成测试用例？

**参考答案：**  
把 PRD、接口文档、历史缺陷、业务规则、代码 diff 和线上 badcase 输入 AI，让它生成场景清单、边界值、异常路径、决策表和脚本草案。风险包括幻觉、遗漏规则、重复低价值用例、泄露敏感数据和断言不准确。要做脱敏、人工评审、规则校验和执行反馈闭环。

**AI 相关加分点：**  
AI 是初稿生成器和覆盖提醒器，最终测试责任仍在测试开发。

### 37. 如何做智能回归测试选择？

**参考答案：**  
输入包括代码 diff、接口变更、配置变更、用例覆盖关系、历史缺陷、近期失败、核心链路和风险等级。规则层保证商品、下单、库存、支付、退款、物流、卖家中心等核心链路必跑，模型层做风险排序和额外推荐。

**AI 相关加分点：**  
交易和资金链路不能完全交给模型裁剪，AI 更适合做推荐和排序。

### 38. 如何测试 LLM 客服或卖家助手？

**参考答案：**  
覆盖意图识别、订单查询、退款政策、物流查询、商品发布帮助、广告投放建议、多轮上下文、知识库检索、转人工、拒答、隐私保护、响应速度和成本。重点测试错误承诺、越权查询、提示注入、隐私泄露、政策幻觉和多语言质量。

**AI 相关加分点：**  
LLM-as-judge 可辅助评测，但需要人工标注样本校准，且高风险答案要人工审核。

### 39. 如何测试 RAG 应用？

**参考答案：**  
RAG 测试要拆成检索质量和生成质量。检索侧关注文档切分、向量召回、重排、权限隔离和知识更新；生成侧关注答案正确性、引用准确性、无答案拒答、幻觉和一致性。指标包括召回率、答案准确率、引用正确率、延迟和成本。

**AI 相关加分点：**  
跨境规则、退货政策、物流限制更新频繁，RAG 必须测试知识时效性和证据引用。

### 40. 如何用 AI 分析用户评论和商品评价？

**参考答案：**  
可以把商品评论、客服工单、售后原因、星级、图片、国家/地区和业务标签输入模型，做情感分析、聚类、摘要、质量问题发现和责任模块推荐。落地时要脱敏、结构化输出，并与规则、历史缺陷和商家数据交叉验证。

**AI 相关加分点：**  
可先用向量检索找相似历史问题，再让 LLM 总结差异，比直接让模型猜更可靠。

### 41. 如何测试 AI Agent 的工具调用？

**参考答案：**  
覆盖工具选择、参数生成、权限控制、失败重试、超时、结果校验、多工具编排和审计日志。Agent 不应在未授权情况下执行退款、取消订单、修改商品价格、调整广告预算、封禁账号或外发数据等高风险动作。

**AI 相关加分点：**  
建立工具调用黄金集和红队集，量化成功率、误调用率、越权率和任务完成率。

## 八、项目与行为面试

### 42. 介绍一个你做过的自动化测试项目。

**参考答案：**  
用 STAR 结构回答：背景、痛点、职责、方案、难点、收益。要讲覆盖率、执行时长、发现缺陷、稳定性、接入流水线和团队使用情况。不要只说写脚本，要体现平台化和业务收益。

**AI 相关加分点：**  
可以补充如何用 AI 生成初版用例、分析失败日志或总结报告，并说明人工校验机制。

### 43. 开发不认为你提的是 bug，怎么办？

**参考答案：**  
补充复现步骤、环境、版本、日志、截图、接口请求、数据库状态和预期依据。再和开发对齐需求或设计，确认是缺陷、体验问题、需求不明确还是测试误判。争议仍在时拉产品、架构或业务负责人决策。

**AI 相关加分点：**  
AI 可以整理缺陷描述和影响范围，但不能替代真实证据。

### 44. 大促上线时间被压缩怎么办？

**参考答案：**  
先确认变更范围、影响链路、风险等级和回滚方案。按风险优先级保留搜索、商品、购物车、下单、库存、优惠、支付、退款、物流等核心用例。明确未覆盖风险并同步相关方，上线后加强监控和应急值守。

**AI 相关加分点：**  
AI 可推荐最小回归集，但最终选择要结合业务影响和历史缺陷。

### 45. 你如何理解测试开发岗位？

**参考答案：**  
测试开发是用工程化能力提升质量效率的岗位。它既要理解业务风险，也要能开发工具、框架和平台；既要发现问题，也要推动研发流程改进。优秀测开会从需求、开发、测试、发布、监控、复盘全链路建立质量保障体系。

**AI 相关加分点：**  
AI 时代的测开要把重复分析、初稿生成和报告总结交给 AI，把风险判断和关键验证掌握在自己手里。

## 九、反问面试官

1. 团队测试开发更偏业务质量保障、自动化平台，还是测试基础设施建设？
2. 当前最影响质量效率的问题是什么：自动化覆盖、环境稳定、数据构造、失败定位，还是大促风险？
3. 团队是否已经在使用 AI 辅助测试、智能回归、用户评价分析或 LLM 客服评测？
4. 电商、支付、物流、卖家中心或数字银行业务中，当前最核心的质量指标有哪些？
5. 对新人前三个月的期待是什么？希望先补业务，还是先交付工具能力？

## 十、复习建议

1. 准备两个项目故事：一个自动化/平台项目，一个电商交易或金融支付质量项目。
2. 每天刷 2 道算法题，重点是哈希、栈、字符串处理、回文、LRU、SQL 和并发。
3. 熟悉 Shopee 高频基础题：cookie/session、进程线程、死锁、事务、锁、GET/POST、TCP、HTTP 报文、URL 加载过程。
4. 用 Shopee 业务练场景题：登录、商品、购物车、下单、优惠、支付退款、卖家中心、跨境物流、多语言多币种。
5. 准备 AI + 测试方案：AI 用例生成、智能回归、LLM 客服、RAG、用户评论分析和 Agent 工具调用。

## 十一、参考来源

- 牛客网：Shopee 测开一面面经，涉及 cookie/session、进程线程、死锁、事务、GET/POST 和 SQL：https://www.nowcoder.com/feed/main/detail/e96705b0aeb94b7c8e229f4615b30a54
- 爱测-测试人社区：Shopee 测试岗面经，涉及 HTTP 报文、B+ 树索引、事务、TCP、并发、URL 加载和双十一性能测试：https://ceshiren.com/t/topic/18161
- 爱测-测试人社区：Shopee 测试一面二面凉经，涉及 Web 登录测试、软件测试流程、自动化理解和 Monkey 等工具：https://ceshiren.com/t/topic/18050
- 爱测-测试人社区：Shopee 测试面经，涉及项目深挖和字符串中数字/英文/中文字符统计：https://ceshiren.com/t/topic/17202
- 牛客网：Shopee 测试工程师面经话题，涉及自动化测试框架、Mock、朋友圈点赞测试和算法题：https://www.nowcoder.com/creation/subject/662adbe0d7cc4a1188901f537e056156
- 测试之家：Shopee 测试工程师/测试开发工程师招聘，涉及 Java/Python、持续集成、白盒测试、接口测试、自动化测试和工具二次开发：https://www.testerhome.com/topics/18481
- Dataford：Shopee QA Engineer Interview Guide，涉及 QA 面试流程、自动化测试和 Python/Java 能力：https://dataford.io/interview-guides/shopee/qa-engineer
- NodeFlair：Shopee SDET 面试信息，涉及 SDET/QA/Automation QA 岗位面试数据：https://nodeflair.com/companies/shopee/interviews/software-development-engineer-in-test-sdet
- arXiv：Sentiment Analysis Of Shopee Product Reviews Using Distilbert，作为用户评论情感分析和 AI 质量分析参考：https://arxiv.org/abs/2511.22313

