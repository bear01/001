# JPMorgan Chase 测试开发工程师面试题

## 公司名

JPMorgan Chase

## 岗位方向

测试开发工程师 / SDET / QA Automation Engineer / 金融科技质量工程方向

## 资料来源与整理依据

- [JPMorgan Chase Software Engineer Program](https://www.jpmorganchase.com/careers/explore-opportunities/programs/software-engineer-fulltime)
- [JPMorgan Chase Technology Blog](https://www.jpmorganchase.com/about/technology)
- [JPMorgan Chase Data & Analytics Opportunities](https://www.jpmorganchase.com/careers/explore-opportunities/programs/data-analytics-opportunities)
- [JPMorgan Payments Developer Portal](https://developer.payments.jpmorgan.com/)
- [Global Payments API Overview](https://developer.payments.jpmorgan.com/api/treasury/global-payments/overview)
- 公开候选人面经中常见的软件工程、SDET、自动化测试、Java、SQL、系统设计、行为面和金融场景问题。

> 说明：以下内容不是 JPMorgan Chase 官方题库，而是结合公开招聘信息、金融科技业务场景和测试开发岗位能力模型整理的复习题与模拟题。

## 面试画像

JPMorgan Chase 的技术岗位通常服务于零售银行、支付、投行、资产管理、风控、数据平台、内部工具和全球基础设施。测试开发工程师需要证明自己能在强监管、高可用、高安全要求的环境中交付质量：既要能写代码和自动化框架，也要理解支付链路、账户权限、数据一致性、审计日志、CI/CD、云原生、性能稳定性和 AI/ML 风险控制。

## 题目分类

### 一、岗位理解与金融科技质量

### 1. 你如何理解 JPMorgan Chase 测试开发工程师的核心职责？

参考答案：核心职责是用工程化方式保障金融系统的可靠、安全和可审计。JPMorgan Chase 的系统可能服务消费者、企业客户、交易、支付和内部运营，质量问题会影响资金、风控、合规和客户信任。测试开发不仅要写自动化测试，还要参与需求评审、风险分析、测试平台建设、CI/CD 门禁、生产监控、故障复盘和质量数据分析，把质量嵌入软件生命周期。

### 2. 银行业测试和普通互联网测试有什么不同？

参考答案：银行业更关注资金正确性、权限隔离、审计追踪、合规要求、灾备能力和数据一致性。普通互联网产品可以更快试错，但支付、账户、风控、交易类系统必须严控边界条件和回滚策略。测试用例要覆盖幂等、重复提交、并发扣款、汇率、手续费、交易状态流转、账务对平、权限变更和日志留痕。

### 3. 如果一个支付接口偶发返回成功但客户没有收到入账通知，你会如何排查？

参考答案：先确认接口成功代表哪个阶段成功：请求受理、清算提交、银行处理成功还是最终入账成功。然后按链路排查：请求日志、幂等键、消息队列、状态机、下游支付轨道、通知服务、回调重试和客户配置。测试侧应补充端到端状态断言、异步回调校验、超时补偿、重复通知、消息丢失和人工对账场景。

### 4. 金融系统中为什么幂等测试非常重要？

参考答案：支付、转账和订单类操作常因网络重试、客户端重复点击、队列重复投递或服务超时而重复执行。没有幂等保护可能导致重复扣款、重复入账或状态错乱。测试要验证同一幂等键重复请求只产生一次业务效果，不同请求不会被误合并，并且幂等记录有合理过期时间和审计日志。

### 5. 你如何设计账户余额变更的测试策略？

参考答案：覆盖正常入账、出账、冻结、解冻、冲正、退款、手续费、汇率转换和并发请求。核心断言是账务平衡、余额不可为负、交易流水和余额快照一致、失败操作不产生资金影响。还要验证数据库事务、消息一致性、重试补偿、审计日志和对账任务。并发测试要重点验证锁粒度、隔离级别和最终一致性。

### 6. 如何在面试中展示你具备金融业务学习能力？

参考答案：可以结合项目说明自己如何快速理解领域模型，例如支付状态机、风控规则、账户体系或交易清算。回答时要说清楚你如何阅读文档、和业务方确认边界、把业务规则转化为测试模型，并通过自动化校验防止回归。重点不是假装熟悉所有金融业务，而是证明你能把复杂业务拆成可验证规则。

### 二、API、自动化与支付测试

### 7. 如何测试 Global Payments 这类统一支付 API？

参考答案：需要覆盖鉴权、签名、证书、请求参数、收付款方信息、币种、支付轨道、状态查询、取消、退回、回调和错误码。还要验证同一 API 对不同支付方式的字段差异和状态流转。自动化应包含 mock 下游、沙箱数据、契约测试、幂等测试、回调重放和端到端对账。

### 8. 支付 API 的错误码测试要注意什么？

参考答案：错误码不仅要正确，还要可诊断且不泄露敏感信息。测试要覆盖参数错误、权限不足、签名失败、余额不足、限额超限、重复请求、下游超时、系统忙和未知异常。每类错误要验证 HTTP 状态、业务错误码、错误消息、重试建议、日志和告警。对于资金类接口，失败响应必须确认是否产生业务副作用。

### 9. 如何设计 API 自动化框架？

参考答案：框架应支持环境配置、鉴权封装、请求构造、响应断言、数据驱动、schema 校验、契约测试、报告输出、日志关联和 CI 集成。金融场景还要支持敏感数据脱敏、证书管理、幂等键生成、测试账户隔离、对账校验和审计字段断言。框架设计要强调可维护性，避免把业务数据和断言散落在脚本里。

### 10. 如何测试 API 签名和证书认证？

参考答案：覆盖有效证书、过期证书、吊销证书、错误私钥、签名字段缺失、签名顺序错误、body 被篡改、时间戳过期和重放攻击。还要验证错误提示、审计日志和告警。自动化中要把密钥和证书放在安全配置中，不应硬编码到仓库。

### 11. 如何做支付回调测试？

参考答案：构造成功、失败、处理中、重复回调、乱序回调、延迟回调和签名错误回调。系统应能验证来源、幂等处理、正确更新状态并记录日志。对于乱序场景，例如先收到成功再收到处理中，状态机不能回退。测试还要验证回调失败后的重试策略和死信队列处理。

### 12. 如何测试批量付款文件上传？

参考答案：覆盖文件格式、字段校验、重复记录、超大文件、编码、币种、收款方信息、部分成功、全失败、审批流程和结果下载。自动化可以生成边界文件，并校验批处理结果、失败明细和对账记录。性能测试要关注大文件解析时间、并发上传、队列积压和失败恢复。

### 13. 如何测试支付限额和风控拦截？

参考答案：构造不同用户、账户、币种、地区、交易类型和时间窗口的限额规则，覆盖低于限额、等于限额、超过限额和累计超过限额。风控测试要验证命中规则、拦截原因、人工复核、白名单、黑名单和日志审计。还要检查规则变更是否实时生效，以及缓存是否导致旧规则继续使用。

### 14. UI 自动化在银行业务中应如何取舍？

参考答案：UI 自动化适合覆盖关键客户路径、表单校验、权限展示和端到端冒烟，但不适合承担全部业务规则校验。复杂规则应下沉到 API、服务层和数据层自动化。银行 UI 经常有安全控件、MFA、动态数据和审批流，UI 自动化要稳定可维护，避免过度依赖脆弱定位和固定等待。

### 三、测试平台、CI/CD 与工程实践

### 15. 如果为 JPMorgan Chase 团队建设测试平台，你会提供哪些能力？

参考答案：平台应包含接口自动化、契约测试、测试数据管理、mock 服务、支付状态模拟、批量文件生成、对账校验、性能压测、环境健康检查、报告看板和质量门禁。金融场景要支持敏感数据脱敏、权限隔离、审计追踪、证书配置和合规证据归档。平台目标是让团队快速、可信、可追溯地验证变更。

### 16. 如何把自动化测试接入银行级 CI/CD？

参考答案：提交阶段跑单元测试、静态扫描、依赖漏洞扫描；合并阶段跑接口、契约和关键业务回归；部署测试环境后跑端到端、对账和性能冒烟；灰度后跑生产探针和监控校验。对高风险模块要设置更严格门禁，例如支付、账户、风控和权限。流水线还要保留审计记录，方便合规检查。

### 17. 如何设计质量门禁？

参考答案：质量门禁可以包含测试通过率、关键用例通过、代码覆盖率、漏洞扫描、性能阈值、错误预算、变更审批和生产监控结果。门禁不应只看数量指标，要按业务风险分级。比如支付成功率、重复扣款保护、权限测试和账务对平属于硬门禁；低风险 UI 文案变更可以使用轻量门禁。

### 18. 如何治理 flaky test？

参考答案：先定位原因：异步等待、测试数据冲突、环境不稳定、外部依赖波动、并发污染或断言不合理。治理方法包括数据隔离、mock 外部依赖、显式等待业务事件、用例独立运行、失败上下文采集和稳定性看板。金融系统的 flaky test 风险更高，因为它会削弱团队对门禁的信任，必须持续治理。

### 19. 如何做测试数据管理？

参考答案：银行测试数据要兼顾真实性、安全和可复现。可以使用脱敏生产样本、合成数据、沙箱账户和数据工厂。测试数据要有生命周期管理，运行前创建、运行后清理，并避免不同用例共用可变账户状态。涉及 PII、账户号、证件号和交易记录时必须脱敏，并限制访问权限。

### 20. 如何设计生产探针？

参考答案：探针要模拟真实但低风险的关键路径，例如登录、余额查询、支付状态查询、账单查询和通知服务健康检查。探针不能产生真实资金影响，应使用沙箱账户或只读接口。指标包括成功率、延迟、错误码、数据新鲜度和区域可用性。失败时要自动关联最近发布、依赖服务和日志 trace。

### 21. 如何让自动化测试报告更适合高风险业务？

参考答案：报告应包含用例意图、业务风险、请求响应、日志链接、trace id、测试数据、环境版本、证书配置、错误码和对账差异。对于失败用例，要能快速判断是代码缺陷、环境问题、数据问题还是依赖波动。报告还应支持审计导出，方便复盘和合规证明。

### 22. 代码评审中你会从测试角度关注什么？

参考答案：关注边界条件、异常处理、重试幂等、事务一致性、权限校验、敏感信息日志、并发安全、监控告警和可测试性。金融系统还要检查金额精度、币种、时区、状态机、审计字段和回滚方案。好的测试开发应在代码评审阶段提前发现质量风险，而不是等测试执行才发现。

### 23. 如何评估自动化测试的效果？

参考答案：不要只看用例数量，而要看缺陷提前发现率、回归时间下降、线上事故减少、门禁拦截有效性、用例稳定性和维护成本。金融场景中，少量覆盖资金、权限和风控核心链路的自动化，比大量低价值 UI 自动化更重要。可以通过质量看板持续跟踪这些指标。

### 四、性能、稳定性与分布式系统

### 24. 如何设计支付系统的性能测试？

参考答案：先确定业务模型：峰值 TPS、支付方式分布、查询比例、回调比例、地区分布和下游延迟。压测要覆盖正常峰值、突刺流量、下游慢响应、批量任务并发和网络抖动。指标包括吞吐、P95/P99 延迟、错误率、队列积压、线程池、数据库连接、CPU、内存和恢复时间。资金类系统还要校验压测后的账务一致性。

### 25. 如何测试系统的灾备和多活能力？

参考答案：覆盖单节点故障、可用区故障、区域故障、数据库主从切换、消息队列故障和配置中心不可用。测试要验证 RTO、RPO、流量切换、数据一致性、重复处理和客户可感知影响。演练前要有回滚计划，演练后要做复盘并补充自动化探针。

### 26. 如何排查接口 P99 延迟升高？

参考答案：先确认影响范围，然后按链路看应用、网关、缓存、数据库、消息队列、下游服务和外部支付轨道。使用 trace 抽样定位慢环节，再结合最近发布、配置变更、流量变化和资源指标分析。测试开发可用压测或回放复现问题，并补充性能回归用例和监控阈值。

### 27. 如何测试消息队列在支付链路中的可靠性？

参考答案：覆盖消息重复、丢失、乱序、延迟、死信、消费者重启和积压。系统应通过幂等键、状态机和补偿任务保证最终一致。测试要断言业务最终状态，而不仅是消息消费成功。还要验证监控指标，例如 lag、消费失败率、重试次数和死信数量。

### 28. 分布式事务失败时如何测试补偿机制？

参考答案：构造不同阶段失败：扣款成功但记账失败、记账成功但通知失败、下游超时但实际成功、补偿任务重复执行。系统要通过状态机、重试、冲正、人工处理队列和对账发现异常。测试要验证补偿不会造成二次错误，并且所有操作可审计。

### 29. 如何测试数据库迁移？

参考答案：覆盖 schema 兼容、默认值、索引、历史数据迁移、回滚脚本、双写、灰度读和数据校验。金融系统迁移要特别关注金额精度、时间字段、状态枚举和审计字段。上线前可用影子流量或离线对账验证迁移前后结果一致。

### 30. 如何验证日志和监控不会泄露敏感信息？

参考答案：定义敏感字段清单，如账户号、证件号、手机号、token、密钥和完整卡号。自动化扫描日志、错误响应、监控标签和测试报告，确认敏感字段被脱敏或不输出。还要检查异常堆栈、第三方 SDK 日志和调试开关，防止低级别日志在生产开启。

### 31. 如何设计银行系统的可观测性？

参考答案：要有指标、日志、trace 和审计事件。指标关注成功率、延迟、错误码、队列积压、资金状态和对账差异；日志要结构化并包含 trace id；trace 要覆盖网关、服务、数据库和消息；审计事件要记录关键业务操作。测试时要验证监控触发、告警路由和故障定位能力。

### 五、AI、数据分析与智能质量工程

### 32. JPMorgan Chase 这类机构使用 AI/ML 时，测试重点是什么？

参考答案：重点是准确性、稳定性、可解释性、公平性、安全性、隐私和合规。AI 模型可能用于风控、反欺诈、客户服务、文档处理、开发效率和数据分析。测试要覆盖数据漂移、特征缺失、模型版本、阈值变化、异常输入、人工复核和审计记录，不能只看离线指标。

### 33. 如何测试反欺诈模型？

参考答案：构建正常交易、可疑交易、边界交易和历史欺诈样本，评估召回率、误报率、漏报率、延迟和规则解释。还要测试模型在新地区、新支付方式、节假日和促销高峰下的稳定性。上线后要监控漂移和策略效果，避免模型长期偏离业务实际。

### 34. 如何测试 LLM 客服助手？

参考答案：覆盖账户查询、交易解释、费用说明、投诉处理、无权限请求和敏感问题。关键断言是不能泄露隐私、不能编造账户信息、不能绕过认证、不能给出不合规建议。回答应可追溯到知识库，遇到高风险问题要转人工。还要做提示注入、越权检索和多轮对话状态测试。

### 35. 如何用 AI 辅助生成测试用例？

参考答案：可以让 LLM 基于需求、接口文档、代码 diff、历史缺陷生成候选测试点，尤其适合边界值、组合参数、异常流程和负向用例。测试开发要做审查、去重和风险分级，然后转化为自动化。AI 生成内容要保留来源和 prompt，不能无审查进入门禁。

### 36. 如何用 LLM 做日志分析？

参考答案：先用规则、检索和聚合缩小日志上下文，再让 LLM 总结异常模式、关联最近变更和给出排查建议。输出必须带证据，例如 trace id、错误码、时间窗口和指标变化。高风险操作不能由模型自动执行，模型只能辅助定位和生成排查清单。

### 37. 如何测试 RAG 内部知识库问答？

参考答案：测试检索权限、召回质量、引用准确、答案忠实、无答案拒答和时效性。银行内部知识库可能包含权限分级文档，因此必须验证用户只能检索被授权内容。还要测试提示注入、敏感信息泄露、过期政策和相似条款混淆。

### 38. 如何用数据分析提升测试效率？

参考答案：可以分析历史缺陷、代码变更、用例失败、线上告警和模块复杂度，建立风险模型来推荐回归范围。数据分析也可以识别 flaky test、低价值用例和高缺陷模块。关键是让推荐结果可解释，帮助团队决策，而不是替代测试判断。

### 39. AI 生成自动化脚本有哪些风险？

参考答案：风险包括断言无效、等待不稳定、误用真实数据、泄露密钥、依赖不安全包、误删环境数据和生成不可维护代码。应在沙箱运行，做代码审查、安全扫描和小范围试点。对资金类系统，AI 脚本不能直接操作真实资金或生产数据。

### 六、编码、SQL、Linux 与系统设计

### 40. 编程题：实现一个 LRU Cache。

参考答案：使用哈希表加双向链表。哈希表用于 O(1) 查找节点，双向链表维护访问顺序。`get` 命中后移动到链表头，`put` 更新或插入节点，容量超限时删除链表尾。回答时要说明边界条件：容量为 0、重复 key、并发访问和内存释放。

### 41. 编程题：给定交易流水，找出重复支付请求。

参考答案：可以用哈希表记录幂等键或业务唯一键，例如 payer、payee、amount、currency、merchant_order_id 和时间窗口。若同一唯一键在窗口内出现多次，标记为重复。真实系统不能只按金额判断，还要结合订单号、请求 id 和状态。测试要覆盖同金额不同订单、同订单重试和跨天请求。

### 42. 编程题：设计一个滑动窗口限流器。

参考答案：可使用队列保存请求时间戳，请求进入时清理窗口外时间戳，若队列长度小于阈值则允许，否则拒绝。高并发场景可用 Redis sorted set 或令牌桶近似。要说明时间复杂度、内存上限、分布式一致性、时钟偏差和用户维度隔离。

### 43. SQL 题：查询过去 1 小时失败率最高的前 10 个支付渠道。

参考答案：

```sql
SELECT channel,
       COUNT(*) AS total_count,
       SUM(CASE WHEN status = 'FAILED' THEN 1 ELSE 0 END) AS failed_count,
       SUM(CASE WHEN status = 'FAILED' THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS fail_rate
FROM payment_transactions
WHERE created_at >= CURRENT_TIMESTAMP - INTERVAL '1 hour'
GROUP BY channel
HAVING COUNT(*) >= 100
ORDER BY fail_rate DESC
LIMIT 10;
```

实际回答要补充样本量阈值，否则小流量渠道可能误排第一。还可以按地区、币种和错误码继续 drill down。

### 44. Linux 题：线上服务内存持续增长，你如何排查？

参考答案：先用 `top`、`ps`、`free`、`pmap` 或容器指标确认增长进程，再结合语言工具查看堆、对象数量、GC、线程和 native memory。检查最近变更、缓存策略、连接池、队列积压和大对象。测试开发要补充长稳测试和内存基线，验证修复后增长停止。

### 45. 系统设计题：设计一个支付状态查询与通知系统。

参考答案：系统包括支付请求服务、状态存储、状态机、消息队列、回调服务、查询 API、重试任务和监控告警。状态变更要幂等，回调要签名并支持重试，查询 API 要做权限校验和缓存控制。测试重点是状态流转、乱序回调、重复通知、下游超时、查询一致性和审计日志。

### 46. 行为题：你如何推动一个团队接受更严格的质量门禁？

参考答案：先用数据说明当前风险，例如回归耗时、线上缺陷、重复事故和发布回滚。然后从核心链路小范围试点，选择高价值、低维护成本的门禁，如关键接口自动化、契约测试和安全扫描。推动时要和开发一起优化执行速度和失败诊断，避免门禁成为单纯阻碍。最后用指标证明效果，再逐步扩大范围。

## AI 相关加分点

- 能把 LLM 用于需求分析、测试点生成、代码 diff 风险识别和自动化脚本草稿生成。
- 熟悉金融 AI 的安全边界：隐私、权限、审计、提示注入、越权检索和合规回答。
- 能设计反欺诈、风控、客服助手、文档抽取和 RAG 系统的评测集。
- 能用日志、trace 和指标构建智能排障助手，但保留人工确认和证据链。
- 能通过历史缺陷和变更数据做风险回归推荐，提高 CI/CD 效率。
- 理解模型漂移、特征缺失、阈值调整和人工复核对金融模型质量的影响。
- 能将 AI 生成测试资产纳入沙箱运行、代码审查、安全扫描和质量门禁。

## 复习建议

- 编码重点练习哈希表、滑动窗口、队列、堆、树、图、字符串、动态规划基础和复杂度分析。
- 熟悉 Java/Python 自动化框架、API 测试、契约测试、数据库校验、mock、CI/CD 和报告体系。
- 准备支付、账户、风控、权限、账务对平、异步回调和幂等场景的测试设计。
- 系统设计重点准备支付状态机、限流、消息队列、灾备、多活、审计日志和可观测性。
- AI 方向重点准备 RAG、LLM 客服、反欺诈模型、日志分析和 AI 辅助测试生成。
- 行为面用 STAR 法准备质量推动、线上事故、跨团队协作、合规约束和快速学习金融业务的案例。
