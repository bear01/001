# Oracle 测试开发工程师面试题

## 公司名

Oracle

## 岗位方向

测试开发工程师 / SDET / QA Automation Engineer / 云平台与企业应用质量工程方向

## 资料来源与整理依据

- [Oracle Cloud Infrastructure Documentation](https://docs.oracle.com/iaas/)
- [Oracle DevOps Service](https://www.oracle.com/devops/)
- [OCI Generative AI](https://docs.oracle.com/en-us/iaas/Content/generative-ai/home.htm)
- [OCI Monitoring Overview](https://docs.oracle.com/iaas/Content/Monitoring/Concepts/monitoringoverview.htm)
- [Observability and Management in the Cloud](https://docs.oracle.com/en-us/iaas/Content/cloud-adoption-framework/monitoring-visibility-audit.htm)
- [Oracle Artificial Intelligence](https://www.oracle.com/ai)
- [Oracle Fusion AI Governance and Standards](https://docs.oracle.com/en/cloud/saas/fusion-ai/aiaqa/what-ai-governance-and-standards-accreditations-does-oracle-have-for-fusion-applications.html)
- 公开候选人面经中常见的 Oracle / OCI / Fusion Cloud 测试开发方向：Java、数据库、API 自动化、云服务、企业应用、CI/CD、可观测性、AI/GenAI、权限安全和行为面。

> 说明：以下题目不是 Oracle 官方题库，而是结合公开资料、云平台与企业 SaaS 场景、测试开发岗位能力模型整理的高频准备题与模拟题。

## 面试画像

Oracle 的技术场景横跨数据库、OCI 云基础设施、Fusion Cloud Applications、NetSuite、DevOps、Observability、AI/GenAI、数据平台和企业安全。测试开发工程师需要证明自己能在大规模分布式系统与企业关键业务中建立可靠的质量体系，覆盖 API、数据库、权限、集成、AI 输出、性能、稳定性、监控告警和持续交付。

## 题目分类

### 一、岗位理解与 Oracle 业务质量

### 1. 你如何理解 Oracle 测试开发工程师的核心价值？

参考答案：核心价值是用工程化方法保障企业级平台的正确性、稳定性、安全性和可持续交付。Oracle 的产品覆盖数据库、云基础设施、ERP、HCM、SCM、CX、AI 服务和 DevOps 工具，质量问题可能影响客户生产业务、财务数据和合规审计。测试开发要把业务风险转化为自动化测试、契约测试、性能基线、可观测性探针和发布门禁。

### 2. Oracle 云平台测试和普通 Web 应用测试有什么区别？

参考答案：云平台测试更关注多租户隔离、资源生命周期、身份权限、API 稳定性、区域可用性、网络与存储可靠性、配额限流和计费准确性。普通 Web 应用更多关注页面流程和用户体验。OCI 类云平台还要覆盖控制面、数据面、异步任务、故障恢复、监控指标和跨区域复制。

### 3. Oracle 企业应用测试的重点是什么？

参考答案：重点包括业务流程、审批链、权限、财务金额、主数据、报表、审计、集成、配置组合和升级兼容。ERP/HCM/SCM 等系统承载企业关键流程，测试不能只看页面成功，还要验证数据库状态、审计记录、权限隔离、报表结果和下游接口。跨地区、税务、币种、时区和语言也是高风险点。

### 4. 为什么数据库能力对 Oracle 测试开发很重要？

参考答案：Oracle 的核心能力之一是数据库和数据平台，很多质量问题最终体现为数据一致性、事务隔离、索引性能、锁等待、SQL 执行计划和备份恢复。测试开发应能写 SQL 验证业务结果、分析慢查询、构造边界数据、设计数据对账，并理解事务、锁、索引和隔离级别对测试的影响。

### 5. 面试中如何体现你适合 Oracle 这类企业技术公司？

参考答案：可以强调四点：懂企业级业务风险，如权限、审计、数据准确性和合规；懂云平台工程，如 API、异步任务、可观测性和故障恢复；具备自动化与平台化能力，如 Java/Python、CI/CD、测试框架和质量看板；关注 AI 新能力的安全和可控性。最好结合项目案例说明如何把高风险场景沉淀为持续回归资产。

### 6. 如何为一个新上线的 OCI 服务制定测试策略？

参考答案：先区分控制面 API、数据面行为、资源生命周期、权限策略、计费、监控、告警和配额。测试分层包括单元测试、契约测试、API 自动化、集成测试、性能测试、故障注入和发布冒烟。上线前应有灰度、回滚、SLO、告警和 runbook，确保服务异常时能被快速发现和恢复。

### 二、API、云服务与企业集成

### 7. 如何设计 OCI API 自动化测试框架？

参考答案：框架应包含鉴权签名、租户与 compartment 配置、资源创建和清理、请求构造、schema 校验、错误码断言、幂等键、异步 work request 轮询、日志采集和 CI 报告。用例层表达业务场景，底层封装 HTTP、SDK 和重试。资源清理非常重要，避免产生孤儿资源和额外成本。

### 8. 如何测试云资源生命周期？

参考答案：覆盖创建、查询、更新、停止、启动、扩容、删除、恢复、标签变更和权限变更。异步资源要验证中间状态、最终状态、超时、失败原因和幂等删除。还要检查控制台、API、CLI、SDK 和监控指标是否一致。

### 9. 如何测试云服务的权限策略？

参考答案：构造不同用户、组、动态组、策略、compartment 和资源类型，验证允许、拒绝、继承和最小权限。负向测试包括跨 compartment 访问、无策略访问、只读用户执行写操作、服务主体权限不足和策略变更延迟。还要验证审计日志能记录关键操作。

### 10. 如何测试 API 幂等性？

参考答案：对创建、更新、删除等接口使用相同请求 ID 或幂等键重复调用，验证不会重复创建资源或产生双重扣费。覆盖网络超时后重试、服务端已执行但客户端未收到响应、并发重复提交和删除后再次删除。断言要检查资源数量、最终状态、事件日志和计费记录。

### 11. 如何测试云平台中的异步任务？

参考答案：OCI 类服务经常使用 work request 或后台任务。测试要覆盖任务入队、状态流转、进度、失败原因、取消、重试、超时和最终一致性。自动化不能只看提交接口返回成功，还要轮询任务状态并验证实际资源状态和审计日志。

### 12. 如何测试 SDK 兼容性？

参考答案：选择 Java、Python、Go、Node 等关键 SDK，验证同一 API 在不同 SDK 中的请求构造、序列化、错误处理、重试和分页行为一致。对于服务端 schema 变更，要通过契约测试和 SDK 集成测试发现破坏性变化。还要关注 SDK 的默认超时、重试策略和日志脱敏。

### 13. 如何测试 Oracle Fusion Cloud 与第三方系统集成？

参考答案：先梳理源系统、目标系统、对象映射、触发方式、鉴权、转换规则、错误处理和重试策略。测试覆盖正常同步、字段缺失、重复数据、乱序事件、部分成功、权限不足、下游超时和手工补偿。最终断言应包括业务对象、集成日志、审计记录和报表结果。

### 14. 如何测试 API 版本兼容？

参考答案：建立关键消费者契约，覆盖字段类型、必填规则、枚举、错误码和分页协议。新版本发布时运行 API diff 和多版本回归，确保旧客户端仍能工作。对破坏性变更要有弃用周期、迁移文档、灰度策略和监控。

### 三、数据库、数据一致性与报表

### 15. 如何测试事务一致性？

参考答案：设计包含多表写入、失败回滚、并发更新和异常中断的场景，验证原子性、一致性、隔离性和持久性。比如订单、发票或薪资计算中任一步失败都不能留下半成品数据。断言要检查业务表、审计表、消息事件和报表是否一致。

### 16. 如何测试数据库隔离级别相关问题？

参考答案：构造并发读写、重复读、幻读、脏读和锁等待场景，观察不同隔离级别下的结果。对企业业务而言，测试要关注是否会重复审批、重复付款、库存负数或报表不一致。可以使用并发测试工具和 SQL 日志辅助定位。

### 17. 如何定位一个 SQL 性能问题？

参考答案：先确认慢查询范围、输入参数、数据量和执行频率，再查看执行计划、索引使用、统计信息、锁等待和资源消耗。测试开发可以构造代表性数据集，建立性能基线，并在 CI 或预发中捕获慢 SQL。修复后要验证功能正确性和性能回归。

### 18. 如何测试数据库迁移或 schema 变更？

参考答案：覆盖正向迁移、回滚、兼容读写、默认值、索引创建、数据转换和大表变更耗时。服务发布时要避免新旧代码同时运行导致字段不兼容。测试还要验证备份恢复、灰度租户和迁移失败后的补偿方案。

### 19. 如何测试企业报表准确性？

参考答案：先定义报表口径、数据源、过滤条件、权限、时间区间和汇总公式。准备小规模可手算数据和大规模真实分布数据，分别验证正确性和性能。还要检查权限过滤、导出、缓存刷新、时区、币种和小数精度。

### 20. 如何设计数据对账测试？

参考答案：明确源数据、目标数据、主键映射、金额字段、状态字段和时间窗口。测试要覆盖成功同步、失败重试、重复事件、延迟到达和补偿修复。对账结果应输出差异明细，并能追踪到 API 请求、事件、任务和审计日志。

### 21. 如何测试备份和恢复？

参考答案：不仅要验证备份任务成功，还要定期做恢复演练，确认 RPO、RTO、数据完整性和权限配置。恢复后要运行业务冒烟、数据校验和报表对账。对云服务还要验证跨区域备份、加密密钥、保留策略和删除保护。

### 四、AI、GenAI 与负责任 AI 测试

### 22. OCI Generative AI 服务的测试重点是什么？

参考答案：重点包括 API 鉴权、模型选择、prompt 输入、输出格式、延迟、限流、错误处理、内容安全、数据隐私和计费。企业场景还要验证模型输出是否符合业务规则，是否泄露敏感数据，是否可追溯。自动化可结合规则断言、golden set、人工抽检和 LLM-as-judge。

### 23. 如何测试 RAG 应用？

参考答案：测试检索正确性、权限过滤、引用出处、答案忠实度、幻觉率、召回率、延迟和缓存。构造用户有权访问和无权访问的文档，验证输出只基于授权上下文。还要检查文档更新后索引刷新、向量检索降级和无答案时的安全回复。

### 24. 如何测试企业 AI Agent？

参考答案：先区分建议型、半自动和自动执行型 Agent。测试覆盖身份权限、动作确认、审批、幂等、撤销、审计、失败补偿和人类接管。Agent 不能绕过 ERP/HCM/SCM 原有业务规则，所有动作都应通过同样的 API 权限和策略验证。

### 25. 如何验证 AI 输出的安全性？

参考答案：从敏感信息泄露、越权引用、幻觉、注入攻击、恶意内容、错误建议和合规风险入手。测试用例包括 prompt injection、越权查询、跨租户相似文档、敏感字段、危险操作建议和多语言绕过。高风险输出应有拒答、脱敏、人工复核或降级机制。

### 26. Oracle Fusion AI 治理相关测试要关注什么？

参考答案：关注数据来源、权限、安全控制、模型用途、人工复核、审计、可解释性和合规证明。测试要验证 AI 功能是否继承应用安全框架，是否记录关键决策，是否能让用户理解推荐原因。对于财务、人力和供应链场景，要特别关注偏差、误导和自动决策风险。

### 27. 如何测试模型漂移？

参考答案：建立训练数据、线上输入、模型输出和业务指标的基线，持续监控分布变化、准确率、召回率、拒答率和人工反馈。测试环境可以回放历史样本、边界样本和对抗样本。发现漂移后应触发告警、降级、再训练评估和发布审批。

### 28. 如何用 AI 提升测试开发效率？

参考答案：AI 可以用于需求摘要、用例生成、代码补全、日志聚类、失败归因、测试数据生成和缺陷报告整理。测试开发要建立提示词模板、知识库和审核流程，避免 AI 生成错误断言或泄露敏感数据。真正的收益来自把 AI 输出接入自动化和质量闭环，而不是只生成文本。

### 29. 如何评估 LLM 自动生成测试用例的质量？

参考答案：看覆盖的业务风险、异常路径、断言清晰度、可执行性、重复率和缺陷发现率。可以用历史缺陷回放评估 AI 是否能生成对应用例，也可以比较人工修改成本。最终应由测试开发审核并转化为稳定自动化，而不是直接依赖 AI 结果。

### 五、自动化、CI/CD 与测试平台

### 30. 如何设计 Oracle 项目的自动化测试金字塔？

参考答案：底层是单元测试、规则测试和数据库逻辑测试；中层是 API、契约、集成、权限和后台任务测试；上层是少量关键 UI 和端到端业务流程。云服务还要加入资源生命周期和故障注入测试，AI 服务要加入安全评测和模型质量评测。不同层级在 CI/CD 中承担不同门禁。

### 31. 如何把测试接入 OCI DevOps 或类似 CI/CD？

参考答案：提交阶段运行单元测试、静态扫描、契约测试和快速 API；合并阶段运行核心回归、集成测试和安全检查；发布前运行端到端、性能基线、迁移验证和灰度冒烟。流水线应输出测试报告、日志、trace、覆盖率和失败归因，并对高风险失败自动阻断发布。

### 32. 如何治理 flaky test？

参考答案：先对失败分类：产品缺陷、环境问题、测试数据、脚本等待、外部依赖和并发污染。治理手段包括稳定定位、接口造数、独立清理、显式等待、mock 依赖、隔离并发和失败自动归因。重试只能作为短期缓解，不能替代根因修复。

### 33. 如何设计测试数据管理平台？

参考答案：平台应支持租户/环境隔离、数据模板、敏感数据脱敏、版本化、快速生成、回收清理和数据快照。企业应用需要组织、员工、客户、供应商、订单、发票、审批链和权限角色等复杂对象。云平台需要资源 ID、compartment、策略、配额和标签。

### 34. 如何构建质量看板？

参考答案：看板指标包括自动化通过率、flaky 率、缺陷密度、逃逸缺陷、接口错误率、性能趋势、SLO、发布阻塞、MTTR、资源清理成功率和 AI 评测指标。指标应能按服务、版本、租户、区域和团队下钻。高质量看板不仅展示数量，还要暴露风险和改进行动。

### 35. 如何设计 mock 和服务虚拟化？

参考答案：对第三方依赖、下游慢服务、不可控云资源和收费资源可以使用 mock。mock 要支持成功、失败、超时、限流、乱序、重复事件和 schema 变更。必须避免 mock 与真实行为长期偏离，所以要通过契约测试和定期真实环境回归校准。

### 36. 如何进行测试左移？

参考答案：在需求阶段参与风险分析和验收标准定义，在设计阶段补充可测试性、日志、指标和回滚方案，在开发阶段推动单元测试、契约测试和静态扫描。测试开发还可以提供脚手架、数据生成器和本地 mock，让开发者更早发现问题。左移目标是减少缺陷发现延迟和修复成本。

### 六、性能、稳定性与可观测性

### 37. OCI Monitoring 和 Observability 场景下，测试开发要验证什么？

参考答案：要验证指标是否准确、维度标签是否完整、日志是否可检索、trace 是否贯穿关键链路、告警是否能触发并带有可诊断信息。还要检查租户隔离、日志脱敏、仪表盘和 runbook。可观测性不是上线后才补，而是服务验收标准的一部分。

### 38. 如何设计云服务性能测试？

参考答案：先定义业务负载模型，例如 API QPS、资源创建速率、并发用户、数据量和区域分布。测试指标包括 P50/P95/P99 延迟、错误率、吞吐、CPU、内存、队列长度、数据库等待和成本。要区分控制面和数据面的性能，并建立基线和回归阈值。

### 39. 如何测试限流和配额？

参考答案：构造接近配额、超过配额和突发请求场景，验证错误码、重试头、告警、日志和用户提示。还要测试不同租户、不同区域、不同资源类型的配额隔离。限流不能导致系统雪崩，客户端 SDK 应有合理的退避重试。

### 40. 如何做故障注入测试？

参考答案：选择网络延迟、依赖超时、数据库不可用、消息队列积压、缓存失效、证书过期和区域故障作为注入点。演练前定义假设、影响范围、回滚和监控指标，演练中观察降级、重试、告警和恢复。演练后把遗漏场景沉淀到自动化和 runbook。

### 41. 如何测试跨区域容灾？

参考答案：覆盖主备切换、DNS/路由、数据复制延迟、读写一致性、权限和密钥同步、告警通知和回切。测试要验证 RPO、RTO 和客户可见影响。对于控制面服务，还要确认故障区域不会影响健康区域的资源管理。

### 42. 如何测试发布回滚和灰度？

参考答案：灰度测试要覆盖不同租户、区域、版本和配置组合，持续观察错误率、延迟、资源消耗和业务指标。回滚测试要验证代码、配置、数据库变更、缓存和异步任务能恢复到兼容状态。对不可逆数据变更，要设计前向修复和兼容读写。

### 七、编程、SQL 与行为面模拟题

### 43. 编程题：如何实现一个接口重试器？

参考答案：重试器应支持最大次数、指数退避、抖动、可重试错误判断、超时控制和幂等保护。伪代码上可以循环调用接口，遇到 5xx、429 或网络超时再重试，遇到 4xx 业务错误直接失败。面试中要强调非幂等请求不能盲目重试，日志要记录 request id 和每次失败原因。

### 44. SQL 题：如何找出过去一天 P95 延迟最高的接口？

参考答案：按 api_name 聚合过去一天的请求日志，使用窗口函数或数据库提供的 percentile 函数计算 P95，同时统计调用量和错误率。为了避免低样本误导，可以设置 `HAVING count(*) >= 1000`。结果按 P95 倒序，并结合错误码和版本维度进一步分析。

### 45. 系统设计题：设计一个云资源生命周期测试平台。

参考答案：平台包含资源模板、租户配置、执行编排、API/SDK client、状态轮询、断言引擎、资源清理、成本保护、日志追踪和报告。执行流程是创建资源、验证状态、执行变更、注入异常、验证监控和删除资源。平台还应支持并发、区域选择、配额检查、失败重试和孤儿资源扫描。

### 46. 行为面：线上发布后出现大面积 API 失败，你会怎么处理？

参考答案：先快速止损，确认影响范围、错误码、版本、区域和最近变更，必要时回滚或暂停灰度。然后组织开发、运维、产品和支持同步客户影响，收集日志、指标和 trace 定位根因。恢复后补充回归用例、监控告警、发布门禁和复盘行动项，避免同类问题再次发生。

## AI 相关加分点

- 能把 OCI Generative AI、RAG、AI Agent 和 Fusion AI 治理拆成可验证的质量项。
- 熟悉 prompt injection、越权检索、跨租户泄露、幻觉、内容安全和敏感信息脱敏测试。
- 能用 AI 辅助生成用例、分析日志、归类失败和沉淀测试知识库，同时保留人工审核。
- 能为 LLM 输出设计 golden set、规则断言、人工抽检、LLM-as-judge 和线上反馈闭环。
- 能把模型漂移、数据漂移、AI 质量指标接入监控和发布门禁。
- 能从企业合规角度讨论审计、解释性、人工复核和风险分级。

## 复习建议

- 重点复习 Java/Python 自动化、REST API、SDK 测试、数据库 SQL、事务、索引和慢查询分析。
- 准备云服务测试案例：资源生命周期、权限策略、异步任务、限流配额和跨区域容灾。
- 准备企业应用测试案例：ERP/HCM/SCM 流程、审批、权限、报表、审计和集成对账。
- 熟悉 CI/CD 质量门禁、契约测试、测试数据管理、mock、flaky test 治理和质量看板。
- AI 方向重点准备 RAG 权限、GenAI 输出评测、AI Agent 安全、Responsible AI 和模型监控。
- 性能稳定性重点复习 P95/P99、SLO、告警、日志、trace、故障注入、灰度和回滚。
- 行为面围绕客户影响、快速止损、跨团队协作、复盘闭环和质量前移组织答案。
