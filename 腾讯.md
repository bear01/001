# 腾讯测试开发工程师面试题整理

生成时间：2026-05-31  
岗位方向：测试开发工程师 / SDET / 质量工程 / 自动化测试 / 移动端质量 / 后台服务测试 / AI 测试  
适用场景：腾讯 CSIG、TEG、WXG、IEG、PCG、腾讯云、微信、QQ、游戏、内容与音视频业务方向。

## 一、面试画像

腾讯测试开发面试常见特点是“基础扎实 + 工程能力 + 业务场景”。公开面经里高频出现 Linux、网络、数据库、编程题、接口测试、性能测试、自动化框架、项目深挖、移动端兼容、游戏测试、后台服务稳定性等内容。腾讯云 WeTest、QAPM、SCT 质量团队等公开实践，也体现出腾讯对移动质量、兼容性、性能监控、云测试平台、持续集成和智能化测试的重视。

注意：本文不是官方题库，而是基于公开面经、腾讯质量工具实践、岗位能力模型和测试开发高频考点整理出的复习材料。

复习优先级建议：

1. 基础能力：Linux、网络协议、数据库、Redis、并发、数据结构与算法。
2. 自动化能力：接口自动化、移动端自动化、云测平台、测试数据、CI/CD、报告与归因。
3. 业务场景：微信消息、QQ 音视频、游戏登录/匹配/支付、腾讯云资源、内容推荐。
4. 稳定性：性能压测、客户端卡顿/崩溃、后台 SLO、灰度发布、监控告警。
5. AI 能力：AI 生成用例、日志分析、智能回归、LLM 应用评测、AI Agent 测试。

## 二、计算机基础

### 1. Linux 中如何排查一个服务 CPU 飙高？

**参考答案：**  
先用 `top` 或 `htop` 确认进程，再用 `top -H -p pid` 查看线程级 CPU。Java 服务可以用 `jstack` 查看线程栈，Go 服务可以用 pprof，Python 服务可以结合 py-spy 或日志定位。随后检查是否存在死循环、锁竞争、GC、热点函数、异常重试、流量突增或下游超时导致的线程堆积。测试开发要能把系统指标、应用日志、链路 trace 和变更记录关联起来。

**AI 相关加分点：**  
可以让 AI 对线程栈和日志做摘要，但要让它输出证据，例如线程 ID、堆栈片段、时间窗口和对应请求。

### 2. TCP 和 UDP 有什么区别？腾讯哪些业务会关注 UDP？

**参考答案：**  
TCP 面向连接、可靠、有序、有拥塞控制，适合文件传输、支付、普通 HTTP 接口等可靠性优先场景。UDP 无连接、开销小、延迟低，但不保证可靠和有序，适合实时音视频、游戏同步、直播、弱网场景。腾讯的音视频、游戏、直播、实时通信业务会重点关注 UDP 丢包、抖动、重传、FEC、弱网降级和 QoE 指标。

**AI 相关加分点：**  
AI 生成音视频测试方案时，要明确不仅测接口可用，还要测首帧时间、卡顿率、丢包恢复、音画同步和主观体验评分。

### 3. WebSocket 和 HTTP 轮询有什么区别？

**参考答案：**  
HTTP 轮询由客户端定时请求服务端，简单但实时性差、请求开销高。WebSocket 在一次握手后建立长连接，服务端可以主动推送消息，适合聊天、通知、游戏状态同步和实时协作。测试时要覆盖连接建立、鉴权、心跳、断线重连、消息顺序、重复消息、弱网、服务扩容和网关转发。

**AI 相关加分点：**  
LLM 自动生成接口用例时常忽略长连接生命周期，要补充连接级状态和消息级断言。

### 4. HTTPS 请求的大致过程是什么？

**参考答案：**  
HTTPS 在 HTTP 与 TCP 之间增加 TLS 层。客户端发起握手，服务端返回证书，客户端校验证书合法性，双方协商密钥，之后使用对称密钥加密传输。测试要覆盖证书过期、域名不匹配、弱 TLS 配置、代理抓包、证书 pinning、重定向和混合内容。

**AI 相关加分点：**  
接入 AI 测试工具时，不能为了方便抓包就默认关闭证书校验；涉及 token、cookie、手机号、用户内容的日志必须脱敏。

### 5. MySQL 事务隔离级别有哪些？会导致哪些问题？

**参考答案：**  
常见隔离级别包括读未提交、读已提交、可重复读和串行化。并发读写可能出现脏读、不可重复读、幻读和锁等待。测试开发要能设计并发用例验证库存扣减、账户余额、订单状态和消息消费的事务一致性。MySQL InnoDB 默认可重复读，并通过 MVCC 和锁机制处理许多并发问题。

**AI 相关加分点：**  
AI 可以生成并发测试脚本草案，但事务一致性必须通过最终数据对账和日志校验确认。

### 6. Redis 缓存穿透、击穿、雪崩分别是什么？如何测试？

**参考答案：**  
缓存穿透是大量请求查询不存在的数据，绕过缓存打到数据库；击穿是热点 key 过期后大量请求同时回源；雪崩是大量 key 同时过期或缓存集群故障，导致数据库压力暴增。测试要覆盖非法 ID、高并发热点 key、批量过期、Redis 不可用、限流、熔断、空值缓存和互斥重建。

**AI 相关加分点：**  
AI 可以根据业务接口生成缓存风险清单，但要结合真实 QPS、热点分布和数据库容量做压测验证。

### 7. 线程池核心参数有哪些？测试时关注什么？

**参考答案：**  
线程池核心参数包括核心线程数、最大线程数、队列长度、空闲时间、拒绝策略和线程工厂。测试要关注高并发下的排队、拒绝、超时、线程泄漏、上下游限流、异常重试和资源耗尽。压测时要监控线程数、队列长度、响应时间、错误率、CPU、内存和下游依赖耗时。

**AI 相关加分点：**  
AI 能给出调参建议，但不能只看单点指标；要通过对照实验验证吞吐、延迟和错误率是否真的改善。

### 8. 客户端崩溃问题如何定位？

**参考答案：**  
先收集崩溃堆栈、设备型号、系统版本、App 版本、用户操作路径、网络状态和最近变更。Native 崩溃要关注符号化、空指针、越界、线程安全、OOM；Android 还要看 ANR、主线程阻塞、内存泄漏；iOS 关注 crash log、符号表和系统权限。定位后要复现、修复、灰度验证并观察崩溃率。

**AI 相关加分点：**  
可以用 AI 聚类相似崩溃堆栈，提取高频机型和版本，但修复优先级应结合影响用户数和业务链路。

## 三、编码与算法

### 9. 写一个函数，判断字符串括号是否合法。

**参考答案：**  
使用栈。遇到左括号入栈，遇到右括号时检查栈顶是否匹配，不匹配则非法。遍历结束后栈为空才合法。时间复杂度 O(n)，空间复杂度 O(n)。

```python
def is_valid_brackets(s: str) -> bool:
    pairs = {')': '(', ']': '[', '}': '{'}
    stack = []
    for ch in s:
        if ch in pairs.values():
            stack.append(ch)
        elif ch in pairs:
            if not stack or stack.pop() != pairs[ch]:
                return False
    return not stack
```

**AI 相关加分点：**  
AI 生成代码后，要补充空串、混合普通字符、只有左括号、只有右括号和嵌套括号测试。

### 10. 如何实现 LRU 缓存？

**参考答案：**  
使用哈希表加双向链表。哈希表支持 O(1) 查找，双向链表维护访问顺序。每次访问或写入都把节点移动到链表头部，容量超限时淘汰尾部节点。需要注意更新已有 key、删除尾节点、容量为 0 和并发访问。

**AI 相关加分点：**  
AI 写 LRU 时常漏掉更新已有 key 或删除哈希表映射，测试时要覆盖这些细节。

### 11. Top K 高频元素怎么做？

**参考答案：**  
先用哈希表统计频次，再用小顶堆维护 K 个高频元素，时间复杂度 O(n log k)。如果数据范围有限也可以桶排序。海量数据时可以先分片统计，再合并 Top K，或使用近似算法。

**AI 相关加分点：**  
日志分析和用户反馈聚类可以先用 Top K 快速发现热点问题，再用 LLM 做语义归并。

### 12. 如何判断两个链表是否相交？

**参考答案：**  
可以用双指针。两个指针分别从两个链表头开始走，走到尾部后切换到另一个链表头。若相交，两个指针会在交点相遇；若不相交，最终都为空。时间复杂度 O(m+n)，空间复杂度 O(1)。

**AI 相关加分点：**  
让 AI 生成测试用例时要覆盖长度不同、头节点相交、中间相交、尾节点相交和不相交。

### 13. 如何用代码判断一个接口返回列表是否按时间倒序且 ID 不重复？

**参考答案：**  
遍历列表，记录上一个时间戳，当前时间不能大于上一个时间；同时用 set 记录 ID，出现重复则失败。分页接口还要验证跨页顺序、跨页重复、游标过期、新数据插入和删除导致的翻页漂移。

**AI 相关加分点：**  
AI 根据响应 JSON 生成基础断言很有用，但排序规则、分页一致性和业务过滤条件要由测试开发明确。

### 14. 给定一个数组，如何找出和为 target 的两个数？

**参考答案：**  
用哈希表记录已经遍历过的数字及下标。遍历到当前数字 x 时，判断 target-x 是否已经出现。时间复杂度 O(n)，空间复杂度 O(n)。要考虑重复数字、返回下标还是值、无解时的处理策略。

**AI 相关加分点：**  
AI 生成代码后，要检查它是否正确处理 `[3,3] target=6` 这种重复值场景。

### 15. 如何测试一个排序函数？

**参考答案：**  
不仅要检查结果有序，还要检查元素数量不变、频次不变、没有丢失和新增。如果要求稳定排序，还要检查相等元素的相对顺序。测试数据包括空数组、单元素、已排序、逆序、重复元素、负数、极值、大量随机数和局部有序数组。

**AI 相关加分点：**  
可以使用 property-based testing，让 AI 辅助生成属性断言，但最终要用程序自动验证。

## 四、业务场景测试

### 16. 如何测试微信消息发送功能？

**参考答案：**  
覆盖单聊、群聊、文本、图片、语音、视频、文件、表情、撤回、转发、引用、未读数、多端同步、离线消息、弱网、断线重连和消息顺序。服务端要关注消息 ID、去重、幂等、存储、推送、漫游、敏感内容和反垃圾。异常场景包括重复发送、发送中断、接收方封禁、群成员变更和多设备同时在线。

**AI 相关加分点：**  
AI 可以根据消息类型生成测试矩阵，但多端一致性、消息顺序和弱网重试要用真实设备或仿真环境验证。

### 17. 如何测试语音/视频通话？

**参考答案：**  
功能包括呼叫、接听、拒接、挂断、切后台、免提、摄像头切换、屏幕共享和多人会议。质量指标包括首帧时间、接通率、卡顿率、延迟、丢包、音画同步、回声、噪声和弱网恢复。兼容性要覆盖不同机型、系统版本、蓝牙耳机、权限状态和网络切换。

**AI 相关加分点：**  
AI 可以辅助总结主观体验反馈，但音视频质量必须结合客观指标、录制回放和用户体验评审。

### 18. 如何测试游戏登录与匹配系统？

**参考答案：**  
登录要覆盖账号体系、游客、第三方授权、token 过期、封禁、设备切换和防沉迷。匹配要覆盖段位、地区、延迟、组队、超时、取消、重连、机器人补位和公平性。后台要验证队列堆积、匹配耗时、并发量、异常回滚和监控告警。

**AI 相关加分点：**  
AI 可以生成玩家画像和匹配场景，但公平性和体验指标要用模拟数据、线上指标和人工评估结合。

### 19. 如何测试游戏内支付？

**参考答案：**  
覆盖商品展示、下单、支付渠道、到账、补单、退款、重复点击、网络中断、回调重复、订单超时、未成年人限制、风控和对账。必须验证客户端展示、服务端订单、支付平台回调、虚拟货币余额、道具发放和财务流水一致。异常场景要重点测试幂等和补偿。

**AI 相关加分点：**  
AI 能帮助枚举支付异常路径，但支付类测试必须有精确断言和对账机制。

### 20. 如何测试腾讯云对象存储上传下载？

**参考答案：**  
覆盖小文件、大文件、分片上传、断点续传、秒传、覆盖写、权限、临时密钥、加密、跨地域、校验和、下载限速和删除恢复。性能上关注吞吐、延迟、并发连接、失败重试和弱网恢复。安全上关注越权访问、URL 过期、恶意文件和敏感信息泄露。

**AI 相关加分点：**  
AI 可以从 API 文档生成参数组合，但权限、临时密钥和签名校验必须用真实鉴权链路验证。

### 21. 如何测试 QQ 音乐播放链路？

**参考答案：**  
覆盖搜索、播放、暂停、上一首/下一首、歌词、收藏、下载、会员权限、音质切换、缓存、断点续播、后台播放、耳机控制和多端同步。性能关注首播耗时、卡顿率、缓存命中、耗电和流量。异常场景包括版权限制、网络切换、无网、账号过期和播放资源失效。

**AI 相关加分点：**  
AI 可以分析用户评论提取播放质量问题，例如卡顿、歌词不同步、会员权益异常，再转成回归场景。

### 22. 如何测试内容推荐 Feed 流？

**参考答案：**  
覆盖刷新、加载更多、点赞、评论、分享、关注、屏蔽、举报、广告、无网、弱网和缓存。算法质量关注相关性、重复率、多样性、低质内容、冷启动和 AB 实验。工程链路关注召回、排序、过滤、埋点、降级和推荐接口性能。

**AI 相关加分点：**  
LLM 可以生成 query 和用户画像组合，但推荐质量评估需要指标体系和 badcase 样本集。

### 23. 如何测试小程序发布和灰度？

**参考答案：**  
覆盖版本上传、审核、灰度比例、用户命中、回滚、兼容基础库、权限、接口域名、缓存、离线包和多端一致性。灰度期间要观察错误率、启动耗时、接口失败、用户反馈和关键转化。回滚要验证老版本兼容新数据，避免数据结构升级后无法回退。

**AI 相关加分点：**  
AI 可以汇总灰度反馈和日志异常，但发布/回滚决策要绑定明确阈值。

## 五、自动化测试与测试平台

### 24. 如何设计接口自动化测试框架？

**参考答案：**  
底层封装请求、鉴权、环境、日志、重试；中间层封装业务 API、测试数据和依赖提取；上层编排场景、断言和报告。框架要支持参数化、Mock、并发执行、失败截图/日志、CI 集成、历史趋势和质量看板。关键是分层清晰，接口变更时维护成本低。

**AI 相关加分点：**  
AI 可以根据 OpenAPI 生成基础用例和 client，但业务断言、数据准备和幂等校验需要测试开发把关。

### 25. 移动端自动化如何选型？

**参考答案：**  
常见方案包括 Appium、Airtest、UIAutomator、XCUITest、Espresso 和自研云测平台。选型要考虑端类型、稳定性、执行速度、设备管理、截图视频、元素定位、跨平台能力和 CI 集成。核心链路可以放入流水线，完整兼容性回归更适合云测设备池。

**AI 相关加分点：**  
AI 可以分析截图、DOM 和失败日志辅助定位，但自动修复脚本要经过 review。

### 26. 如何治理 UI 自动化 flaky case？

**参考答案：**  
先统计用例失败率、失败原因、影响流水线次数和修复时长。常见治理包括稳定定位、显式等待、独立测试数据、Mock、失败重试、截图视频、网络日志、设备隔离和低价值用例下线。流程上要有责任人、SLA 和定期复盘。

**AI 相关加分点：**  
可以用 LLM 结合截图、日志和异常堆栈做失败归因，但分类结果要保留证据。

### 27. 如何把自动化测试接入 CI/CD？

**参考答案：**  
主干流水线跑单元测试、静态扫描、接口冒烟、契约测试和少量高价值 UI 用例；夜间任务跑完整回归、兼容性和性能基线。流水线要保存日志、报告、截图、视频、覆盖率和变更信息，并设置质量门禁。失败要自动通知责任人并沉淀趋势。

**AI 相关加分点：**  
AI 可以根据代码 diff 推荐回归集、总结失败原因，但阻断发布的规则要明确可解释。

### 28. 如何设计云测平台？

**参考答案：**  
云测平台应包含设备管理、任务调度、脚本管理、应用安装、执行引擎、截图录屏、日志采集、兼容性报告、性能数据、权限和通知。移动端云测还要处理设备占用、系统版本、厂商 ROM、网络环境、清理恢复和隐私隔离。平台价值是降低设备维护成本，提高兼容性覆盖。

**AI 相关加分点：**  
可加入 AI 失败分析、截图识别、崩溃聚类和报告摘要，但要避免模型读取敏感用户数据。

### 29. 质量看板应该展示哪些指标？

**参考答案：**  
包括需求测试进度、用例通过率、自动化覆盖率、核心链路覆盖率、缺陷密度、线上缺陷、构建失败率、回归耗时、客户端崩溃率、卡顿率、接口错误率、P95/P99 延迟、灰度异常和用户反馈。每个指标要有口径、阈值、负责人和行动策略。

**AI 相关加分点：**  
LLM 可以自动生成质量日报，但原始指标要可追溯，不能只展示模型结论。

### 30. 如何做测试数据管理？

**参考答案：**  
要解决数据构造、隔离、复用、清理、脱敏和一致性。常见方案包括数据工厂、接口造数、数据库快照、Mock、影子库、标签化数据和自动清理。核心目标是让用例可重复、可并发、可追踪，并避免真实敏感数据进入测试环境。

**AI 相关加分点：**  
AI 生成数据时要遵守业务约束和隐私规则，尤其是账号、支付、消息内容和用户画像。

## 六、性能、稳定性与安全

### 31. 如何做后台服务性能测试？

**参考答案：**  
先明确压测目标、场景比例、并发模型、数据量、环境规格和 SLA。执行时监控 QPS、响应时间、错误率、CPU、内存、GC、线程池、连接池、数据库慢查询、缓存命中、队列堆积和下游依赖。压测后要分析瓶颈，优化后复测，并输出容量结论。

**AI 相关加分点：**  
AI 可以汇总监控指标并生成瓶颈假设，但要用实验验证，例如调整索引、线程池、缓存或限流参数。

### 32. 客户端性能测试关注哪些指标？

**参考答案：**  
关注启动耗时、页面加载、FPS、卡顿、内存、CPU、电量、流量、包体积、崩溃率和 ANR。测试要覆盖冷启动、热启动、弱网、低端机、长时间使用、前后台切换和大列表滑动。需要结合性能采集工具、埋点、系统日志和用户反馈。

**AI 相关加分点：**  
AI 可以分析性能报告并提取异常机型、版本和页面，但优化结论要结合真实设备复测。

### 33. 如何测试灰度发布？

**参考答案：**  
覆盖灰度规则、用户命中、版本兼容、配置下发、数据兼容、回滚和监控告警。测试时构造灰度用户和非灰度用户，对比功能、接口、埋点和数据写入。发布后观察错误率、延迟、崩溃率、用户反馈和业务指标，异常时暂停或回滚。

**AI 相关加分点：**  
AI 可总结灰度日志和用户反馈，但是否扩大灰度要依据明确阈值。

### 34. 如何测试限流和降级？

**参考答案：**  
限流测试要验证阈值、维度、返回码、提示、恢复和误伤；降级测试要验证核心链路可用、非核心功能关闭、默认值、缓存兜底和用户体验。还要模拟下游超时、错误率升高、流量突增和配置变更。测试结果应包含业务影响和监控告警。

**AI 相关加分点：**  
AI 可以根据依赖图生成降级场景清单，但故障注入要在可控环境逐步进行。

### 35. 如何测试安全风控登录？

**参考答案：**  
覆盖正常登录、密码错误、验证码、设备变更、异地登录、频繁尝试、账号冻结、二次验证、token 过期和多端互踢。安全上要测试暴力破解、防刷、撞库、短信轰炸、会话固定、CSRF、XSS 和敏感信息泄露。日志和告警要能记录风险事件。

**AI 相关加分点：**  
AI 可以生成攻击样例和异常登录画像，但真实风控策略要防止泄露和误伤正常用户。

### 36. 线上 P0 故障时，测试开发应该做什么？

**参考答案：**  
先协助止血，确认影响范围、版本、时间、用户群和核心指标，推动降级、回滚或配置关闭。然后保留现场证据，包括日志、trace、监控、配置和变更记录。恢复后参与复盘，补充自动化用例、监控告警、发布门禁和故障演练。

**AI 相关加分点：**  
AI 可以快速摘要日志和告警，但故障现场的高风险操作必须人工确认。

## 七、AI + 测试专项

### 37. 如何用 AI 辅助测试用例生成？

**参考答案：**  
可以把需求文档、接口文档、历史缺陷、埋点说明和业务规则输入 AI，让它生成场景清单、边界值、异常路径和自动化脚本草案。风险包括幻觉、遗漏隐性规则、重复低价值用例、泄露敏感数据和断言不准确。因此要有模板化提示词、脱敏、人工评审、规则校验和执行反馈闭环。

**AI 相关加分点：**  
强调 AI 是初稿生成器和覆盖提醒器，测试责任仍在测试开发。

### 38. 如何测试一个 LLM 客服或游戏助手？

**参考答案：**  
覆盖意图识别、知识库检索、多轮上下文、工具调用、拒答、转人工、敏感内容、越权查询、响应速度和成本。正确性要有黄金问答集；安全性要测试提示注入、隐私泄露和越权工具调用；稳定性要测试模型超时、降级和重试；体验上要评估语气、一致性和可解释性。

**AI 相关加分点：**  
LLM-as-judge 可以辅助评测，但需要人工标注样本校准，避免评委模型自身偏差。

### 39. 如何测试 RAG 应用？

**参考答案：**  
RAG 测试要拆成检索质量和生成质量。检索侧关注文档切分、向量召回、重排、权限隔离和知识更新；生成侧关注答案正确性、引用准确性、无答案拒答、幻觉和一致性。指标包括召回率、命中率、答案准确率、引用正确率、延迟和 token 成本。

**AI 相关加分点：**  
不要只看最终回答，要能定位是没检到、检错了，还是检到了但生成错了。

### 40. 如何做智能回归测试选择？

**参考答案：**  
输入包括代码 diff、模块依赖、接口变更、用例覆盖关系、历史缺陷、近期失败、核心链路和风险等级。规则层保证核心链路必跑，模型层做风险排序和额外推荐。输出要包含推荐原因，便于人工判断。上线后持续评估命中率、漏测率、节省时间和误报率。

**AI 相关加分点：**  
核心链路不能完全交给模型裁剪，AI 更适合作为回归范围推荐和优先级排序。

### 41. 如何用 AI 分析崩溃和用户反馈？

**参考答案：**  
可以把崩溃堆栈、版本、机型、系统、用户反馈、日志摘要和操作路径输入模型，做聚类、摘要、严重级别建议、责任模块推荐和复现步骤提取。落地时要先脱敏，统一字段结构，输出结构化结果，并与规则、历史缺陷和监控指标交叉验证。

**AI 相关加分点：**  
可以用向量检索找相似历史问题，再让 LLM 总结差异，比单纯让模型猜更可靠。

### 42. 如何构建测试 Agent？

**参考答案：**  
测试 Agent 可以串联需求解析、用例生成、测试集选择、环境准备、任务触发、执行监控、失败分析、缺陷草稿和报告生成。架构需要工具层、知识库、规划层、权限控制、审计日志和人工确认。落地顺序建议先做只读分析，再逐步扩展到触发测试和创建缺陷。

**AI 相关加分点：**  
必须设置操作边界：不能自动发布、不能删除环境、不能访问未授权数据，高风险动作要 human-in-the-loop。

## 八、项目与行为面试

### 43. 介绍一个你做过的自动化测试项目。

**参考答案：**  
建议用 STAR 结构：背景是什么，痛点是什么；你负责了哪些模块；技术方案如何设计；遇到什么难点；最终收益如何。要讲覆盖率、执行时长、发现缺陷、稳定性、接入流水线和团队使用情况。不要只说“写了很多脚本”，要体现平台化和质量收益。

**AI 相关加分点：**  
可以补充自己如何使用 AI 生成初版用例、分析失败日志或总结报告，并说明人工校验机制。

### 44. 紧急版本上线，测试时间被压缩怎么办？

**参考答案：**  
先确认变更范围、影响链路、风险等级和回滚方案。按风险优先级选择核心用例，保留主链路、支付/权限/数据一致性、兼容性和灰度验证。明确未覆盖风险并同步相关方，上线后加强监控和快速回滚准备。不要简单说“加班全测”，要体现风险管理。

**AI 相关加分点：**  
AI 可以根据代码 diff 和需求描述推荐最小回归集，但最终选择要结合业务影响和历史缺陷。

### 45. 开发不认可你提的 bug，怎么办？

**参考答案：**  
补充证据，包括复现步骤、环境、版本、日志、截图、接口请求、数据库状态和预期依据。再和开发对齐需求或技术设计，确认是缺陷、体验问题、需求不明确还是测试误判。如果争议仍在，拉产品或架构一起决策。关键是用证据推动闭环。

**AI 相关加分点：**  
AI 可以帮忙整理缺陷描述和影响范围，但不能替代真实证据。

### 46. 你如何理解测试开发岗位？

**参考答案：**  
测试开发是用工程化能力提升质量效率的岗位。它既要理解业务风险，也要能开发工具、框架和平台；既要发现问题，也要推动研发流程改进。优秀测开会从需求、开发、测试、发布、监控、复盘全链路建立质量保障体系。

**AI 相关加分点：**  
AI 时代的测开更像质量工程效率放大器：把重复分析、初稿生成和报告总结交给 AI，把风险判断和关键验证掌握在自己手里。

## 九、反问面试官

1. 团队测试开发更偏业务质量保障，还是偏测试平台和工具建设？
2. 当前最影响质量效率的问题是什么：自动化覆盖、环境稳定、设备兼容、失败定位，还是发布风险？
3. 团队是否已经在使用 AI 辅助测试、智能回归、崩溃聚类或测试 Agent？
4. 对新人前三个月的期待是什么？希望先补业务，还是先交付工具能力？
5. 团队如何衡量测试开发的产出和晋升表现？

## 十、复习建议

1. 准备两个项目故事：一个自动化/平台项目，一个业务质量或稳定性项目。
2. 每天刷 2 道算法题，重点是哈希、栈、链表、堆、滑动窗口、LRU 和并发。
3. 熟悉 Linux 排障命令、网络基础、MySQL、Redis 和线程池。
4. 用腾讯典型业务练场景题：微信消息、音视频、游戏匹配、游戏支付、对象存储、内容推荐。
5. 准备移动端质量知识：崩溃、ANR、卡顿、耗电、兼容性、云测平台。
6. 准备 AI + 测试落地方案：AI 用例生成、日志分析、智能回归、LLM/RAG 评测、测试 Agent。

## 十一、参考来源

- 测试人社区：腾讯测试开发三轮面试，涉及 Linux、HTTP、TCP/UDP、进程线程、项目和游戏测试场景：https://ceshiren.com/t/topic/17515
- 腾讯云开发者社区：腾讯测试工程师面试题集合，涉及基础、Linux、数据库、测试方法和项目问题：https://cloud.tencent.com/developer/article/1906232
- 腾讯云开发者社区：腾讯测试开发工程师常见面试题，涉及 Python、接口测试、自动化、数据库和算法：https://cloud.tencent.com/developer/article/2081054
- 腾讯 WeTest 质量开放平台，覆盖兼容测试、性能测试、云真机、游戏测试和移动质量能力：https://wetest.qq.com/
- 腾讯 WeTest 博客，包含移动端质量、自动化测试、兼容性和性能相关实践：https://wetest.qq.com/lab/
- 腾讯 QAPM 应用性能监控，覆盖崩溃、卡顿、网络、启动和客户端性能质量指标：https://qapm.qq.com/
- 腾讯云开发者社区：SCT 测试团队相关内容，涉及云测试、自动化、质量平台和持续集成实践：https://cloud.tencent.com/developer/column/93298

