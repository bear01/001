# Morgan Stanley 测试开发工程师面试题

## 公司名

Morgan Stanley

## 岗位方向

测试开发工程师 / SDET / QA Automation Engineer / 金融科技质量工程方向

## 资料来源与整理依据

- [Morgan Stanley Technology](https://www.morganstanley.com/about-us/technology)
- [Morgan Stanley Developer Portal](https://developer.morganstanley.com/)
- [Morgan Stanley Developer Portal - APIs](https://developer.morganstanley.com/apis)
- [Morgan Stanley Developer Portal - API Changelog](https://developer.morganstanley.com/api-changelog)
- [Morgan Stanley Developer Portal - Terms](https://developer.morganstanley.com/terms)
- 公开候选人面经中常见的 Morgan Stanley SDET / QA Automation / Software Engineer 面试方向：Java、Selenium、TestNG、API 自动化、SQL、微服务、Kafka、CI/CD、行为面。

> 说明：以下题目不是 Morgan Stanley 官方题库，而是结合公开资料、金融科技业务和测试开发岗位能力模型整理的高频准备题与模拟题。

## 面试画像

Morgan Stanley 的测试开发岗位通常要求候选人既具备扎实的工程能力，也能理解金融系统的风险边界。技术面常围绕 Java/Python、Selenium/TestNG、RestAssured、SQL、微服务、消息队列、CI/CD、测试框架设计和线上问题排查展开；业务场景则偏向财富管理、机构客户 API、交易生命周期、市场数据、风控、权限和审计。AI 方向可重点准备 AI 辅助测试、金融 RAG、日志分析、异常检测和模型评测。

## 题目分类

### 一、岗位理解与金融科技质量

### 1. 你如何理解 Morgan Stanley 测试开发工程师的核心价值？

参考答案：核心价值是用工程化手段保障金融平台的正确性、稳定性、安全性和可审计性。Morgan Stanley 的系统服务机构客户、财富管理、交易、研究、数据和内部运营，质量问题可能影响客户决策、交易处理和监管合规。测试开发要参与需求评审、代码评审、自动化框架、CI/CD 门禁、数据校验、生产监控和故障复盘，而不是只在发布前执行用例。

### 2. 金融系统测试和普通互联网系统测试有什么不同？

参考答案：金融系统更关注资金、交易、权限、审计、数据一致性和监管要求。普通互联网系统可能更重视体验和增长，金融系统还要验证金额精度、时区、市场日历、交易状态、数据血缘、权限隔离、日志留痕和灾备恢复。测试策略需要包含契约测试、对账、回放、故障注入、性能基线和生产探针。

### 3. 为什么 Morgan Stanley 这类机构会重视 API Developer Portal 的质量？

参考答案：Developer Portal 让机构客户和第三方平台以程序化方式接入数据和服务，质量直接影响客户集成效率和业务可信度。测试要覆盖 API 文档、鉴权、权限、沙箱、版本兼容、错误码、示例代码、变更日志和服务可用性。平台质量不仅是接口成功率，还包括文档一致性、集成体验和安全边界。

### 4. 如果客户反馈某个 API 返回的数据和页面展示不一致，你会如何排查？

参考答案：先确认用户权限、请求参数、时间范围、数据版本和环境是否一致。然后沿链路检查 API 服务、缓存、数据源、页面聚合逻辑和权限过滤。测试侧应补充 API 与 UI 的交叉校验、数据快照、字段级 diff、trace id 和回归用例。金融场景下还要确认时区、市场日历和数据修订。

### 5. 如何测试一个交易生命周期系统？

参考答案：先建立状态模型，例如创建、校验、提交、执行、确认、清算、失败、取消和修正。测试覆盖正常路径、失败路径、重复提交、乱序消息、超时补偿、人工处理和审计记录。断言不能只看接口成功，还要验证状态机、下游通知、对账记录和权限控制。

### 6. 如何在面试中体现你能快速学习金融业务？

参考答案：可以讲自己如何把陌生业务拆成实体、状态、规则和风险点，例如账户、交易、订单、权限、数据源和报表。再说明如何用示例数据、流程图、边界条件和自动化断言把业务知识固化下来。重点是展示学习方法和落地能力，而不是机械背金融术语。

### 二、Java、Selenium 与自动化框架

### 7. Java 自动化中 HashMap 和 Hashtable 有什么区别？

参考答案：HashMap 非线程安全，允许一个 null key 和多个 null value；Hashtable 是早期同步容器，不允许 null，整体同步开销较大。自动化框架中如果多线程并发写共享上下文，不应直接使用普通 HashMap，可使用 ConcurrentHashMap 或线程隔离的上下文对象。回答时要结合并发执行用例和测试数据隔离。

### 8. 如何设计 Selenium + TestNG 自动化框架？

参考答案：框架可分为 driver 管理、页面对象、业务流程、测试数据、断言、配置、日志、截图、报告和 CI 集成。使用 Page Object 或 Screenplay 降低页面定位和业务逻辑耦合；TestNG 负责分组、并行、数据驱动和生命周期管理。失败时要自动保存截图、DOM、网络日志和环境信息，便于诊断。

### 9. 如何处理动态元素和动态 XPath？

参考答案：优先使用稳定的 data-testid、aria-label、id 或业务属性，少依赖脆弱的绝对 XPath。动态表格可通过列名定位列索引，再按行内容查找目标单元格。对异步加载要使用显式等待和业务条件，而不是固定 sleep。必要时和前端约定可测试属性，提升自动化稳定性。

### 10. 如何处理 iframe、alert 和多窗口？

参考答案：iframe 需要先切换到目标 frame，再查找内部元素，操作完成后切回 default content。alert 需要显式等待并 accept/dismiss。多窗口要保存当前句柄，等待新窗口出现后切换，并在结束后关闭或返回原窗口。测试要保证上下文恢复，否则后续用例会被污染。

### 11. 如何处理 2FA/CAPTCHA 自动化？

参考答案：真实 2FA 和 CAPTCHA 不应在自动化中硬绕过，因为它们本身是安全机制。测试环境可以使用白名单账号、模拟认证服务、固定测试 token 或后端测试接口。自动化应验证认证流程、失败提示和安全日志，而不是用不稳定方式识别验证码。生产安全能力应通过专门安全测试覆盖。

### 12. 如何治理 UI 自动化 flaky test？

参考答案：常见原因包括固定等待、测试数据不稳定、环境慢、定位脆弱、并发污染和外部依赖波动。治理方法包括显式等待业务状态、稳定定位、数据隔离、失败重试但不掩盖问题、失败证据采集和稳定性看板。核心链路 flaky test 不能长期留在门禁里，否则会削弱自动化可信度。

### 13. Page Object Model 的优缺点是什么？

参考答案：优点是封装页面元素和操作，提升复用性和可维护性；缺点是过度封装可能导致页面类臃肿，业务流程散落。更好的实践是页面层只封装基础交互，业务层封装用户流程，测试层只表达验证意图。大型金融后台系统还要结合组件对象和数据驱动，避免重复代码。

### 14. 如何让 UI 自动化适合 CI 执行？

参考答案：需要无头浏览器或稳定浏览器镜像、环境健康检查、测试数据隔离、并行策略、失败截图、录像、重跑策略和报告归档。CI 中不应跑所有低价值 UI 用例，核心冒烟和高风险流程优先。对金融系统，UI 自动化还要避免操作真实资金或敏感生产数据。

### 三、API、微服务与测试平台

### 15. 如何测试 Morgan Stanley Developer Portal 的 API？

参考答案：覆盖鉴权、授权、请求参数、响应 schema、错误码、版本兼容、限流、沙箱数据、文档示例和变更日志。机构客户 API 还要验证权限边界、审计日志、数据新鲜度和字段脱敏。自动化应结合契约测试、沙箱端到端测试和 mock 外部依赖。

### 16. RestAssured API 自动化框架你会如何设计？

参考答案：可分为配置层、鉴权层、请求封装、业务 client、schema 校验、业务断言、测试数据和报告。框架要支持多环境、证书、token、trace id、日志脱敏和并行执行。用例层应只表达业务场景，例如“查询授权账户交易明细”，底层 HTTP 细节由 client 封装。

### 17. 如何做 API 契约测试？

参考答案：把请求、响应、字段类型、错误码、分页、排序、权限规则和版本策略固化成契约。服务端变更前跑 provider contract，客户端或 SDK 变更时跑 consumer contract。金融 API 还要检查金额精度、日期、时区、证券标识、权限过滤和审计字段。

### 18. 如何测试 API 鉴权和权限？

参考答案：构造不同用户、客户、角色、产品订阅、地区和数据授权，覆盖允许访问、拒绝访问、字段脱敏和权限过期。测试不能只看入口按钮，还要直接调用后端验证越权。权限变更后要测试缓存失效，审计日志要记录访问主体、资源和结果。

### 19. 如何测试 API 限流？

参考答案：明确限流维度：用户、客户、应用、IP、接口或时间窗口。覆盖阈值内、达到阈值、超过阈值、窗口重置、并发突刺和分布式节点一致性。断言包括错误码、重试建议、日志和监控，同时确认不会误伤其他客户。

### 20. 如何测试 Kafka 或消息队列链路？

参考答案：覆盖正常消费、重复消息、乱序消息、消息丢失、消费者重启、死信、积压和重放。系统应通过幂等、offset 管理、状态机和补偿任务保证最终一致。测试断言要关注最终业务状态、日志、告警和对账，而不只是消息被消费。

### 21. 如何测试微服务超时和重试？

参考答案：使用 mock 或故障注入构造下游慢响应、连接失败、5xx、部分成功和网络抖动。验证客户端超时、重试次数、退避策略、熔断、降级和日志。金融系统要特别注意重试不能造成重复交易或重复通知，必须配合幂等设计。

### 22. 如何设计测试数据管理平台？

参考答案：平台应支持合成数据、脱敏数据、账户/交易/权限数据工厂、数据版本、运行前创建和运行后清理。金融数据要避免真实 PII 和敏感交易泄漏。对回归和故障复现，需要保留数据快照、输入参数和预期输出，确保结果可复现。

### 23. 如何设计质量看板？

参考答案：看板应展示自动化通过率、关键链路健康、flaky test、缺陷趋势、代码覆盖、发布门禁、API 错误率、性能基线和线上事故复盘项。金融场景还可加入数据质量、权限缺陷、审计缺陷和对账差异。看板目标是指导行动，而不是堆指标。

### 四、数据质量、性能与稳定性

### 24. 如何测试机构客户 API 的数据质量？

参考答案：从准确性、完整性、及时性、一致性和可追溯性验证。测试要覆盖字段类型、时间戳、数据源、权限过滤、分页、排序、缺失值和历史修订。自动化可做字段级 diff、数据源对账、异常值检测和数据新鲜度监控。

### 25. 如何测试财富管理账户数据展示？

参考答案：覆盖账户列表、资产持仓、收益、现金余额、交易明细、权限、币种、报表导出和移动/网页一致性。测试重点是前后端数据一致、金额精度、日期时区、权限隔离和敏感信息脱敏。还要验证缓存刷新和历史数据修订。

### 26. 如何排查接口 P99 延迟升高？

参考答案：先确认影响范围：某接口、某区域、某客户还是全局。然后看网关、服务、数据库、缓存、消息队列、下游服务和最近发布。用 trace 样本定位慢环节，用压测或回放复现。修复后补充性能回归和监控阈值。

### 27. 如何设计性能测试场景？

参考答案：基于真实流量模型设计，包括用户数、请求比例、数据量、峰值倍数、区域和依赖服务延迟。指标包括吞吐、P95/P99、错误率、CPU、内存、线程池、连接池和队列积压。金融系统还要验证压测后的数据一致性和审计完整性。

### 28. 如何测试缓存一致性？

参考答案：覆盖缓存命中、失效、预热、回源失败、权限隔离、分布式节点一致性和数据更新事件。对金融数据，不能只追求命中率，还要控制数据新鲜度和权限变更后的缓存失效。测试可比较源时间戳、响应时间和缓存版本。

### 29. 如何测试灾备和故障恢复？

参考答案：构造单节点故障、可用区故障、数据库切换、消息队列故障、配置中心异常和网络分区。验证 RTO、RPO、流量切换、数据一致性、重复处理和用户可感知影响。演练后要复盘并补充自动化探针。

### 30. 如何测试生产监控和告警？

参考答案：对 API 成功率、延迟、错误码、队列积压、数据新鲜度、权限失败、缓存异常和下游故障设置告警。通过故障注入确认告警触发、路由正确、上下文充足。告警要避免长期误报，否则团队会失去响应敏感度。

### 31. 如何做日志脱敏测试？

参考答案：定义敏感字段清单，如账户号、证件号、邮箱、手机号、token、密钥和交易 id 的敏感部分。扫描 API 响应、日志、错误消息、监控标签、测试报告和导出文件。异常堆栈和 debug 日志也要覆盖，避免非正常路径泄漏。

### 五、AI、数据分析与智能测试

### 32. Morgan Stanley 使用 AI/ML 的场景中，测试重点是什么？

参考答案：重点是数据质量、模型准确性、可解释性、稳定性、漂移、权限、隐私和审计。公开资料提到 Morgan Stanley 技术团队使用 AI 分析美联储沟通来预测政策方向，类似金融 NLP 系统必须验证输入语料、时间范围、引用、结果稳定性和模型输出边界。

### 33. 如何测试金融 RAG 问答系统？

参考答案：检索层验证召回率、权限过滤、时间有效性、文档去重和排序；生成层验证答案忠实、引用正确、不编造、不泄露。测试集要覆盖有答案、无答案、权限不足、过期文档、相似实体和多跳推理。还要做提示注入和越权检索测试。

### 34. 如何评估 LLM 生成的投研摘要？

参考答案：维度包括事实准确、数字正确、时间线正确、实体识别、重要信息覆盖、风险提示和来源引用。测试集应包含新闻、财报、研究报告、宏观事件和相似公司对比。高风险摘要必须支持引用追踪和人工复核，避免幻觉影响决策。

### 35. 如何用 AI 辅助测试用例生成？

参考答案：可以让 LLM 基于需求、API 文档、代码 diff、历史缺陷和线上事故生成候选测试点。测试开发要审查、去重、风险分级，并转化为自动化。AI 适合补充边界、负向和组合场景，但不能未经审查直接进入门禁。

### 36. 如何用 LLM 做日志分析？

参考答案：先通过规则、检索和聚合缩小日志上下文，再让 LLM 总结异常模式、关联最近变更并给出排查路径。输出必须包含证据，例如 trace id、错误码、时间窗口和指标变化。高风险操作不能让模型自动执行，只能辅助工程师判断。

### 37. 如何测试异常检测系统？

参考答案：构造 API 错误率突增、延迟升高、数据缺失、交易量异常和特征漂移。评估误报率、漏报率、检测延迟和解释能力。金融系统还要考虑市场开盘、节假日、批处理窗口等正常波动，避免把季节性误判为故障。

### 38. AI 生成自动化脚本有哪些风险？

参考答案：风险包括断言无效、等待不稳定、依赖不安全、误用真实数据、泄露密钥、破坏测试环境和代码难维护。应在沙箱运行，做代码审查、安全扫描、权限限制和小范围试点。核心金融系统不能直接信任 AI 生成脚本。

### 39. 如何建立 AI 测试质量指标？

参考答案：可以包括测试点采纳率、自动化落地率、缺陷发现率、误报率、用例稳定性、生成脚本 review 通过率和节省时间。对 LLM 应用还要看事实准确率、幻觉率、拒答正确率、权限违规率和引用正确率。指标要能指导改进，而不是只展示 AI 使用量。

### 六、编码、SQL、Linux 与行为面

### 40. 编程题：统计字符串中重复单词。

参考答案：将字符串按非字母数字分隔，统一大小写，使用哈希表统计词频，最后输出频次大于 1 的单词。要考虑标点、大小写、空字符串和 Unicode。复杂度 O(n)。在 SDET 面试中可以补充如何为该函数设计单元测试。

### 41. 编程题：实现一个最近 5 分钟请求计数器。

参考答案：低流量可用队列保存时间戳，每次查询清理 5 分钟外数据；高流量可用环形数组按秒聚合，每个槽保存时间戳和计数。需要考虑并发、时间回拨和多实例聚合。分布式场景可用 Redis sorted set 或流式聚合。

### 42. 编程题：给定交易事件，找出重复请求。

参考答案：优先使用幂等键或业务唯一键，例如 client_id、request_id、account_id、amount、currency 和时间窗口。使用哈希表记录已处理请求，重复出现时标记。真实系统不能只按金额判断，要结合状态、请求来源和交易 id。测试要覆盖同金额不同交易和同请求重试。

### 43. SQL 题：查询过去一小时错误率最高的 API。

参考答案：

```sql
SELECT api_name,
       COUNT(*) AS total_count,
       SUM(CASE WHEN status_code >= 500 THEN 1 ELSE 0 END) AS error_count,
       SUM(CASE WHEN status_code >= 500 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS error_rate
FROM api_logs
WHERE created_at >= CURRENT_TIMESTAMP - INTERVAL '1 hour'
GROUP BY api_name
HAVING COUNT(*) >= 100
ORDER BY error_rate DESC
LIMIT 10;
```

要补充样本量阈值，避免低流量 API 误判。还可以按客户、区域和版本继续分析。

### 44. Linux 题：线上服务 CPU 飙高，你如何排查？

参考答案：先用 `top`、`pidstat` 找到进程和线程，再用 `jstack`、`perf`、`async-profiler` 或 APM 定位热点。检查最近发布、异常重试、日志打印、正则、序列化、加密和线程池。修复后补充压测场景和 CPU 监控阈值。

### 45. 系统设计题：设计一个机构客户 API 质量监控系统。

参考答案：系统包括 API 探针、日志采集、指标聚合、数据质量校验、权限校验、告警、样本留存和看板。指标包括成功率、延迟、错误码、数据新鲜度、字段缺失、权限失败和客户影响范围。要支持按客户、接口、区域和版本分析，并能关联最近发布。

### 46. 行为题：你如何推动开发团队接受更严格的自动化门禁？

参考答案：先用数据说明问题，如回归耗时、线上缺陷、重复事故和发布回滚。再选择高价值核心链路试点，保证门禁执行快、失败可诊断、维护成本可控。推动时和开发一起优化框架和环境，让门禁变成交付保障。最后用缺陷拦截率和回归时间下降证明价值。

## AI 相关加分点

- 能用 LLM 基于需求、API 文档、代码 diff 和历史缺陷生成测试点，并进行人工审查和自动化落地。
- 熟悉金融 RAG/LLM 测试，能验证权限过滤、引用忠实性、提示注入防护和无答案拒答。
- 能用日志、trace、指标和变更记录构建智能排障流程，缩短 API 和数据平台故障定位时间。
- 能用异常检测发现 API 错误率突增、数据缺失、延迟升高、缓存异常和模型漂移。
- 能设计投研摘要、宏观 NLP、风控模型和客户服务助手的评测集。
- 理解金融 AI 的合规边界：隐私、客户权限、审计、人工复核和模型输出责任。
- 能将 AI 生成脚本纳入沙箱、代码审查、安全扫描和 CI/CD 门禁。

## 复习建议

- 编码重点练习 Java 集合、字符串、哈希表、滑动窗口、队列、并发和复杂度分析。
- 熟悉 Selenium、TestNG、Maven、RestAssured、SQL、Jenkins、Git、Docker 和报告框架。
- 金融业务重点准备交易生命周期、账户数据、权限、审计、市场数据、对账和数据新鲜度。
- 系统设计重点准备 API 质量监控、测试数据管理、限流、缓存、消息队列、灾备和可观测性。
- AI 方向重点准备 RAG、LLM 评测、日志分析、异常检测、AI 辅助测试生成和安全测试。
- 行为面用 STAR 法准备质量推动、线上事故、跨团队协作、复杂业务学习和自动化平台落地案例。
