# OpenAI 测试开发工程师面试题整理

- 公司：OpenAI
- 岗位方向：测试开发工程师 / Quality Engineering / AI Product Quality / SDET
- 生成日期：2026-06-06
- 适用场景：AI 产品质量、LLM 应用测试、模型评测、API 平台、Agent 安全、CI/CD、性能与稳定性

## 资料来源与整理依据

- OpenAI 官方 Interview Guide、Careers 页面，以及公开软件工程面试经验中对编码、系统设计、研究协作和行为面试的描述。
- OpenAI 官方 API 文档：Evaluation best practices、Production best practices、Latency optimization、Safety best practices、Structured Outputs、Safety checks、Agent safety。
- OpenAI Evals 开源项目与 Preparedness/Safety 相关公开资料。
- 通用 SDET/测试开发面试题型：自动化测试、测试平台、API 测试、CI/CD、稳定性、数据分析、SQL/Linux、故障排查。

> 说明：OpenAI 不一定设置传统“测试开发工程师”统一面试题库。以下问题按 AI 公司质量工程岗位能力模型整理，用于准备测试开发、AI 产品质量、平台质量和工程效率方向面试。

## 面试画像

OpenAI 相关岗位通常强调强工程能力、系统思维、AI 原生问题理解、快速学习、使命感和高标准质量意识。测试开发候选人除了要掌握传统测试体系，还要理解 LLM 输出不确定性、评测集设计、幻觉与安全风险、提示词注入、结构化输出校验、API 可靠性、成本/延迟/质量权衡，以及如何把 AI 能力接入研发质量闭环。

回答建议：不要只说“用大模型生成测试用例”，而要讲清楚评测目标、数据集、指标、自动化执行、误报治理、人工复核、线上监控和回归门禁。

## 一、岗位理解与项目经验

### 1. 你如何理解 OpenAI 场景下的测试开发工程师价值？

**参考答案：**  
价值在于把 AI 产品和模型能力的不确定性转化为可度量、可回归、可治理的质量体系。传统测试关注确定性输入输出，而 LLM 系统还要关注回答质量、安全边界、鲁棒性、延迟、成本、漂移和人工反馈。测试开发需要建设 eval、自动化回归、线上监控、红队样本、API 稳定性测试和质量数据平台。

**AI 相关加分点：**  
强调“评测即测试”的能力，把模型评估、产品测试、日志分析和发布门禁连接起来。

### 2. OpenAI 面试中你会如何介绍自己的测试平台项目？

**参考答案：**  
按背景、目标、方案、指标和复盘回答。例如：为了降低 AI 应用回归成本，建设了覆盖 prompt、RAG、工具调用、结构化输出和安全策略的自动化评测平台。平台支持数据集管理、批量运行、指标计算、失败归因、人工抽样和 CI 门禁。结果可以量化为回归时间下降、严重缺陷提前发现、误报率下降和发布阻塞更精准。

### 3. 测试 LLM 应用和测试普通 Web 应用最大的区别是什么？

**参考答案：**  
普通 Web 应用大多是确定性逻辑，断言可以直接比较状态码、字段和页面元素。LLM 应用有概率性和语义性，同一输入可能产生不同输出，所以需要使用 rubric、参考答案、语义相似度、规则校验、人工评审和统计指标。还要测试 prompt injection、越狱、幻觉、上下文污染和工具误用。

### 4. 如何证明你不是“只会点点点”的测试？

**参考答案：**  
可以展示代码能力、测试框架设计、CI/CD 接入、服务观测、数据分析和质量决策能力。比如能写接口自动化、构造 Mock、做并发压测、分析日志、定位根因、建设质量看板，并能参与代码评审。测试开发的核心是用工程手段提升质量效率，而不是只执行人工用例。

### 5. OpenAI 这类快速迭代团队中如何保持质量？

**参考答案：**  
靠分层门禁和风险分级，而不是让所有变更走同一套重流程。低风险变更跑快速单测和小评测，高风险模型/安全/支付/API 变更跑完整回归、红队样本、灰度和人工审核。质量策略要嵌入研发流程：PR 检查、自动 eval、发布前检查、线上监控和回滚机制。

## 二、LLM 评测与 AI 产品测试

### 6. 什么是 eval？它和传统自动化测试有什么关系？

**参考答案：**  
Eval 是用于衡量模型或 AI 系统表现的结构化测试。它和传统自动化测试一样追求可重复、可对比、可回归，但评估对象从确定性功能扩展到语义质量、安全、鲁棒性和偏好对齐。传统测试回答“功能有没有坏”，eval 还要回答“模型表现是否变好、变差或偏离目标”。

### 7. 如何设计一个客服机器人回答质量评测集？

**参考答案：**  
先定义任务范围和成功标准，例如准确、完整、礼貌、不编造、不泄露隐私、能拒答高风险请求。再收集真实问题、历史缺陷、边界样本和 adversarial 样本，标注参考答案和评分规则。指标可以包括正确率、拒答准确率、幻觉率、用户满意度、平均响应时延和人工复核通过率。

### 8. 如何处理 LLM 输出不稳定导致断言困难？

**参考答案：**  
可以组合多种断言：结构化字段用 schema 校验，事实性内容用检索证据和规则校验，开放文本用 rubric 或 LLM judge，并对关键样本进行人工抽样。不要只依赖字符串精确匹配。对高风险场景要设置确定性约束，例如固定输出格式、限制工具范围和增加后处理校验。

### 9. LLM judge 评测有什么风险？

**参考答案：**  
风险包括评审模型偏见、评分漂移、被提示词操纵、对事实错误不敏感、不同模型间不可比。治理方式是保留人工黄金集、做 judge 校准、定期抽样复核、使用多维评分、记录评分理由，并监控 judge 与人工一致率。重要发布不能只靠 LLM judge 通过。

### 10. 如何测试 RAG 应用的质量？

**参考答案：**  
分三层测试：检索层看召回率、精准率、排序和文档新鲜度；生成层看是否基于证据回答、是否引用来源、是否拒答无依据问题；系统层看延迟、权限过滤、多轮上下文和缓存。常见缺陷包括检索不到、检索错、模型编造、引用错误、权限越权和长上下文遗忘。

### 11. 如何构建 AI 应用的回归测试集？

**参考答案：**  
回归集应包含高频真实请求、历史线上缺陷、关键业务路径、安全样本、边界输入和代表性长尾。每个样本要有预期行为、评分规则、标签和 owner。回归集需要版本管理，避免只增加不清理；同时按风险分为 PR 快速集、发布门禁集和夜间全量集。

### 12. 如何衡量 prompt 变更是否可以发布？

**参考答案：**  
先跑离线 eval，比对核心指标、失败样本和分层表现；再做灰度，监控线上用户反馈、拒答率、转人工率、投诉率、成本和延迟。若 prompt 变更提升平均指标但在关键安全或付费场景退化，也不能发布。要关注总体指标和关键切片。

### 13. 如何测试结构化输出？

**参考答案：**  
使用 JSON Schema 或类型约束验证字段、类型、枚举、必填项和嵌套结构。再验证语义正确性，例如金额是否合理、分类是否符合上下文、数组长度是否满足限制。异常场景包括非法输入、超长上下文、模型拒答、字段缺失、额外字段和解析失败重试。

### 14. 如何测试模型幻觉？

**参考答案：**  
构造无答案问题、过期事实、相似实体、反事实提示和诱导性问题。评估模型是否承认不知道、是否引用可靠来源、是否编造细节。RAG 场景要校验回答是否被证据支持。指标可以是 hallucination rate、unsupported claim rate 和 citation accuracy。

### 15. 如何发现模型能力退化？

**参考答案：**  
建立固定评测集、线上抽样集和业务指标监控。每次模型、prompt、检索、工具或安全策略变更后跑对比实验，关注总体分数、关键切片、失败聚类和置信区间。线上要监控用户反馈、重试率、人工接管率、延迟和异常日志。退化要能定位到模型、数据、prompt、工具或基础设施。

## 三、API 平台与服务端测试

### 16. OpenAI API 类平台的接口测试重点是什么？

**参考答案：**  
重点覆盖认证、权限、组织/项目隔离、参数校验、流式响应、错误码、限流、幂等、超时、重试、计费、日志脱敏和兼容性。AI API 还要测试 token 限制、模型选择、工具调用、结构化输出、内容安全和批量请求。接口测试要能在 CI 中稳定运行。

### 17. 如何测试流式响应？

**参考答案：**  
验证首 token 延迟、chunk 顺序、断流、客户端取消、网络抖动、服务端错误、部分结果处理和最终聚合结果。还要测试前端展示是否逐步更新、后端是否正确记录用量和日志。自动化可以模拟慢网络和中途断开，校验资源释放。

### 18. 如何测试 API rate limit？

**参考答案：**  
覆盖不同组织、项目、key、模型和接口维度。构造接近限制、超过限制、恢复窗口、并发突刺和长期稳态负载。校验状态码、错误信息、响应头、客户端退避和重试策略。还要验证限流不会影响其他租户，避免多租户隔离问题。

### 19. 如何测试 API key 安全？

**参考答案：**  
覆盖 key 创建、轮换、撤销、权限范围、日志脱敏、环境隔离和泄露检测。测试错误 key、过期 key、跨项目访问、低权限调用高权限接口。工程实践上不应把 key 写入代码仓库，CI 中使用 secret 管理，并监控异常调用。

### 20. 如何测试 Batch 或异步任务类接口？

**参考答案：**  
验证任务创建、排队、执行、取消、失败、重试、结果下载、部分失败和过期清理。大批量任务要测试文件格式、重复任务、幂等、状态流转和成本统计。监控指标包括排队时长、成功率、失败原因分布和吞吐。

### 21. 如何测试工具调用或 Agent 工作流？

**参考答案：**  
覆盖工具选择是否正确、参数是否符合 schema、工具权限、错误重试、循环调用、超时、用户取消和结果注入。高风险工具必须有人审或规则校验。测试样本要包含正常任务、恶意输入、权限越界、工具失败和多工具编排。

### 22. 如何测试多租户隔离？

**参考答案：**  
构造多个组织、项目、用户、API key 和权限角色，验证资源、日志、计费、文件、评测集和模型配置互不泄漏。重点测试缓存、异步任务、批处理、错误信息和管理后台。多租户系统中，任何跨租户数据泄露都是高严重度缺陷。

## 四、安全、红队与合规质量

### 23. 如何做 prompt injection 测试？

**参考答案：**  
构造直接注入、间接注入、文档注入、工具结果注入、多轮上下文注入和编码混淆。验证系统是否忽略恶意指令、是否泄露系统提示、是否调用危险工具。RAG 和 Agent 场景尤其要测试外部内容不能覆盖系统策略。

### 24. OpenAI 官方安全最佳实践中为什么强调红队测试？

**参考答案：**  
因为 AI 应用可能被用户主动攻击，普通代表性样本不足以暴露越狱、滥用和边界问题。红队测试通过 adversarial 输入模拟恶意用户，能发现模型是否偏离主题、是否被诱导泄露信息、是否绕过安全策略。测试开发要把红队样本沉淀为可回归数据集。

### 25. 如何测试内容安全系统？

**参考答案：**  
覆盖正常内容、边界内容、明显违规内容、隐晦表达、多语言、拼写变体、图片/文本组合和上下文反转。指标包括召回率、误杀率、分级准确率和处理时延。高风险场景要有人审、申诉和日志追踪，不能只看模型单次分类结果。

### 26. 如何测试 AI 产品的人工审核流程？

**参考答案：**  
验证命中规则后是否进入审核队列、审核员是否看到必要上下文、决策是否可追溯、超时如何处理、申诉是否闭环。还要测试隐私脱敏、权限控制、抽样策略和审核结果反哺评测集。人工审核是质量和安全闭环的一部分。

### 27. 如何测试“拒答”能力？

**参考答案：**  
准备应该拒答、应该正常回答、应该安全改写和需要澄清的样本。评估拒答准确率、过度拒答率、拒答理由是否清晰、是否提供安全替代方案。不能只追求拒答多，因为过度拒答会伤害用户体验。

### 28. 如果线上出现安全绕过，你如何处理？

**参考答案：**  
先止血：临时规则、限流、关闭高风险工具或灰度回滚。然后收集样本、复现链路、定位是 prompt、模型、工具权限、检索内容还是后处理问题。修复后加入红队回归集，补充监控和告警，并复盘缺陷为什么逃过发布门禁。

## 五、自动化测试、CI/CD 与测试平台

### 29. AI 应用的测试金字塔怎么设计？

**参考答案：**  
底层是单元测试、schema 校验、工具函数和 prompt 模板测试；中间是 API、RAG、工具调用、Agent 流程和 eval；顶层是关键用户路径 E2E 和人工验收。AI 场景不应把所有质量押在 E2E 上，eval 和契约测试要成为中间层核心。

### 30. 如何把 eval 接入 CI/CD？

**参考答案：**  
按风险分层接入：PR 阶段跑小规模快速评测和安全冒烟；主干跑关键回归；发布前跑完整评测和灰度检查。CI 输出要包含分数、失败样本、指标变化、置信区间和是否阻塞。失败时要能定位样本、prompt、模型版本和代码变更。

### 31. 如何治理 AI 测试中的 flaky？

**参考答案：**  
先区分模型随机性、外部依赖、测试数据变化、评测器不稳定和真实缺陷。治理方法包括固定参数、固定数据集、结构化输出、规则断言、重复采样统计、Mock 外部服务和人工黄金集校准。对非确定性结果要用统计阈值，而不是简单重跑到通过。

### 32. 如何设计模型评测平台的数据模型？

**参考答案：**  
核心实体包括数据集、样本、标签、prompt 版本、模型配置、运行批次、评分器、指标、失败原因和人工复核记录。每次运行要可追溯：谁触发、用的什么代码、什么模型、什么数据、什么评分规则。这样才能支持回归对比和审计。

### 33. 如何做变更影响分析？

**参考答案：**  
根据代码路径、prompt 文件、工具 schema、检索索引、模型配置和历史缺陷映射到受影响测试集。比如只改支付文案不必跑全量 Agent 红队，但改系统提示、工具权限或检索逻辑必须跑安全与核心业务回归。目标是在速度和风险之间取得平衡。

### 34. 你会如何设计 AI 测试报告？

**参考答案：**  
报告应包含总体结论、核心指标变化、失败样本列表、严重风险、分层切片、成本、延迟、与上一版本对比、是否建议发布。对每个失败样本要展示输入、输出、参考、评分理由和 owner。报告要适合研发、产品和安全团队共同使用。

### 35. 如何测试前端 Chat 产品？

**参考答案：**  
覆盖登录、会话管理、消息发送、流式展示、停止生成、重试、附件上传、错误提示、移动端适配、历史记录、无障碍和多语言。AI 特有场景包括长对话上下文、工具状态展示、引用来源、内容安全提示和模型切换。自动化上可用 Mock 模型响应减少不稳定。

## 六、性能、稳定性与可观测性

### 36. LLM 应用性能测试关注哪些指标？

**参考答案：**  
关注首 token 延迟、总响应时长、tokens per second、超时率、错误率、重试率、队列等待、成本、缓存命中率和用户感知等待。流式体验下，首 token 往往比总时长更影响体验。还要按模型、地区、请求长度和工具调用切片分析。

### 37. 如何优化 LLM 应用延迟？

**参考答案：**  
可以减少输出 token、压缩输入上下文、并行化独立步骤、使用缓存、提前返回流式结果、选择合适模型、减少不必要的模型调用，以及不要默认所有逻辑都交给 LLM。优化前要先分解端到端耗时，明确瓶颈在检索、模型、工具还是前端展示。

### 38. 如何测试高并发 AI API 服务？

**参考答案：**  
构造不同输入长度、模型、工具调用和流式/非流式请求，模拟突刺和稳态负载。观察吞吐、延迟、错误、限流、公平性和资源瓶颈。还要验证降级策略，例如排队提示、较小模型 fallback、缓存回答和非核心功能关闭。

### 39. 如何做 AI 产品线上质量监控？

**参考答案：**  
监控包括技术指标和质量指标。技术指标有错误率、延迟、超时、成本和队列；质量指标有用户反馈、拒答率、幻觉投诉、安全命中、人工接管、任务完成率和评测抽样分数。线上样本要经过脱敏和权限控制，再进入评测或复盘。

### 40. 如果用户反馈“回答质量突然变差”，你如何排查？

**参考答案：**  
先确认影响范围和时间点，再排查模型配置、prompt 版本、检索索引、工具服务、安全策略、缓存和数据源。对比变更前后的 eval、线上样本和用户切片。若无法快速定位，先回滚或灰度降级，再做失败聚类和根因分析。

### 41. 如何测试成本异常？

**参考答案：**  
构造超长输入、长输出、多轮对话、工具循环、重试风暴和批量任务。监控 token 用量、请求次数、缓存命中和用户/项目维度成本。要设置预算告警、配额、最大 token、循环保护和异常自动熔断。

## 七、编程、SQL 与 Linux

### 42. 编程题：如何统计 eval 运行中失败最多的标签？

**参考答案：**  
遍历样本结果，用哈希表按标签累计 total 和 failed，再按 failed 数或失败率排序。要先确认一个样本是否允许多个标签、跳过样本是否计入分母、失败率相同时如何排序。复杂度 O(n * t)，其中 t 是样本标签数。

### 43. 编程题：实现一个简单的 rate limiter。

**参考答案：**  
可以用令牌桶或滑动窗口。令牌桶适合允许短时突发，按固定速率补充 token；请求到来时有 token 则通过，否则拒绝或排队。测试用例包括单用户、并发、突刺、长时间空闲后恢复、多租户隔离和系统时间跳变。

### 44. SQL 题：如何统计每个 prompt 版本的评测通过率？

**参考答案：**  
假设表 `eval_results(prompt_version, sample_id, passed, created_at)`，按 `prompt_version` 分组，用 `sum(case when passed then 1 else 0 end) / count(*)` 计算通过率，并按时间过滤。还可以按标签、模型版本和严重等级下钻，避免平均值掩盖关键场景退化。

### 45. Linux 题：如何从大量日志中定位超时请求？

**参考答案：**  
可以用 `grep` 搜索 timeout/request_id，用 `awk` 提取耗时字段并排序，用 `jq` 处理 JSON 日志，用 `tail -f` 观察实时日志。更好的做法是结构化日志和 tracing，把 request_id 贯穿 API、检索、工具和模型调用，避免手工拼日志。

### 46. 系统设计题：设计一个 AI 回归评测平台。

**参考答案：**  
核心模块包括数据集管理、运行调度、模型/配置管理、评分器、指标计算、结果存储、报告、人工复核和 CI/CD 集成。运行时支持并发、重试、成本控制和权限隔离。评分器包括规则、语义相似度、LLM judge 和人工黄金集。平台要支持版本对比、失败聚类、发布门禁和线上样本回流。

## AI 相关加分点

- 熟悉 eval 思维：数据集、rubric、指标、黄金集、人工校准、线上回流和发布门禁。
- 能说明 LLM 质量不是单点准确率，而是准确性、安全性、鲁棒性、延迟、成本和用户体验的综合权衡。
- 能把 prompt、RAG、工具调用、结构化输出、Agent 安全和内容安全都纳入测试体系。
- 能清楚区分规则断言、LLM judge 和人工评审的边界，知道如何治理误报和漂移。
- 能用 AI 反过来提升测试效率：生成候选用例、日志摘要、失败聚类、PR 风险分析、自动补全测试脚本。

## 复习建议

1. 重点复习 OpenAI 官方 Evaluation best practices，理解 eval 与传统测试的差异。
2. 准备一个 AI 应用质量项目案例，最好覆盖 RAG、prompt、工具调用、评测集和 CI 门禁。
3. 复习 API 测试：认证、限流、流式响应、异步任务、幂等、错误码、日志脱敏。
4. 复习安全测试：prompt injection、越狱、内容安全、人工审核、红队样本和拒答能力。
5. 复习性能与稳定性：首 token 延迟、吞吐、队列、重试、缓存、成本和降级。
6. 编程题重点练习哈希表、滑动窗口、队列、TopK、日志统计、rate limiter 和 SQL 聚合。
7. 行为面试中强调高标准、快速学习、跨团队协作和对 AI 安全质量的责任感。
