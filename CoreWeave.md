# CoreWeave 测试开发工程师面试题

## 公司名

CoreWeave

## 岗位方向

测试开发工程师 / SDET / QA Automation Engineer / AI 云基础设施、GPU 集群、Kubernetes、HPC、推理服务、可观测性、平台稳定性与质量工程方向

## 资料来源与整理依据

- [CoreWeave Documentation](https://docs.coreweave.com/)
- [Product Home](https://docs.coreweave.com/product-home)
- [Get started with CoreWeave](https://docs.coreweave.com/get-started)
- [CoreWeave Kubernetes Service](https://docs.coreweave.com/products/cks)
- [CKS Cluster Components](https://docs.coreweave.com/products/cks/reference/cluster-components)
- [CoreWeave Observe](https://docs.coreweave.com/observability)
- [Mission Control Agent](https://docs.coreweave.com/observability/mission_control_agent)
- [Hardware observability](https://docs.coreweave.com/platform/fleet-management/hardware-observability)
- [Security Introduction](https://docs.coreweave.com/security)
- [Security Architecture](https://docs.coreweave.com/security/architecture)
- [Deploy network policies with CKS](https://docs.coreweave.com/security/tutorials/deploying-network-policies)
- [Shared Responsibility Model](https://docs.coreweave.com/policies/terms-of-service/shared-responsibility-model)
- 公开候选人面经中常见的云平台 / Kubernetes / GPU 计算 / AI 推理 / HPC / SRE / 测试开发方向：API 自动化、集群生命周期、调度正确性、性能压测、稳定性、可观测性、安全隔离、CI/CD、故障演练和行为面。

> 说明：以下题目不是 CoreWeave 官方题库，而是结合公开资料、AI 云平台产品形态和测试开发岗位能力模型整理的高频准备题与模拟题。

## 面试画像

CoreWeave 的核心场景围绕 AI 与高性能计算基础设施，包括 CoreWeave Kubernetes Service、GPU/CPU 裸金属集群、SUNK、推理与训练工作负载、网络、存储、可观测性、安全与企业级运维。测试开发工程师需要证明自己能把 GPU 集群、Kubernetes 控制面、HPC 调度、AI 推理服务和生产可观测性转化为可自动化、可复现、可度量的质量体系。

## 题目分类

### 一、岗位理解与 AI 云质量

### 1. 你如何理解 CoreWeave 测试开发工程师的核心价值？

参考答案：核心价值是保障 AI 云基础设施在大规模 GPU 训练、推理和 HPC 工作负载下稳定、可观测、安全且可持续交付。质量不只体现在 API 是否返回成功，还包括集群创建成功率、GPU 可用率、调度正确性、网络吞吐、存储性能、推理延迟、故障恢复、安全隔离和发布回滚能力。

### 2. AI 云平台测试和普通云平台测试有什么不同？

参考答案：普通云平台更关注计算、网络、存储和权限的通用能力；AI 云平台还要关注 GPU 拓扑、驱动版本、CUDA 镜像、RDMA、高吞吐存储、分布式训练、模型推理延迟和资源碎片。很多问题只有在多节点、多 GPU、长时间运行和高并发推理下才会暴露。

### 3. 如果入职 CoreWeave，你会优先建立哪些质量指标？

参考答案：可以建立集群创建成功率、节点就绪时长、GPU 分配成功率、Pod 调度等待时间、推理 P95/P99 延迟、训练任务成功率、GPU 利用率、存储吞吐、网络丢包、控制面错误率、告警误报率、回滚成功率和客户可见事故数。指标要能按区域、集群、节点池、GPU 类型、Kubernetes 版本和工作负载类型下钻。

### 4. 如何设计 AI 基础设施发布门禁？

参考答案：门禁应包含单元测试、API 合约测试、集群生命周期测试、节点池升级测试、GPU smoke test、Kubernetes 兼容测试、性能基准、安全策略校验和回滚演练。对高风险发布要增加灰度、分区域放量和观测窗口，只有关键 SLO 未退化才继续推进。

### 5. 面试中如何体现自己适合 CoreWeave？

参考答案：用项目案例说明自己既懂测试开发，也懂云原生和 AI 工作负载。例如介绍如何搭建 Kubernetes E2E 自动化、如何压测推理服务、如何用指标定位 GPU 节点异常、如何在 CI/CD 中加入发布门禁，以及如何用 AI 辅助生成测试用例和分析故障日志。

### 二、CKS 与 Kubernetes 集群质量

### 6. 如何测试 CKS 集群创建和删除流程？

参考答案：覆盖不同区域、Kubernetes 版本、节点池规模、GPU 类型、网络配置、存储挂载和权限配置。创建后要断言控制面可用、节点 Ready、系统组件健康、GPU 能被工作负载识别、默认监控和安全组件正常。删除时验证资源回收、凭证失效、账单资源释放和重复删除的幂等性。

### 7. 如何测试 Kubernetes 版本兼容性？

参考答案：建立版本矩阵，覆盖当前支持版本、升级路径、弃用 API、Helm chart、CSI、CNI、GPU 驱动、监控组件和典型客户工作负载。测试要包含新建集群、原地升级、回滚、长跑 workload 和 API 兼容性扫描，避免升级后出现调度失败或系统组件不兼容。

### 8. 节点池配置更新和回滚怎么测试？

参考答案：构造 GPU 驱动、系统镜像、Kubernetes 版本、标签、污点和容量变更场景，验证 staged update、滚动更新、失败中断、回滚和配置历史。重点断言更新期间已有任务是否被正确保护，新任务是否按预期调度，以及失败后节点池状态是否可解释、可恢复。

### 9. 如何验证 GPU 调度正确性？

参考答案：使用不同 GPU 请求、亲和性、污点容忍、优先级、队列和资源配额组合，验证 Pod 是否落到符合条件的节点。还要检查 GPU 数量、MIG 或设备插件状态、显存占用、驱动可见性和调度失败原因。对负向用例要确保错误信息明确，而不是静默 Pending。

### 10. 如何测试 CKS 的网络能力？

参考答案：覆盖 VPC、Service、Ingress、DNS、NetworkPolicy、跨节点通信、外部访问、带宽、延迟和故障注入。对 AI 工作负载要增加多节点通信、分布式训练、模型服务入口和大流量数据传输场景。断言不仅看连通性，还要看吞吐、抖动、重试和隔离策略。

### 11. 如何测试存储和数据路径？

参考答案：覆盖对象存储、共享文件系统、PVC 创建删除、挂载权限、并发读写、小文件和大文件、断点恢复、快照或备份策略。AI 训练场景要关注数据加载吞吐、元数据压力和长时间训练期间的稳定性。测试结果应记录吞吐、延迟、错误率和数据一致性校验。

### 12. Kubernetes Secrets 加密这类安全能力怎么验证？

参考答案：从功能和安全两层验证。功能层面检查 Secret 创建、读取、轮换和升级后仍可用；安全层面验证 etcd 中敏感数据不以明文存储、无权限用户无法读取、审计日志完整。还要覆盖升级、备份恢复和跨版本兼容，避免安全能力破坏业务可用性。

### 三、SUNK、HPC 与训练工作负载

### 13. 如何测试 Slurm on Kubernetes 方案？

参考答案：测试 Slurm 控制组件部署、登录节点、队列、作业提交、取消、重试、节点分配、日志收集和资源释放。还要验证 Kubernetes 与 Slurm 状态一致，避免出现 Slurm 认为节点可用但 Kubernetes 不可调度的状态偏差。

### 14. 多节点多 GPU 训练任务的自动化测试怎么做？

参考答案：准备轻量但能真实占用 GPU 的训练样例，覆盖 1 节点、N 节点、不同 GPU 数量、失败重试和长跑场景。断言训练脚本启动、节点间通信、checkpoint 写入、吞吐、GPU 利用率和任务退出码。测试应收集日志、指标和作业元数据，便于失败定位。

### 15. 如何测试 HPC 调度公平性和优先级？

参考答案：构造多个用户、多个队列、不同优先级和不同资源请求的作业，验证先来先服务、抢占、配额、回填和资源释放规则。公平性不能只看单个任务成功，要看一段时间内资源分配是否符合策略，是否存在低优先级任务饿死或高优先级任务无法启动的问题。

### 16. 登录 Pod 生命周期管理有哪些测试点？

参考答案：覆盖创建、重启、超时、用户会话、权限、环境变量、配置更新和异常退出。还要测试登录 Pod 与计算 Pod 的网络、文件系统和身份信息一致性。用户体验方面要验证错误提示、MOTD、命令行工具和故障恢复路径是否清晰。

### 17. CUDA 镜像和驱动兼容性如何测试？

参考答案：建立 CUDA、NVIDIA 驱动、容器基础镜像、框架版本和 GPU 型号矩阵。测试样例包括 `nvidia-smi`、PyTorch/TensorFlow CUDA 可用性、简单训练、NCCL 通信和推理。兼容性测试要进入 CI，防止系统镜像或驱动升级导致客户 workload 突然失败。

### 四、推理服务、性能与稳定性

### 18. 如何测试 LLM 推理服务？

参考答案：覆盖模型加载、冷启动、流式输出、并发请求、长上下文、批处理、超时、限流、错误码和资源释放。断言首 token 延迟、总延迟、吞吐、显存、GPU 利用率和输出格式。还要加入异常输入、超长 prompt、取消请求和模型实例滚动升级。

### 19. 推理服务自动扩缩容怎么验证？

参考答案：构造阶梯流量、突增流量和回落流量，验证扩容触发、节点准备、模型加载、流量切换和缩容保护。指标包括扩容耗时、请求失败率、队列等待、冷启动延迟和资源浪费。缩容时要确保不杀死正在生成结果的请求。

### 20. 如何做 P95/P99 延迟压测？

参考答案：先定义接近真实业务的 prompt 长度、输出长度、并发模型和请求分布，再用压测工具持续施压。需要分开观察首 token、整体响应、错误率、队列时长、GPU 利用率和网络指标。P99 抖动通常比平均值更能说明生产体验，报告中要给出瓶颈分析。

### 21. 模型服务灰度发布怎么测试？

参考答案：覆盖新旧模型并存、按比例路由、按租户路由、回滚、兼容旧 API 和指标对比。自动化要验证新版本错误率、延迟、显存占用和输出质量没有明显退化。对不可确定输出可以用评测集、规则检查和人工抽检结合，而不是只断言字符串相等。

### 22. 如何定位 GPU OOM 导致的服务失败？

参考答案：先从容器日志、事件、DCGM 指标、显存曲线和请求大小确认 OOM 是否发生，再分析是否由 batch size、上下文长度、并发、模型权重或内存碎片引起。测试上要补充压力边界、显存保护、错误恢复和降级策略，确保 OOM 后实例能自动恢复并返回明确错误。

### 23. 如何验证多租户工作负载隔离？

参考答案：从命名空间、RBAC、网络、存储、GPU 资源、日志和监控权限验证隔离。负向测试包括跨命名空间访问、共享卷误挂载、缓存泄露、服务发现越界和指标数据越权。多租户隔离还要在高负载下验证，避免资源争抢影响其他租户 SLO。

### 五、可观测性、告警与 AI 运维

### 24. 如何测试硬件可观测性？

参考答案：验证 GPU 温度、功耗、显存、ECC、节点健康、网络和存储指标是否采集、聚合、展示和告警。测试要覆盖正常、异常、指标缺失、节点重启和采集组件升级。硬件指标必须能关联到节点、作业、租户和时间窗口，方便定位训练或推理问题。

### 25. DCGM 指标如何用于测试？

参考答案：DCGM 指标可以帮助验证 GPU 利用率、显存、温度、错误计数和健康状态。测试开发可以在压测和训练样例中采集这些指标，作为断言和基准对比。比如发布后同样 workload 的 GPU 利用率下降或 ECC 错误增多，就可能是质量风险。

### 26. 如何测试 Mission Control Agent 这类 AI 运维助手？

参考答案：测试意图识别、指标检索、日志摘要、权限边界、引用来源、回答一致性和错误处理。构造真实事故样例，例如节点异常、推理延迟升高、训练任务失败，验证 Agent 能否给出可追溯的诊断线索。对 AI 输出要做事实性评测，避免编造不存在的指标或操作建议。

### 27. 告警集成到 Slack 时要测什么？

参考答案：覆盖 OAuth 授权、权限申请、频道选择、告警发送、重复告警抑制、恢复通知、撤销授权和失败重试。企业场景还要验证安装审批流程、私有频道权限、消息内容脱敏和审计记录。自动化要能模拟外部服务失败，验证告警不丢失。

### 28. 如何用可观测性数据支持事故复盘？

参考答案：事故复盘需要时间线、指标曲线、日志、事件、变更记录、告警和影响范围。测试开发可以推动统一 trace id、作业 id、集群 id 和发布版本字段，让数据可关联。好的复盘不仅找根因，还要沉淀自动化回归、告警规则和发布门禁。

### 六、安全、权限与合规质量

### 29. IAM 和 RBAC 的测试重点是什么？

参考答案：覆盖用户、组、角色、策略、token、过期、撤销和最小权限。正向测试验证授权用户能完成操作，负向测试验证无权限用户无法访问集群、存储、指标和配置。还要检查错误信息不泄露敏感资源名称，审计日志能记录主体、动作、资源和结果。

### 30. 如何测试 NetworkPolicy？

参考答案：先建立默认拒绝策略，再按业务需要添加允许规则，使用测试 Pod 验证允许和禁止路径。覆盖同命名空间、跨命名空间、DNS、外部出口和服务入口。测试要确认策略变更生效时间、回滚和误配置提示，避免网络策略只在文档上安全。

### 31. 数据隔离怎么验证？

参考答案：验证存储、网络、执行环境、日志和指标层面的隔离。测试用例包括不同租户同名资源、共享存储误配置、对象路径猜测、日志查询越权和备份恢复后的权限保持。数据隔离测试应作为企业客户场景的发布门禁。

### 32. OIDC Workload Identity Federation 有哪些测试点？

参考答案：覆盖 token 签发、audience、issuer、过期时间、权限映射、外部云服务访问、失败重试和密钥轮换。重点验证无需长期密钥也能安全访问外部资源，同时无权限 workload 无法伪造身份。还要测试时钟漂移和 token 过期后的错误路径。

### 33. 如何理解共享责任模型对测试的影响？

参考答案：共享责任模型要求区分平台负责和客户负责的边界。测试开发要验证平台提供的安全能力可靠，例如基础设施隔离、日志、网络、安全默认值；同时在文档、错误提示和示例中明确客户需要配置的部分，例如应用权限、镜像安全和数据访问策略。

### 七、测试平台、CI/CD 与工程能力

### 34. 如何为 CoreWeave 设计 API 自动化框架？

参考答案：框架应支持多环境、多租户、鉴权、资源创建回收、重试、并发、契约校验和测试数据隔离。云资源成本较高，必须有清理兜底和超时保护。测试报告要记录请求、资源 id、集群版本、区域、节点池和失败上下文，方便定位。

### 35. 如何控制端到端测试成本？

参考答案：将测试分层：PR 阶段跑 mock、契约和小规模 smoke；每日跑集群生命周期和少量 GPU workload；发布前跑完整性能、故障和兼容矩阵。昂贵 GPU 测试要按风险触发，并自动回收资源。通过基线比较减少无意义长跑。

### 36. 如何做云平台故障注入？

参考答案：可以注入节点 NotReady、Pod 重启、网络延迟、存储错误、控制面 API 限流、指标采集中断和推理实例失败。故障注入要有安全边界，只在隔离环境或灰度范围执行。验证点包括告警、降级、自动恢复、客户可见错误和数据一致性。

### 37. 如何设计 AI 云平台测试数据？

参考答案：准备小模型、大模型、轻量训练脚本、分布式训练样例、不同大小数据集、模拟客户配置和异常输入。测试数据要可版本化、可清理、可复现。对敏感场景使用合成数据，避免真实客户数据进入测试环境。

### 38. 性能基准测试如何避免不稳定？

参考答案：固定硬件类型、镜像版本、数据集、并发模型、预热时间、测试时长和指标采集方式。每次运行要记录环境元数据，并与历史基线比较。对 GPU 和网络性能要允许合理波动，但超过阈值必须输出趋势和可能原因，而不是简单判失败。

### 39. 你会如何把 AI 引入测试平台？

参考答案：可用于需求生成测试点、从事故日志提取回归用例、辅助分析失败、生成 Kubernetes 资源组合、总结性能报告和做变更风险评分。关键是建立人工审核、事实校验和可追溯输入，避免 AI 直接决定发布结果。

### 40. 如何评估 AI 生成测试用例的质量？

参考答案：从覆盖率、有效性、可执行性、重复率、发现缺陷能力和误导风险评估。可以用历史缺陷、接口契约、代码变更和人工标注样本构建评测集。AI 生成内容必须进入现有测试管理流程，经过去重、审查和稳定性验证。

### 41. RAG 或 Agent 在运维测试中有什么风险？

参考答案：风险包括检索到过期文档、错误关联指标、泄露敏感日志、执行危险命令和编造结论。测试要覆盖权限过滤、来源引用、拒答策略、操作确认、审计日志和回滚机制。Agent 适合辅助诊断，但生产操作必须有安全护栏。

### 42. 如何在 CI/CD 中处理 flaky 测试？

参考答案：先通过重试统计、失败日志和环境信息识别 flaky 类型，再按原因治理，例如等待条件不稳定、共享资源污染、外部依赖波动或测试数据竞争。短期可以隔离和标记，长期要修复根因。云平台测试不能让 flaky 掩盖真实稳定性问题。

### 八、编程、数据与行为面模拟题

### 43. 给你一批推理请求日志，如何找出 P99 延迟异常的租户？

参考答案：按租户、模型、区域和时间窗口聚合，计算 P50、P95、P99、错误率和请求量。先过滤请求量过小的噪声，再对比历史基线和全局趋势。最后关联发布版本、节点池、GPU 类型和队列时长，判断是单租户模型问题还是平台问题。

### 44. 如何设计一个脚本检查 Kubernetes 资源泄漏？

参考答案：脚本应读取测试运行 id 或标签，扫描 Pod、PVC、Service、Ingress、Secret、Job 和自定义资源，识别超过 TTL 的资源并输出清单。清理前要确认资源属于测试环境，避免误删客户资源。结果应写入 CI 报告，并在清理失败时阻断后续昂贵测试。

### 45. 如果多个训练任务争抢 GPU，你会如何排查调度延迟？

参考答案：先看队列长度、资源请求、节点可用 GPU、污点、亲和性、优先级和配额，再看调度器日志和事件。若资源足够但仍延迟，检查设备插件、节点标签、网络或存储约束。最终要把排查步骤沉淀成自动化诊断脚本和告警规则。

### 46. 讲一次你推动稳定性提升的经历。

参考答案：可以按 STAR 结构回答：背景是某服务在发布后 P99 延迟波动；任务是定位并建立防线；行动包括补充压测、指标分层、故障注入、发布门禁和回滚策略；结果是延迟回归提前暴露、事故减少、排查时间缩短。重点体现你能从单次问题扩展到工程化质量体系。

## AI 相关加分点

- 能把 GPU 云、Kubernetes、HPC、推理服务和 LLM 应用质量放在同一个质量模型中分析。
- 熟悉 Kubernetes 自动化、Helm、CI/CD、Prometheus/Grafana、DCGM、日志分析和故障注入。
- 能设计 AI 辅助测试平台，但同时强调事实校验、权限隔离、审计和人工审核。
- 理解 LLM 推理质量不只看功能，还看首 token 延迟、吞吐、显存、稳定性、输出质量和安全边界。
- 能用数据分析定位性能退化，例如按租户、区域、GPU 型号、版本和 workload 类型下钻。
- 具备云成本意识，知道如何分层设计昂贵 GPU 测试，避免资源泄漏。

## 复习建议

- 复习 Kubernetes 基础：Pod、Deployment、Job、Service、Ingress、RBAC、NetworkPolicy、CSI、CNI、污点、亲和性和调度。
- 复习 GPU 与 AI 基础设施：CUDA、NVIDIA 驱动、DCGM、显存、NCCL、分布式训练、推理延迟和 batch。
- 练习云平台测试设计：集群生命周期、节点池升级、回滚、权限、安全隔离、资源清理和多租户场景。
- 准备 2 个项目故事：一个自动化测试平台故事，一个性能或稳定性治理故事。
- 准备 AI 加分案例：用 LLM 生成测试点、分析日志、总结事故或构建质量助手，并说明如何控制幻觉和权限风险。
- 面试表达时多用指标：P95/P99、错误率、GPU 利用率、调度等待时间、创建成功率、恢复时间和告警准确率。
