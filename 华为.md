# 华为测试开发工程师面试题整理

生成时间：2026-05-31  
岗位方向：测试开发工程师 / 软件测试开发 / 解决方案测试 / 质量工程 / DevOps / 云服务测试 / AI 测试  
适用场景：华为云、鸿蒙/终端 OS、通信网络、编译器与芯片协同、企业软件、数字能源、智能汽车解决方案等业务方向。

## 一、面试画像

华为测试开发面试通常强调基础扎实、工程规范、交付质量、可靠性、安全性和端到端解决方案能力。公开面经中常见问题包括测试理论、测试设计、脚本编写、Linux、网络、数据库、自动化测试、项目经历、压力面试和行为面试。华为云 CodeArts/TestPlan、云测试服务、流水线、质量看板、接口自动化、测试计划和 DevOps 产品资料，也体现出华为对工程方法、流程规范、自动化、质量门禁和规模化质量管理的重视。

注意：本文不是官方题库，而是基于公开面经、华为云/开发者官方资料、岗位描述和测试开发常见考点整理出的复习材料。

复习优先级建议：

1. 基础能力：Linux、网络协议、MySQL、Redis、Shell、Python/Java/C++、数据结构。
2. 测试能力：测试分析、用例设计、接口自动化、性能、安全、可靠性、兼容性。
3. 工程能力：CodeArts/DevOps 思维、流水线、质量门禁、测试计划、质量看板。
4. 业务理解：云服务、终端 OS、鸿蒙应用、通信网络、边缘设备、芯片/编译器。
5. AI 能力：AI 用例生成、LLM/RAG 测试、AI Agent 工具调用、AI 代码质量评估。

## 二、计算机基础

### 1. Linux 中如何排查服务 CPU 飙高？

**参考答案：**  
先用 `top` 或 `htop` 定位进程，再用 `top -H -p pid` 定位线程。Java 服务可用 `jstack` 查看线程栈，Go 服务可用 pprof，Python 服务可用 py-spy。结合日志、trace、GC、线程池、锁等待、异常重试和流量变化判断根因。排查时要保留时间线，关联最近发布、配置变更和告警。

**AI 相关加分点：**  
AI 可以辅助总结线程栈和日志，但必须给出证据：线程 ID、堆栈、异常时间和对应请求。

### 2. TCP 和 UDP 有什么区别？通信业务为什么要关注？

**参考答案：**  
TCP 面向连接、可靠、有序、有拥塞控制，适合文件传输、HTTP 接口和可靠控制链路。UDP 无连接、延迟低、开销小，但不保证可靠和有序，适合实时音视频、弱网通信、部分 IoT 和边缘场景。通信业务要关注丢包、抖动、重传、拥塞、带宽、时延和可靠性。

**AI 相关加分点：**  
AI 生成通信测试方案时，要明确 QoS/QoE 指标，如延迟、丢包、抖动、吞吐、恢复时间。

### 3. HTTPS 请求的大致过程是什么？

**参考答案：**  
HTTPS 在 HTTP 与 TCP 之间增加 TLS 层。客户端发起握手，服务端返回证书，客户端校验证书，双方协商密钥，后续使用对称密钥加密传输。测试要覆盖证书过期、域名不匹配、弱 TLS 配置、证书链、代理抓包、重定向和混合内容。

**AI 相关加分点：**  
AI 测试工具采集日志时必须脱敏 token、证书、cookie 和用户隐私。

### 4. MySQL 事务隔离级别有哪些？会导致哪些并发问题？

**参考答案：**  
常见隔离级别包括读未提交、读已提交、可重复读和串行化。并发读写可能出现脏读、不可重复读、幻读和锁等待。测试开发要能设计并发用例验证账户、库存、配置、订单、资源状态等事务一致性，并用日志和数据库快照验证结果。

**AI 相关加分点：**  
AI 可生成并发测试脚本草案，但事务正确性必须通过最终数据对账确认。

### 5. Redis 缓存穿透、击穿、雪崩是什么？如何测试？

**参考答案：**  
穿透是大量查询不存在数据打到数据库；击穿是热点 key 过期后大量请求同时回源；雪崩是大量 key 同时过期或缓存集群异常导致数据库压力暴增。测试要覆盖非法 ID、高并发热点、批量过期、Redis 故障、空值缓存、互斥重建、限流和熔断。

**AI 相关加分点：**  
AI 可以根据依赖关系生成缓存风险清单，但容量模型要结合真实流量和数据分布。

### 6. 线程池核心参数有哪些？测试时关注什么？

**参考答案：**  
核心参数包括核心线程数、最大线程数、队列长度、空闲时间、拒绝策略和线程工厂。测试要关注高并发下排队、拒绝、超时、线程泄漏、异常重试和资源耗尽。压测时监控线程数、队列长度、响应时间、错误率、CPU、内存和下游依赖耗时。

**AI 相关加分点：**  
AI 可以给调参建议，但要通过对照实验验证吞吐、延迟和错误率是否改善。

### 7. 进程、线程和协程有什么区别？

**参考答案：**  
进程是资源分配单位，线程是 CPU 调度单位，同一进程内线程共享内存；协程是用户态轻量级执行单元，切换成本低，适合高并发 IO。测试平台、压测工具、设备云和执行器设计时，需要根据 CPU 密集、IO 密集、隔离性和资源限制选择并发模型。

**AI 相关加分点：**  
AI Agent 并发执行测试任务时，要限制模型请求、浏览器实例、设备占用和文件句柄。

## 三、编码与算法

### 8. 写一个 Shell 命令，统计日志中出现次数最多的 10 个接口路径。

**参考答案：**  
可以用 `awk` 提取路径，再排序计数。实际字段要根据日志格式调整。

```bash
awk '{print $7}' access.log | sort | uniq -c | sort -nr | head -10
```

面试时要说明日志格式、字段位置、异常行、压缩日志和大文件处理策略。

**AI 相关加分点：**  
AI 可以生成命令草案，但要确认字段位置和脱敏规则。

### 9. 写一个函数，判断字符串括号是否合法。

**参考答案：**  
使用栈。遇到左括号入栈，遇到右括号时检查栈顶是否匹配；遍历结束后栈为空才合法。时间复杂度 O(n)，空间复杂度 O(n)。

```python
def valid_brackets(s: str) -> bool:
    pairs = {')': '(', ']': '[', '}': '{'}
    stack = []
    for ch in s:
        if ch in pairs.values():
            stack.append(ch)
        elif ch in pairs:
            if not stack or stack.pop() != pairs[ch]:
                return False
    return not stack
```

**AI 相关加分点：**  
AI 生成代码后，要补空串、只有右括号、嵌套括号和普通字符场景。

### 10. Top K 高频元素怎么做？

**参考答案：**  
先用哈希表统计频次，再用小顶堆维护 K 个高频元素，时间复杂度 O(n log k)。如果数据范围有限可用桶排序。海量日志可先分片统计，再合并 Top K。

**AI 相关加分点：**  
线上缺陷、设备崩溃和用户反馈可先用 Top K 找热点，再用 LLM 做语义聚类。

### 11. 如何判断链表是否有环？

**参考答案：**  
使用快慢指针。慢指针每次一步，快指针每次两步，如果存在环，二者最终会相遇；如果快指针到达空节点，则无环。时间复杂度 O(n)，空间复杂度 O(1)。

**AI 相关加分点：**  
AI 生成用例时要覆盖空链表、单节点自环、尾节点成环和无环链表。

### 12. 如何实现 LRU 缓存？

**参考答案：**  
使用哈希表加双向链表。哈希表支持 O(1) 查找，双向链表维护访问顺序。读取或写入节点时移动到头部，容量超限时淘汰尾节点。要注意更新已有 key、容量为 0、删除哈希映射和并发访问。

**AI 相关加分点：**  
AI 写 LRU 时常漏掉更新已有 key 后移动节点或删除旧映射，测试用例要专门覆盖。

### 13. 如何测试一个排序函数？

**参考答案：**  
不仅检查输出有序，还要检查元素数量不变、频次不变、没有丢失和新增。如果要求稳定排序，还要验证相等元素相对顺序。用例包括空数组、单元素、已排序、逆序、重复元素、负数、极值、大量随机数和局部有序数组。

**AI 相关加分点：**  
可以使用 property-based testing，让 AI 辅助生成属性断言，再由程序自动验证。

### 14. 分页接口如何校验排序正确且无重复？

**参考答案：**  
单页内用 set 校验 ID 不重复，并检查排序字段单调；跨页校验上一页最后一条与下一页第一条的顺序关系，检查是否跨页重复或遗漏。还要考虑新增/删除数据导致的翻页漂移、游标过期和排序字段相同的 tie-breaker。

**AI 相关加分点：**  
AI 可根据 JSON schema 生成基础断言，但分页一致性和业务排序规则需要人工明确。

## 四、华为业务场景测试

### 15. 如何测试华为云创建 ECS 云主机流程？

**参考答案：**  
覆盖地域、可用区、镜像、规格、网络、安全组、磁盘、计费、配额、权限、创建进度、失败回滚和资源释放。要验证控制台、OpenAPI、资源编排、账单和监控一致。异常包括库存不足、配额不足、支付失败、依赖服务超时、重复提交和权限不足。

**AI 相关加分点：**  
AI 可从 OpenAPI 生成参数组合，但云资源创建成本高，要用沙箱、Mock 和配额控制。

### 16. 如何测试对象存储上传下载？

**参考答案：**  
覆盖小文件、大文件、分片上传、断点续传、秒传、覆盖写、权限、临时凭证、跨区域、加密、校验和、下载限速和删除恢复。性能上关注吞吐、延迟、并发连接、失败重试。安全上关注越权访问、URL 过期、恶意文件和敏感数据泄露。

**AI 相关加分点：**  
AI 可生成参数矩阵，但权限、临时凭证和签名校验必须用真实鉴权链路验证。

### 17. 如何测试鸿蒙应用兼容性？

**参考答案：**  
覆盖不同设备形态、系统版本、分辨率、权限、窗口模式、深浅色模式、折叠屏、多端流转、消息推送和后台生命周期。核心链路包括安装、启动、登录、页面跳转、网络请求、存储、分享和崩溃恢复。要采集日志、截图、性能和崩溃信息。

**AI 相关加分点：**  
AI 可识别截图异常和聚类崩溃堆栈，但设备覆盖策略要基于用户设备分布。

### 18. 如何测试终端 OS 更新？

**参考答案：**  
覆盖升级包下载、校验、安装、重启、回滚、断点续传、低电量、空间不足、网络中断、不同版本路径、数据保留和兼容性。升级后要验证系统服务、权限、应用兼容、性能、耗电、发热和用户数据完整性。

**AI 相关加分点：**  
AI 可从历史升级故障中生成风险清单，但升级回滚和数据完整性必须真实验证。

### 19. 如何测试通信基站或网络设备软件？

**参考答案：**  
覆盖配置下发、连接建立、数据转发、性能吞吐、时延、丢包、告警、容灾、升级、回滚、兼容性和安全。可靠性测试要关注长稳、故障注入、主备切换、网络抖动、电源异常和资源泄漏。测试环境要尽可能模拟真实网络拓扑。

**AI 相关加分点：**  
AI 可生成拓扑和故障场景，但通信协议和性能指标要用专业工具精确测量。

### 20. 如何测试编译器与芯片协同相关产品？

**参考答案：**  
覆盖编译正确性、性能、兼容性、运行时、指令集、优化选项、异常处理和跨平台一致性。可使用基准测试、随机程序生成、差分测试、回归套件和真实业务模型。重点是定位编译前端、优化器、后端、运行时或硬件适配问题。

**AI 相关加分点：**  
AI 可生成测试程序和变异样例，但编译器正确性要依赖可复现、可归因的测试套件。

### 21. 如何测试云数据库高可用？

**参考答案：**  
覆盖主备切换、读写分离、备份恢复、扩容、故障恢复、网络抖动、慢查询、锁等待、连接池和数据一致性。关键指标包括 RTO、RPO、切换耗时、错误率、延迟和数据丢失风险。异常测试要在可控环境中进行。

**AI 相关加分点：**  
AI 可分析故障日志和监控，但数据一致性必须通过对账脚本验证。

### 22. 如何测试 IoT 设备接入平台？

**参考答案：**  
覆盖设备注册、鉴权、心跳、消息上报、命令下发、离线重连、固件升级、权限、限流和设备影子。异常场景包括网络抖动、设备掉线、重复消息、消息乱序、非法设备、批量接入和固件升级失败。

**AI 相关加分点：**  
AI 可以生成设备行为模拟脚本，但安全鉴权和消息一致性要真实验证。

### 23. 如何测试数字能源或车联网平台？

**参考答案：**  
覆盖设备数据采集、实时监控、告警、远程控制、策略下发、数据分析、权限、安全和故障恢复。车联网还要关注定位、远程诊断、OTA、弱网、消息延迟和数据隐私。数字能源要关注设备稳定性、极端环境和告警准确性。

**AI 相关加分点：**  
AI 可做异常模式识别，但控制类动作必须有权限、审计和人工确认。

## 五、自动化测试、CodeArts 与质量平台

### 24. 如何设计接口自动化测试框架？

**参考答案：**  
底层封装请求、鉴权、环境、日志、重试；中间层封装业务 API、测试数据和依赖提取；上层编排场景、断言和报告。框架要支持参数化、Mock、并发执行、失败重跑、CI 集成、历史趋势和质量看板。关键是分层清晰，接口变化时维护成本低。

**AI 相关加分点：**  
AI 可根据 OpenAPI 生成基础用例和 client，但业务断言、数据准备和幂等校验需要测试开发把关。

### 25. CodeArts TestPlan 这类测试管理平台应包含哪些能力？

**参考答案：**  
应覆盖测试计划、测试设计、测试用例、测试执行、缺陷提交、报告统计、质量看板、需求覆盖率和自动化用例管理。平台要支持手工用例、接口自动化、WebUI 自动化、自定义执行、用例分层分级、权限和协同。

**AI 相关加分点：**  
AI 可以辅助生成用例、总结报告和推荐风险点，但质量看板的原始数据必须可追溯。

### 26. 如何把自动化测试接入 DevOps 流水线？

**参考答案：**  
在代码提交后触发代码检查、编译构建、单元测试、接口冒烟、契约测试、安全扫描和部署验证；发布前执行核心回归、性能基线和灰度验证。流水线要保存日志、报告、覆盖率和变更信息，并设置质量门禁。门禁要分层，核心失败阻断发布，非核心失败进入风险评估。

**AI 相关加分点：**  
AI 可根据代码 diff 推荐回归范围，但发布阻断规则必须可解释。

### 27. 自动化测试金字塔怎么理解？

**参考答案：**  
测试金字塔强调底层单元测试数量多、执行快；中间接口/服务测试关注业务能力；顶层 UI 测试数量少但覆盖关键路径。接口测试相比 UI 测试开发成本低、运行快、稳定性高，适合放入持续集成。实际项目要结合风险和收益调整比例。

**AI 相关加分点：**  
AI 可帮助从需求生成各层测试建议，但要避免把所有场景堆到 UI 自动化。

### 28. 如何治理 flaky case？

**参考答案：**  
先建立 flaky 指标，如近 N 次失败率、失败原因、影响流水线次数和修复时长。再按环境、数据、脚本、产品缺陷、第三方依赖分类治理。技术上增加稳定等待、数据隔离、失败证据采集、重试分级和隔离队列；流程上设置责任人和 SLA。

**AI 相关加分点：**  
LLM 可以对失败日志分类，但分类结果要附带错误栈、截图、接口响应和环境信息。

### 29. 如何设计移动云测试平台？

**参考答案：**  
平台应包含设备管理、任务调度、应用安装、脚本管理、执行引擎、截图录屏、日志采集、性能采集、兼容性报告、权限和通知。还要处理设备占用、系统版本、厂商 ROM、网络环境、清理恢复和数据隔离。平台价值是降低设备维护成本，提高兼容性覆盖和问题定位效率。

**AI 相关加分点：**  
AI 可做截图识别、失败归因、崩溃聚类和报告摘要，但要避免模型读取未脱敏用户数据。

### 30. 质量看板应该展示哪些指标？

**参考答案：**  
包括需求测试进度、需求覆盖率、用例通过率、自动化覆盖率、缺陷趋势、缺陷严重级别、构建失败率、回归耗时、接口错误率、性能基线、线上问题、灰度异常和质量风险。每个指标都应有口径、阈值、负责人和行动策略。

**AI 相关加分点：**  
LLM 可以自动生成质量日报，但原始数据和计算口径必须可追溯。

## 六、性能、可靠性与安全

### 31. 如何做云服务性能测试？

**参考答案：**  
先明确目标、协议、并发模型、数据量、环境规格和 SLA。执行时监控 QPS、响应时间、错误率、CPU、内存、GC、线程池、连接池、数据库慢查询、缓存命中、网络 IO 和下游依赖。测试后输出容量结论、瓶颈分析和优化建议。

**AI 相关加分点：**  
AI 可汇总监控指标并提出瓶颈假设，但必须通过实验验证。

### 32. 如何测试可靠性和长稳？

**参考答案：**  
可靠性测试关注长时间运行、资源泄漏、异常恢复、主备切换、故障注入、限流降级和数据一致性。长稳测试要设置真实负载、周期性操作、监控资源趋势和日志异常。结果要关注内存增长、句柄泄漏、线程泄漏、错误率和恢复时间。

**AI 相关加分点：**  
AI 可分析长稳日志趋势，但泄漏判断需要持续指标和复现实验。

### 33. 如何测试限流和降级？

**参考答案：**  
限流测试要验证阈值、维度、返回码、提示、恢复和误伤；降级测试要验证核心链路可用、非核心功能关闭、默认值、缓存兜底和用户体验。要模拟下游超时、错误率升高、流量突增和配置变更，并验证监控告警。

**AI 相关加分点：**  
AI 可以根据依赖图生成故障注入清单，但执行要在可控环境逐步扩大。

### 34. 安全测试常见关注点有哪些？

**参考答案：**  
关注认证、授权、会话、输入校验、敏感信息、加密、SQL 注入、XSS、CSRF、越权、文件上传、依赖漏洞、日志脱敏和安全配置。企业级产品还要关注租户隔离、审计、合规和权限最小化。

**AI 相关加分点：**  
AI 可生成攻击样例，但安全测试不能把真实密钥、用户数据和生产接口暴露给模型。

### 35. 线上 P0 故障时，测试开发应该如何参与？

**参考答案：**  
先协助止血，确认影响范围、版本、时间、用户群和核心指标，推动降级、回滚或配置关闭。然后保留日志、trace、监控、配置和变更记录。恢复后参与复盘，补充自动化用例、监控告警、发布门禁和故障演练。

**AI 相关加分点：**  
AI 可快速摘要日志和告警，但故障现场高风险操作必须人工确认。

## 七、AI + 测试专项

### 36. 如何用 AI 辅助测试用例生成？

**参考答案：**  
可以把 PRD、接口文档、历史缺陷、业务规则和代码 diff 输入 AI，让它生成场景清单、边界值、异常路径、决策表和自动化脚本草案。风险包括幻觉、遗漏隐性规则、重复低价值用例、泄露敏感数据和断言不准确。因此要做脱敏、人工评审、规则校验和执行反馈闭环。

**AI 相关加分点：**  
AI 是初稿生成器和覆盖提醒器，最终测试责任仍在测试开发。

### 37. 如何测试 LLM 聊天机器人？

**参考答案：**  
覆盖意图识别、多轮上下文、知识库检索、工具调用、拒答、转人工、隐私保护、响应速度和成本。正确性要有黄金问答集；安全性要测提示注入、越权查询、敏感信息泄露；稳定性要测模型超时、降级和重试；体验上评估语气、一致性和可解释性。

**AI 相关加分点：**  
LLM-as-judge 可以辅助评测，但需要人工标注样本校准，避免评委模型自身偏差。

### 38. 如何测试 RAG 应用？

**参考答案：**  
RAG 测试要拆成检索质量和生成质量。检索侧关注文档切分、向量召回、重排、权限隔离和知识更新；生成侧关注答案正确性、引用准确性、无答案拒答、幻觉和一致性。指标包括召回率、命中率、答案准确率、引用正确率、延迟和 token 成本。

**AI 相关加分点：**  
不要只看最终回答，要定位是没检到、检错了，还是检到了但生成错了。

### 39. 如何测试 AI Agent 的工具调用？

**参考答案：**  
覆盖工具选择、参数生成、权限控制、失败重试、超时、结果校验、多工具编排和审计日志。测试要设计正常任务、边界任务、恶意任务、权限不足任务和工具返回异常任务。Agent 不应在未授权情况下执行删除、发布、付款、外发数据等高风险动作。

**AI 相关加分点：**  
要建立工具调用黄金集和红队集，并量化成功率、误调用率、越权率和任务完成率。

### 40. 如何评估 AI 生成代码的质量？

**参考答案：**  
通过单元测试、契约测试、集成测试、静态扫描、代码评审、覆盖率、边界用例和灰度验证评估。AI 代码容易出现看似合理但边界不完整、风格不一致、重复逻辑和隐藏安全风险。团队应把规范固化到工具链中，在提交前自动检查。

**AI 相关加分点：**  
可以让 AI 先生成测试，再生成实现，并通过 CI 验证；但不能跳过人工 review。

### 41. 如何做智能回归测试选择？

**参考答案：**  
输入包括代码 diff、模块依赖、接口变更、用例覆盖关系、历史缺陷、近期失败、核心链路和风险等级。规则层保证核心链路必跑，模型层做风险排序和额外推荐。输出要包含推荐原因，便于人工判断。上线后持续评估命中率、漏测率、节省时间和误报率。

**AI 相关加分点：**  
核心链路不能完全交给模型裁剪，AI 更适合作为回归范围推荐和优先级排序。

## 八、项目与行为面试

### 42. 介绍一个你做过的自动化测试项目。

**参考答案：**  
建议用 STAR 结构：背景是什么，质量痛点是什么；你负责哪些模块；技术方案如何设计；遇到什么难点；最终收益如何。要讲覆盖率、执行时长、发现缺陷、稳定性、接入流水线和团队使用情况。不要只说“写了很多脚本”，要体现平台化和质量收益。

**AI 相关加分点：**  
可以补充自己如何用 AI 生成初版用例、分析失败日志或总结报告，并说明人工校验机制。

### 43. 交付压力很大，测试时间被压缩怎么办？

**参考答案：**  
先确认变更范围、影响链路、风险等级和回滚方案。按风险优先级选择核心用例，保留主链路、权限、数据一致性、兼容性、性能和灰度验证。明确未覆盖风险并同步相关方，上线后加强监控和快速回滚准备。要体现风险管理，而不是简单说“加班全测”。

**AI 相关加分点：**  
AI 可根据 diff 和需求描述推荐最小回归集，但最终选择要结合业务影响和历史缺陷。

### 44. 客户或项目对接人不满意你的测试工作，如何处理？

**参考答案：**  
先明确不满意点，是质量问题、进度问题、沟通问题还是预期不一致。然后用事实对齐测试范围、风险、缺陷、阻塞和交付计划，补充透明的进度和质量报告。如果确实有遗漏，要快速复盘补救；如果是预期不一致，要推动需求、验收标准和风险边界明确。

**AI 相关加分点：**  
AI 可以帮助整理质量报告和风险摘要，但沟通决策要由人负责。

### 45. 开发不认可你提的 bug，怎么办？

**参考答案：**  
先补充证据，包括复现步骤、环境、版本、日志、截图、接口请求、数据库状态和预期依据。再和开发对齐需求或技术设计，确认是缺陷、体验问题、需求不明确还是测试误判。如果争议仍在，拉产品、架构或项目负责人一起决策。关键是用证据推动闭环。

**AI 相关加分点：**  
AI 可以帮忙整理缺陷描述和影响范围，但不能替代真实证据。

### 46. 你如何理解测试开发岗位？

**参考答案：**  
测试开发是用工程化能力提升质量效率的岗位。它既要理解业务风险，也要能开发工具、框架和平台；既要发现问题，也要推动研发流程改进。优秀测开会从需求、开发、测试、发布、监控、复盘全链路建立质量保障体系。

**AI 相关加分点：**  
AI 时代的测开更像质量工程效率放大器：把重复分析、初稿生成和报告总结交给 AI，把风险判断和关键验证掌握在自己手里。

## 九、反问面试官

1. 团队测试开发更偏业务质量保障、解决方案测试，还是测试平台和工具建设？
2. 当前最影响质量效率的问题是什么：自动化覆盖、环境稳定、设备兼容、失败定位，还是交付风险？
3. 团队是否已经在使用 AI 辅助测试、智能回归、AI Agent 或代码质量评估？
4. 云、终端、通信或编译器方向中，当前最核心的质量指标有哪些？
5. 对新人前三个月的期待是什么？希望先补业务，还是先交付工具能力？

## 十、复习建议

1. 准备两个项目故事：一个自动化/平台项目，一个可靠性/性能/解决方案质量项目。
2. 每天刷 2 道算法题，重点是哈希、栈、链表、堆、滑动窗口、LRU 和并发。
3. 熟悉 Linux、Shell、网络协议、MySQL、Redis、接口自动化分层和 CI/CD。
4. 用华为典型业务练场景题：云主机、对象存储、鸿蒙兼容、OS 升级、通信网络、IoT、编译器、云数据库。
5. 准备工程化质量知识：测试计划、质量看板、测试金字塔、流水线、质量门禁、缺陷闭环。
6. 准备 AI + 测试落地方案：AI 用例生成、LLM/RAG 测试、Agent 工具调用、AI 代码质量、智能回归。

## 十一、参考来源

- 华为云 CodeArts 产品介绍，覆盖需求、代码、检查、编译、验证、部署和发布的端到端研发流程：https://support.huaweicloud.com/productdesc-devcloud/devcloud_pdtd_00001.html
- 华为云 CodeArts TestPlan 产品优势，涉及一站式测试管理、自动化测试、亿级用例、质量看板和需求覆盖率：https://support.huaweicloud.com/productdesc-testman/testman_pdtd_1000.html
- 华为云 CodeArts TestPlan 入门，涉及手工测试、接口自动化、测试计划、测试设计、测试执行和测试评估：https://www.huaweicloud.com/product/cloudtest/getting-started.html
- 华为云 CodeArts DevOps 平台，涉及代码检查、构建、测试、部署任务自动化和流水线门禁：https://www.huaweicloud.com/devcloud/
- 华为开发者联盟云测试服务，涉及兼容性、性能、稳定性、功耗、安全和自动化测试：https://developer.huawei.com/consumer/cn/agconnect/cloud-test/
- 华为测试开发工程师校招岗位信息，涉及编译器与芯片协同、专项测试、集成测试、端到端交付、C/C++/Java/Python/Shell：https://www.shushuqiuzhi.com/position/313154
- 测试人社区：华为测试工程师面经，涉及测试岗位动机、基础和项目问题：https://ceshiren.com/t/topic/17607
- 测试人社区：华为解决方案测试工程师面经，涉及笔试、测试用例、项目测试问题：https://ceshiren.com/t/topic/17056
- 测试人社区：华为实习测试工程师面经，涉及现场编写测试脚本与技术说明：https://ceshiren.com/t/topic/18128
- 华为云专业服务产品介绍，涉及测试自动化规划、工程方法、标准规范和平台实践：https://support.huaweicloud.com/productdesc-professionalservices/usermanual-professionalservices-pdf.pdf

