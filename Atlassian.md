# Atlassian 测试开发工程师面试题整理

- 公司：Atlassian
- 岗位方向：测试开发工程师 / SDET / Quality Engineering / Cloud Platform QA
- 生成日期：2026-06-06
- 适用场景：Jira、Confluence、Bitbucket、Atlassian Cloud、Forge、开发协作平台、AI 辅助测试

## 资料来源与整理依据

- Atlassian 官方文档：Jira Cloud REST API、Forge 平台、Jira Automation、Bitbucket Cloud、Atlassian Cloud 管理与安全相关文档。
- Atlassian Engineering Blog 与公开技术文章：云服务、平台工程、可靠性、开发者体验、协作工具和大规模 SaaS 质量实践。
- 公开面试经验与题库：Glassdoor、InterviewQuery、JobMentis、FinalRound AI、GeeksforGeeks 等对 Atlassian 软件工程、测试、系统设计和行为面试的描述。
- 通用测试开发题型：自动化测试、接口测试、测试平台、CI/CD、性能稳定性、数据分析、SQL/Linux、AI 辅助测试与 LLM 应用测试。

> 说明：Atlassian 不同团队的题目差异较大，以下内容按测试开发工程师能力模型整理，重点覆盖云协作平台、开放 API、插件生态、开发者工具和 AI 质量工程。

## 面试画像

Atlassian 的产品服务于软件研发协作、知识协作和 IT 服务管理，典型质量挑战包括多租户权限、复杂工作流、插件生态、Webhook/API 兼容性、云端可靠性、大规模数据迁移、搜索与通知、自动化规则、CI/CD 集成和开发者体验。测试开发岗位通常会考察编码能力、自动化测试设计、系统性质量思维、平台工具建设、问题定位、跨团队协作和数据驱动改进。

回答时建议突出：你能把 Jira issue、workflow、permission、automation、API、webhook、Bitbucket pipeline 和 Forge app 串成完整质量链路；你能用工程化手段降低回归成本；你能用 AI 提升测试生成、失败归因和质量分析效率。

## 一、岗位理解与项目经验

### 1. 你如何理解 Atlassian 测试开发工程师的核心价值？

**参考答案：**  
核心价值是保障开发协作平台在高复杂度工作流、多租户和生态集成下稳定可信。Atlassian 的质量不仅是页面可用，还包括权限正确、工作流状态一致、API 兼容、自动化规则可靠、插件安全、通知及时和跨产品联动稳定。测试开发需要建设自动化测试、接口契约、质量平台、监控告警和数据化评估，让质量反馈尽早进入研发流程。

**AI 相关加分点：**  
可以补充用 LLM 分析 Jira 缺陷、生成自动化用例、归因 CI 失败、总结 release risk，但关键门禁仍要靠规则、数据和人工抽样。

### 2. Atlassian 类 SaaS 和普通业务系统的测试重点有什么不同？

**参考答案：**  
Atlassian 类 SaaS 更强调多租户隔离、权限模型、配置灵活性、插件生态和公开 API 兼容。普通业务系统往往关注固定业务链路，而 Jira/Confluence 这种平台允许用户自定义字段、工作流、权限、自动化规则和第三方集成，因此测试矩阵更大。测试策略要通过模型化、组合覆盖、契约测试和风险分层控制复杂度。

### 3. 你会如何介绍一个测试平台项目？

**参考答案：**  
按背景、目标、架构、难点、指标和复盘回答。例如：建设面向 Jira 工作流的自动化回归平台，支持租户环境创建、测试数据生成、API/UI 混合执行、失败自动归因和质量看板。指标包括回归耗时下降、关键缺陷提前发现、flaky 率降低、人工测试工时减少和发布信心提升。

### 4. 如果入职 Atlassian，前三个月你会做什么？

**参考答案：**  
第一阶段熟悉产品关键链路，例如创建 issue、工作流流转、权限、automation、API、notifications、search 和 app marketplace。第二阶段分析现有质量数据，如 CI 耗时、失败类型、线上缺陷和高风险模块。第三阶段落地一个专项，例如 workflow 组合测试、Jira Automation 回归平台、Forge app 契约测试或 flaky test 治理。

### 5. 如何在产品快速迭代中保持质量？

**参考答案：**  
通过风险分层和质量门禁实现。PR 阶段跑单测、接口契约、静态扫描和变更相关测试；主干阶段跑核心回归和跨产品集成；发布前跑完整回归、性能基线和灰度验证。对高风险变更，如权限、工作流、计费和迁移，需要更严格的评审和回滚方案。

## 二、Jira 与协作平台业务测试

### 6. 如何测试 Jira issue 创建功能？

**参考答案：**  
覆盖必填字段、自定义字段、字段类型、附件、优先级、assignee、labels、权限、项目配置、默认值和错误提示。还要验证 API 与 UI 行为一致、创建后索引可搜索、通知是否发送、审计日志是否记录。边界包括超长文本、非法字符、大附件、无权限用户和并发创建。

### 7. 如何测试 Jira workflow 状态流转？

**参考答案：**  
先建模状态、transition、条件、校验器和后置动作。用组合覆盖验证不同角色、字段条件、审批条件和自动化触发。重点断言状态变化、字段更新、权限限制、通知、历史记录和 Webhook 事件。对复杂工作流可以用状态机模型生成测试路径，避免人工枚举遗漏。

### 8. 如何测试 Jira 权限模型？

**参考答案：**  
构造组织、站点、项目、角色、用户组和匿名用户，覆盖浏览、创建、编辑、删除、评论、分配、转换状态和管理权限。重点验证 UI、API、搜索结果、通知和审计日志都符合权限。权限测试要特别注意缓存、继承、角色变更、用户离职和批量操作。

### 9. Jira 自定义字段会带来哪些测试风险？

**参考答案：**  
风险包括字段类型兼容、字段必填规则、搜索索引、排序过滤、导入导出、API schema、权限显示和性能。测试时要覆盖文本、单选、多选、用户、日期、数字、级联字段等类型，并验证字段变更后历史数据和自动化规则是否仍然正确。

### 10. 如何测试 Jira Automation？

**参考答案：**  
覆盖触发器、条件、动作、分支规则、定时规则、跨项目规则和循环保护。构造 issue created、field changed、transitioned、comment added 等事件，验证规则是否按预期执行。还要测试权限、规则禁用、执行历史、失败重试、配额限制和递归触发。

### 11. 如何测试 Confluence 页面协作编辑？

**参考答案：**  
覆盖多人同时编辑、版本冲突、评论、权限、附件、宏、草稿恢复和发布历史。并发场景要验证内容不丢失、冲突提示合理、光标和协作者状态正确。还要测试长页面、大附件、嵌入 Jira issue、搜索索引和通知。

### 12. 如何测试搜索功能？

**参考答案：**  
覆盖关键词、过滤器、权限过滤、排序、分页、特殊字符、多语言、附件内容、延迟索引和结果高亮。搜索测试要验证“搜得到”和“搜不到”都正确，尤其要确保无权限内容不会出现在搜索结果中。性能上要关注大租户、大项目和复杂过滤条件的响应时间。

## 三、REST API、Webhook 与 Forge 生态

### 13. Atlassian Cloud REST API 的测试重点是什么？

**参考答案：**  
重点覆盖认证、授权、分页、过滤、字段选择、错误码、限流、幂等、版本兼容和数据一致性。公开 API 还要关注向后兼容，因为第三方工具和客户脚本依赖这些接口。自动化应验证 API 与 UI 行为一致，并对错误响应做契约校验。

### 14. 如何测试 API 分页？

**参考答案：**  
构造空数据、单页、多页、最后一页、越界页、排序变化和分页期间数据新增/删除。验证 startAt/maxResults 或 cursor 行为、总数、下一页链接和重复/遗漏。对大数据量租户要关注分页性能和客户端迭代容错。

### 15. 如何测试 API rate limit？

**参考答案：**  
覆盖不同用户、租户、token、接口、并发突刺和长期高频请求。验证状态码、错误信息、响应头、恢复窗口和客户端退避策略。还要验证一个租户的高流量不会影响其他租户，限流策略不会误伤正常自动化。

### 16. Webhook 为什么需要幂等？

**参考答案：**  
Webhook 可能重复投递、重试或乱序到达。如果消费者不幂等，可能重复创建任务、重复发通知或重复触发自动化。可以用 delivery id、event id 和业务资源版本去重。测试应模拟重复事件、乱序事件、失败重投和消费者慢处理。

### 17. 如何测试 Forge app？

**参考答案：**  
覆盖安装、卸载、权限授权、manifest 配置、resolver、UI kit/custom UI、存储、外部请求、事件触发和错误处理。还要验证 app 只能访问授权范围内的数据，不能跨租户泄露信息。对 Marketplace app，还要测试升级、回滚、兼容性和审计日志。

### 18. 如何测试第三方集成？

**参考答案：**  
使用契约测试和沙箱环境验证接口字段、事件 payload、认证、限流、错误处理和重试。第三方不可控时要用 Mock 或 fake server 模拟超时、5xx、非法响应和慢响应。测试重点是系统能优雅降级，不让外部服务故障拖垮核心链路。

## 四、Bitbucket、CI/CD 与开发者体验

### 19. 如何测试 Bitbucket Pull Request 工作流？

**参考答案：**  
覆盖分支创建、PR 创建、review、评论、合并策略、冲突、必需检查、权限、通知和审计日志。还要测试大 diff、文件重命名、二进制文件、force push、重新打开和 reviewer 变更。自动化可用 API 创建仓库和 PR，再用 UI 验证关键交互。

### 20. 如何设计 Bitbucket Pipelines 的测试策略？

**参考答案：**  
覆盖 YAML 解析、触发条件、变量、secrets、缓存、artifacts、并发、失败重试和权限。对 runner 环境要测试隔离、依赖安装、网络访问和资源限制。质量指标包括流水线排队时长、执行时长、失败率、缓存命中率和用户定位失败的时间。

### 21. 如何测试代码合并策略？

**参考答案：**  
覆盖 merge commit、squash、rebase、fast-forward、冲突、必需 review、必需 CI、分支保护和权限。断言提交历史、PR 状态、检查状态和审计日志。边界包括两个 PR 同时合并、检查状态过期和目标分支更新。

### 22. 如何治理 CI 中的 flaky test？

**参考答案：**  
先分类：环境不稳定、等待时序、测试数据污染、并发冲突、外部依赖、资源不足和真实缺陷。治理方式包括隔离环境、唯一测试数据、显式等待、依赖 mock、失败自动归因、有限重试、owner 机制和隔离名单。不能用无限重试掩盖问题。

### 23. 如何优化大型 SaaS 项目的 CI 耗时？

**参考答案：**  
用变更影响分析、测试分层、并发执行、缓存、构建产物复用、慢测试拆分和风险分级。PR 阶段跑快速和相关测试，夜间跑全量兼容性和性能测试。优化前要先量化瓶颈：排队、依赖安装、构建、测试执行还是环境准备。

### 24. 如何设计开发者体验指标？

**参考答案：**  
可以衡量 PR 首次反馈时间、CI 通过率、失败定位时间、API 错误率、Webhook 投递成功率、Jira 自动化执行成功率、页面加载时间、搜索响应时间和插件安装成功率。质量团队要把这些指标和业务价值关联起来，让改进不只是“测试覆盖率提升”。

## 五、测试平台与自动化框架

### 25. Atlassian 产品的测试金字塔怎么设计？

**参考答案：**  
底层是单元测试和组件测试，覆盖权限、workflow、规则引擎和工具函数。中间层是 API、契约、集成和服务测试，覆盖 Jira、Confluence、Bitbucket、Automation、Forge 等服务交互。顶层是少量关键 E2E，如创建项目、创建 issue、流转状态、触发自动化、发送通知和搜索验证。

### 26. 如何设计 Jira workflow 自动化测试框架？

**参考答案：**  
将 workflow 抽象成状态机，输入状态、transition、角色、条件和期望后置动作。框架负责创建测试项目、生成 issue、执行 transition、校验状态/字段/通知/Webhook，并清理数据。这样可以复用到不同项目配置，减少手写用例。

### 27. 如何做测试数据管理？

**参考答案：**  
按租户、项目、用户、issue 和权限隔离测试数据，使用唯一命名和标签标记生命周期。测试结束自动清理，失败时保留诊断快照。对大数据量场景要预置模板，例如十万 issue、大量自定义字段、复杂 workflow 和多用户组。

### 28. 什么时候使用 UI 自动化，什么时候使用 API 自动化？

**参考答案：**  
UI 自动化适合验证关键用户路径、交互体验和前端状态；API 自动化适合快速覆盖业务规则、权限、数据和边界。Atlassian 场景下，很多数据准备应通过 API 完成，最终用少量 UI 检查用户体验。过多 UI 自动化会导致慢和不稳定。

### 29. 如何设计质量看板？

**参考答案：**  
看板包括 CI 成功率、flaky 率、测试耗时、自动化覆盖、线上缺陷、缺陷逃逸率、接口错误率、搜索延迟、Webhook 成功率、Jira Automation 失败率和客户影响范围。看板要支持按产品、团队、模块、版本和租户规模下钻，并关联 owner 和改进任务。

### 30. 如何建设失败自动归因系统？

**参考答案：**  
采集测试报告、日志、截图、API 请求、CI 环境、代码变更和历史失败模式。通过规则和机器学习/LLM 对失败分类：产品缺陷、测试脚本问题、环境问题、依赖问题、数据问题、权限问题。输出相似失败、可能 owner 和建议操作，缩短定位时间。

## 六、性能、稳定性与可观测性

### 31. 如何对 Jira issue 列表做性能测试？

**参考答案：**  
构造不同项目规模、issue 数量、自定义字段数量、过滤条件、排序和权限组合。指标包括响应时间、P95/P99、数据库查询、搜索索引耗时、前端渲染时间和错误率。还要覆盖大租户、复杂 JQL 和高并发访问。

### 32. 如何测试搜索索引延迟？

**参考答案：**  
创建或更新 issue/page 后，持续查询直到结果可见，记录索引延迟分布。覆盖创建、更新、删除、权限变更和批量导入。指标包括 P50/P95/P99 索引延迟和失败率。权限变更尤其重要，索引延迟不能导致无权限数据被搜索到。

### 33. 如何设计通知系统测试？

**参考答案：**  
覆盖触发条件、订阅设置、权限过滤、邮件/站内/移动端渠道、去重、频控、模板、多语言和退订。异常场景包括通知服务延迟、重复事件、用户权限变化和批量事件风暴。测试断言既要确认该发的发出去，也要确认不该发的不发。

### 34. 如何测试多租户隔离？

**参考答案：**  
构造多个 site、organization、project、user group 和 app installation，验证数据、权限、搜索、缓存、日志、计费和异步任务隔离。重点检查缓存 key、后台任务、错误信息和导出文件是否带租户边界。跨租户泄露是最高优先级缺陷。

### 35. 如果客户反馈 Jira 很慢，你会怎么排查？

**参考答案：**  
先确认范围：单客户、单项目、某页面、某接口还是全局。再查看请求链路、数据库、搜索、缓存、前端渲染、网络和第三方 app。结合租户规模、JQL、字段数量和最近变更定位。测试侧可补充复现数据集、性能基线和回归门禁。

### 36. 如何设计容量测试？

**参考答案：**  
根据真实租户模型构造用户数、项目数、issue 数、字段数、automation 规则数、插件数和 API 调用量。模拟峰值、持续负载、突刺和后台任务。观察吞吐、延迟、错误、资源利用率和降级策略。容量测试结果要转化为扩容阈值和告警。

### 37. 如何测试数据迁移？

**参考答案：**  
覆盖小租户、大租户、复杂权限、自定义字段、附件、评论、历史记录、automation 规则和第三方 app 数据。校验迁移前后数量、字段、权限、链接、搜索和审计日志一致。还要测试失败恢复、断点续传、回滚和迁移期间用户操作。

## 七、AI、Atlassian Intelligence 与智能质量工程

### 38. 如何测试 Atlassian Intelligence 这类 AI 功能？

**参考答案：**  
先定义任务，如总结 issue、生成页面摘要、推荐 action item、辅助搜索。评测维度包括准确性、完整性、引用依据、权限过滤、隐私保护、延迟和用户采纳率。AI 输出不能越权读取用户无权限内容，也不能编造项目事实。

### 39. 如何测试“AI 自动总结 Jira issue”？

**参考答案：**  
构造不同复杂度的 issue，包括长描述、多评论、附件、状态变化和冲突信息。校验总结是否覆盖核心问题、当前状态、owner、阻塞点和下一步行动。安全上要验证权限、敏感字段、内部评论和跨项目引用。评估可用人工黄金集、规则检查和 LLM judge 辅助。

### 40. 如何用 AI 提升测试用例生成？

**参考答案：**  
把需求、用户故事、验收标准、历史缺陷和接口契约输入 LLM，生成候选测试点。然后通过规则去重、风险分级和人工评审筛选，最后转化为自动化脚本或测试任务。效果要用缺陷发现率、评审通过率、用例重复率和维护成本衡量。

### 41. 如何用 LLM 分析 CI 失败日志？

**参考答案：**  
先结构化采集日志、堆栈、测试名称、环境、commit 和历史失败。LLM 用于摘要失败、提取关键错误、匹配相似案例和建议 owner。最终分类要结合规则和历史数据，不应完全依赖模型输出。还要避免把 secrets 和敏感日志发送给不安全模型。

### 42. AI 质量评测中如何避免误判？

**参考答案：**  
使用多层评估：规则校验确定性约束，人工黄金集校准关键样本，LLM judge 处理开放文本，线上反馈验证真实效果。定期抽样复核，监控 judge 与人工一致率。对安全和权限问题，必须使用硬规则和人工确认，而不是只看语义评分。

## 八、编程、SQL 与 Linux

### 43. 编程题：统计每个 Jira 项目的缺陷关闭周期。

**参考答案：**  
遍历 issue 事件或状态变更记录，找到从 created 到 resolved/done 的时间差，按 project 聚合平均值、P50、P95。要处理未关闭、重新打开、时区和异常数据。面试中可先写哈希聚合版本，再讨论如何用 SQL 或数据仓库实现。

### 44. 编程题：如何判断 Webhook event 是否重复？

**参考答案：**  
使用 event id 或 delivery id 做去重键，结合数据库唯一索引或 Redis `SET NX`。处理流程是先校验签名，再插入去重记录，插入成功才处理业务。测试要覆盖并发重复、失败重试、TTL 过期和不同事件对应同一 issue 的场景。

### 45. SQL 题：统计过去 7 天每个项目的 issue 创建量和关闭量。

**参考答案：**  
可以按 project_id 分组，使用条件聚合统计 `created_at` 和 `resolved_at` 在过去 7 天内的数量。需要注意关闭量的分母不是创建量，某些 issue 是过去创建但本周关闭。进一步可以按 issue type、priority 和 assignee 下钻。

### 46. Linux 题：如何定位自动化测试中某个接口偶发 500？

**参考答案：**  
先用 request id 串联测试日志、网关日志、服务日志和数据库日志。用 `grep`、`awk`、`jq` 提取时间窗口、接口、状态码和错误栈。再看是否与部署、流量、依赖超时、数据库慢查询或缓存异常相关。长期方案是结构化日志、tracing 和自动失败归因。

## AI 相关加分点

- 能把 Atlassian Intelligence、Jira Automation、Bitbucket Pipelines 和测试平台结合起来，形成 AI 驱动的质量闭环。
- 能用状态机模型测试复杂 workflow，而不是手写大量重复用例。
- 能用 AI 辅助生成测试点、归因 CI 失败、总结缺陷趋势，但会用规则、数据和人工校准控制风险。
- 能关注 AI 权限问题：模型总结、搜索和推荐不能泄露用户无权限访问的 issue、页面或评论。
- 能用数据衡量 AI 质量收益：测试生成采纳率、失败定位时间下降、flaky 率下降、缺陷逃逸率下降。

## 复习建议

1. 熟悉 Jira 核心概念：project、issue、workflow、field、permission、automation、JQL、notification。
2. 复习 Atlassian Cloud REST API、Webhook、Forge app、Bitbucket Pull Request 和 Pipelines。
3. 准备一个测试平台项目，重点讲环境管理、数据构造、接口自动化、失败归因和质量看板。
4. 复习多租户 SaaS 质量：权限隔离、缓存隔离、搜索权限、异步任务、迁移和性能。
5. 准备 AI 质量案例：AI issue 总结、测试用例生成、CI 日志分析、LLM judge 校准和权限安全。
6. 编程题重点练习哈希表、状态机、TopK、日志解析、去重、滑动窗口和 SQL 条件聚合。
7. 行为面试中突出团队协作、用户同理心、ownership、数据驱动和持续改进。
