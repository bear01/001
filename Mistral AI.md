# Mistral AI 测试开发工程师面试题

## 公司名

Mistral AI

## 岗位方向

测试开发工程师 / SDET / QA Automation Engineer / LLM API、Agent 平台、RAG、模型评测、批处理推理、微调、多模态、企业 AI 治理与质量工程方向

## 资料来源与整理依据

- [Mistral AI Documentation](https://docs.mistral.ai/)
- [Models Overview](https://docs.mistral.ai/models/overview)
- [Agents Introduction](https://docs.mistral.ai/studio-api/agents/introduction)
- [Self-Deployment](https://docs.mistral.ai/models/deployment/local-deployment)
- [RAG Observability with Mistral AI and Phoenix](https://docs.mistral.ai/resources/cookbooks/third_party-phoenix-arize_phoenix_evaluate_rag)
- [Mistral batch API](https://mistral.ai/news/batch-api)
- [Mistral AI Studio](https://mistral.ai/products/studio/)
- [Introducing Mistral AI Studio](https://mistral.ai/news/ai-studio/)
- [Build AI agents with the Mistral Agents API](https://mistral.ai/fr/news/agents-api/)
- 公开候选人面经中常见的 LLM 平台 / AI 应用 / API 自动化 / 测试开发方向：模型 API、流式输出、函数调用、Agent 工具、安全沙箱、RAG 评测、批量推理、微调数据质量、CI/CD、可观测性、性能稳定性和行为面。

> 说明：以下题目不是 Mistral AI 官方题库，而是结合公开资料、AI 平台产品形态和测试开发岗位能力模型整理的高频准备题与模拟题。

## 面试画像

Mistral AI 的核心场景覆盖大语言模型、代码模型、Embedding、Moderation、OCR/多模态、Agents API、Studio、批处理 API、企业部署和自部署。测试开发工程师需要证明自己能把 LLM 的非确定性输出、API 稳定性、模型质量、Agent 安全、RAG 可观测性、成本延迟和企业治理转化为可自动化、可度量、可审计的质量体系。

## 题目分类

### 一、岗位理解与 LLM 平台质量

### 1. 你如何理解 Mistral AI 测试开发工程师的核心价值？

参考答案：核心价值是保障模型 API、Agent、RAG、批处理、微调和企业部署在快速迭代中稳定可信。LLM 平台的缺陷可能不是传统报错，而是响应漂移、工具调用错误、幻觉增加、延迟恶化、成本异常、权限越界或安全策略失效。测试开发要把这些风险转化为自动化回归、评测集、性能基准、安全测试和发布门禁。

### 2. LLM 平台测试和普通后端 API 测试有什么区别？

参考答案：普通 API 多数可以用确定性断言验证字段和状态码；LLM API 还要验证语义质量、随机性、上下文窗口、流式输出、工具调用、内容安全、模型版本和成本延迟。断言方式要结合规则、评测集、统计指标、人工抽检和回归基线，不能只比较字符串。

### 3. Mistral AI 的质量指标可以如何设计？

参考答案：可以分为平台指标和模型指标。平台指标包括可用性、错误率、P95/P99 延迟、首 token 延迟、吞吐、限流命中、批处理成功率和发布回滚成功率；模型指标包括任务准确率、拒答正确率、幻觉率、工具调用成功率、RAG 引用命中率、安全违规率和评测稳定性。

### 4. 如何为 Mistral 的模型版本发布设计质量门禁？

参考答案：门禁应包含 API 合约回归、代表性任务评测、安全评测、长上下文测试、流式输出测试、工具调用测试、性能基准和成本对比。新版本要与旧版本做差异分析，若关键场景准确率下降、拒答异常或延迟明显升高，应阻断发布或扩大灰度观测。

### 5. 面试中如何体现自己适合 Mistral AI？

参考答案：用案例证明三类能力：AI 应用质量能力，例如 RAG、Agent、prompt 安全和模型评测；工程质量能力，例如 API 自动化、CI/CD、性能压测和可观测性；数据能力，例如日志分析、评测集构建、指标监控和异常归因。回答要体现能处理 LLM 的不确定性，而不是只会传统 UI 自动化。

### 二、模型 API、流式输出与兼容性

### 6. 如何测试 Chat Completion 类接口？

参考答案：覆盖必填参数、模型名、messages、temperature、max tokens、stream、tool choice、错误参数、鉴权失败和限流。断言状态码、响应结构、token 用量、错误码、耗时和内容基本语义。对非确定输出可用语义规则和评测器检查，而不是硬编码完整答案。

### 7. 如何测试流式输出？

参考答案：验证首包延迟、chunk 顺序、增量内容拼接、结束标志、异常中断、客户端取消、网络抖动和重试策略。自动化要同时验证服务端资源释放和客户端重连行为，避免请求取消后后端仍持续占用推理资源。

### 8. 如何测试长上下文能力？

参考答案：构造不同长度的上下文、跨段引用、远距离检索、重复信息、无关干扰和超限输入。验证模型是否能引用正确片段、是否丢失早期信息、是否在超限时返回明确错误。指标包括准确率、延迟、token 成本和上下文长度增长后的退化曲线。

### 9. 如何测试多模型兼容性？

参考答案：建立模型矩阵，覆盖通用模型、代码模型、Embedding、Moderation、OCR/多模态模型和不同版本。相同接口要验证参数兼容、错误码一致、能力差异明确、文档示例可运行。对于不支持的能力，应返回清晰错误，而不是静默降级。

### 10. 如何测试 OpenAI 兼容接口或 SDK 兼容性？

参考答案：使用主流 SDK、curl、Python、Node 和常见框架构造兼容性用例，验证请求结构、流式协议、工具调用、错误码和 token 统计。还要覆盖依赖升级、代理、超时、重试和环境变量配置。兼容测试要进入 CI，避免模型平台迭代破坏开发者集成。

### 11. 如何测试限流和配额？

参考答案：构造单用户、团队、API key、模型级和区域级并发场景，验证请求数、token 数、批处理容量和错误返回。重点检查限流是否公平、是否可恢复、是否有 Retry-After 信息，以及高并发下是否误伤正常用户。配额变更也要有审计和即时生效测试。

### 12. 如何验证模型输出格式稳定性？

参考答案：对 JSON、表格、函数调用参数和结构化摘要建立 schema 校验。测试 prompt 应包含边界输入、恶意输入和多语言输入，验证模型是否遵守格式。若输出不稳定，可结合 constrained decoding、重试、解析容错和模型评测指标。

### 三、Agents、工具调用与安全沙箱

### 13. 如何测试 Agents API？

参考答案：覆盖 Agent 创建、配置、指令、会话、工具绑定、调用、状态保存和删除。断言 Agent 能按目标规划步骤、调用正确工具、处理工具异常并返回可追溯结果。还要验证不同 Agent、用户和团队之间的配置隔离。

### 14. 工具调用测试的关键点是什么？

参考答案：关键是验证工具选择是否正确、参数是否符合 schema、调用顺序是否合理、错误是否可恢复、结果是否被正确引用。负向用例包括工具不可用、参数缺失、返回异常、超时、权限不足和恶意 prompt 诱导工具误用。

### 15. 如何测试代码执行沙箱？

参考答案：覆盖正常 Python 计算、依赖限制、超时、内存限制、文件访问、网络访问、恶意代码和输出大小限制。安全断言包括不能越权访问环境变量、密钥、宿主文件或其他用户数据。还要验证沙箱异常时 Agent 能给出安全、明确的错误。

### 16. Agent 幻觉和不可追溯结论怎么测试？

参考答案：构造需要引用工具结果或检索证据的问题，检查回答是否包含来源、是否与工具输出一致、是否编造不存在的数据。可以用事实性评测、引用覆盖率、人工标注和日志回放结合。对高风险任务，应要求 Agent 在证据不足时拒答或请求补充信息。

### 17. 如何测试多 Agent 协作？

参考答案：设计需要分工的任务，例如检索、计算、审核和总结，验证任务拆解、上下文传递、冲突解决和最终结果一致性。还要测试循环调用、重复计划、消息膨胀和权限边界。指标包括成功率、步骤数、总延迟、token 成本和错误恢复率。

### 18. Agent 生产化有哪些质量风险？

参考答案：风险包括工具误用、权限越界、提示注入、无限循环、不可解释操作、成本失控和审计不足。测试应覆盖操作确认、权限最小化、敏感信息脱敏、执行日志、回滚机制和异常停止条件。Agent 可以自动规划，但不能绕过企业治理。

### 四、RAG、Embedding 与评测

### 19. 如何测试 Embedding API？

参考答案：覆盖空文本、长文本、多语言、代码片段、批量输入、重复输入和异常字符。验证向量维度、返回顺序、批处理稳定性、延迟和错误码。质量上可以用相似句召回、反例区分、聚类稳定性和历史基线比较。

### 20. RAG 系统的核心测试点有哪些？

参考答案：核心测试点包括文档切分、Embedding、索引更新、检索召回、重排、上下文拼接、引用生成和答案一致性。要构造有答案、无答案、冲突答案、过期答案和跨文档答案场景。指标包括检索召回率、引用准确率、答案事实性和拒答正确率。

### 21. 如何测试 RAG 的可观测性？

参考答案：每次请求应记录 query、检索文档、得分、prompt、模型版本、输出、引用和评测结果。测试要验证日志完整、敏感信息脱敏、trace 可关联和指标能按应用、用户、知识库、版本下钻。可观测性是定位幻觉和召回失败的基础。

### 22. 如何构建 Mistral 应用的评测集？

参考答案：评测集应覆盖真实业务意图、边界问题、对抗输入、多语言、长上下文、工具调用和无答案场景。每条样本要有期望行为、参考答案、可接受范围和风险等级。评测集要版本化，并与生产日志和历史缺陷持续闭环。

### 23. 如何验证模型评测本身可靠？

参考答案：评测器可能有偏差或不稳定，所以要用人工标注样本校准，检查一致性、重复运行波动和误判案例。对重要指标不能只依赖一个 LLM-as-judge，可以结合规则、统计指标和人工抽检。评测结果应记录模型版本、prompt 和采样参数。

### 24. 如何测试多语言能力？

参考答案：准备中文、英文、法语、德语、西班牙语等样本，覆盖问答、摘要、分类、翻译和安全拒答。验证语义正确、格式一致、术语保留和文化语境。多语言测试还要关注 token 成本、延迟和不同语言下的安全策略一致性。

### 五、批处理、微调与数据质量

### 25. 如何测试 Batch API？

参考答案：覆盖 JSONL 文件格式、上传、任务创建、排队、执行、状态查询、结果下载、部分失败和取消。断言每行请求与输出一一对应，失败项有明确错误，重复提交和断点恢复不会产生重复账单或丢失结果。大批量任务还要监控吞吐、成本和排队时长。

### 26. 批处理和同步 API 的一致性怎么验证？

参考答案：选取一组固定输入，分别走同步接口和批处理接口，比较结构、token 统计、错误处理和语义输出是否在可接受范围内。由于模型输出有随机性，应固定参数或使用语义评估。差异需要按模型版本、参数、队列和执行时间记录。

### 27. 微调数据质量如何测试？

参考答案：验证格式、字段、样本长度、标签一致性、重复样本、敏感信息、低质量样本和训练验证集泄漏。还要检查类别分布、异常值和领域覆盖。数据质量不过关会导致微调效果退化，所以应在训练前建立自动数据门禁。

### 28. 如何测试微调后的模型？

参考答案：用独立评测集比较基础模型和微调模型在目标任务上的准确率、鲁棒性、安全性和延迟。还要验证不会明显损害通用能力或安全拒答能力。发布前要跑回归评测、成本评估和回滚策略，避免只看训练 loss。

### 29. 如何测试 OCR 或文档 AI 场景？

参考答案：准备扫描件、PDF、表格、手写、图片混排、多语言、低清晰度和旋转页面样本。验证文本提取准确率、结构还原、表格解析、页码顺序、异常文件处理和处理耗时。对企业场景要重点检查敏感内容脱敏、文件隔离和结果可追溯。

### 30. 如何避免训练或评测数据污染？

参考答案：对数据集做去重、来源记录、时间切分和相似度检查，避免测试样本进入训练集。对生产日志回流要做脱敏和标注流程控制。评测报告要记录数据版本和 hash，保证不同版本结果可复现。

### 六、安全、治理与企业部署

### 31. 如何测试内容安全和 Moderation 能力？

参考答案：构造暴力、仇恨、自残、隐私、违法、越狱、双关和多语言样本，验证分类、拒答、误杀和漏检。安全测试要看召回和误报平衡，并按风险等级设置阈值。模型升级后必须跑安全回归，防止能力漂移。

### 32. Prompt Injection 如何测试？

参考答案：在用户输入、检索文档、工具返回和系统消息中注入恶意指令，验证模型是否泄露系统提示、越权调用工具或忽略安全策略。RAG 场景要特别测试文档中的间接注入。防护包括指令分层、工具权限、引用隔离和输出审查。

### 33. 企业 AI Studio 的质量重点是什么？

参考答案：企业平台要关注项目、团队、权限、Agent 编排、部署、观测、评测、审计和数据所有权。测试要验证不同部署模式下功能一致、权限隔离、操作可追踪和治理策略生效。对低代码工作流还要测试版本管理、回滚和协作冲突。

### 34. 自部署模型怎么测试？

参考答案：覆盖 vLLM、TGI、TensorRT-LLM 等推理引擎，验证模型加载、API 兼容、显存占用、并发、量化、批处理和监控。自部署还要测试安装文档、镜像版本、GPU 驱动、网络限制和安全配置。目标是确保客户在自己基础设施上也能复现稳定能力。

### 35. 如何测试敏感数据保护？

参考答案：验证传输加密、日志脱敏、数据保留策略、访问控制、审计日志和密钥管理。构造含 PII、密钥、合同内容和客户数据的样本，检查是否被错误记录或返回。企业场景还要验证数据隔离和删除请求能被执行。

### 36. 如何验证审计日志质量？

参考答案：每个关键操作都应记录主体、时间、资源、动作、结果、请求 id 和来源。测试覆盖成功、失败、权限拒绝、配置变更、API key 创建删除、Agent 工具调用和模型部署。审计日志必须不可被普通用户篡改，并支持检索和导出。

### 七、CI/CD、性能与稳定性

### 37. 如何设计 Mistral API 的自动化测试框架？

参考答案：框架要支持多模型、多环境、多语言 SDK、鉴权、并发、流式协议、结构化断言和语义评测。测试数据、prompt、模型版本和阈值要配置化。报告应输出错误率、延迟、token、成本、评测分和失败样例，便于开发定位。

### 38. 如何控制 LLM 测试成本？

参考答案：分层执行：PR 阶段跑 mock、契约和少量 smoke；每日跑代表性模型评测；发布前跑完整性能、安全和回归集。对昂贵长上下文和多模型测试要按变更风险触发。还可以缓存确定性结果、限制 token 和自动清理测试资源。

### 39. 如何做 LLM API 性能压测？

参考答案：定义真实流量模型，包括 prompt 长度、输出长度、并发、stream、模型选择和地域。指标包括首 token 延迟、总延迟、吞吐、错误率、限流、排队时长和 token/s。压测报告要区分模型计算瓶颈、网关瓶颈和客户端瓶颈。

### 40. 如何处理 LLM 测试中的 flaky？

参考答案：先区分模型随机性、环境波动、外部依赖和测试断言问题。对语义输出使用统计阈值和多次运行，对接口稳定性使用明确的协议断言。flaky 测试要记录随机种子、参数、模型版本和原始响应，不能简单用无限重试掩盖问题。

### 41. 如何把 AI 辅助能力引入测试开发流程？

参考答案：可以用 AI 从需求生成测试点、从失败日志归因、生成 prompt 变体、总结评测报告和识别高风险变更。关键是所有 AI 建议都要可追溯、可审核、可复现，不能让模型直接决定发布。对安全和隐私相关内容要有脱敏和权限控制。

### 42. 线上事故后如何沉淀回归用例？

参考答案：先收集请求、模型版本、prompt、工具调用、检索文档、指标和日志，复现根因。然后将事故样本脱敏后加入评测集或自动化回归，并补充监控和发布门禁。复盘目标是减少同类问题再次发生，而不是只修复单个案例。

### 八、编程、数据与行为面模拟题

### 43. 给你一批 API 日志，如何找出模型延迟异常的原因？

参考答案：按模型、区域、租户、请求长度、输出长度、stream、错误码和时间窗口聚合，计算 P50、P95、P99 和 token/s。再关联发布版本、限流、队列、网关指标和模型服务指标。若长 prompt 请求集中导致 P99 升高，应进一步拆分上下文长度和并发。

### 44. 如何设计脚本校验 Batch API 输出完整性？

参考答案：脚本读取输入 JSONL 的 request id，解析输出文件，验证行数、id 对齐、状态、错误详情和响应 schema。对缺失、重复、乱序和部分失败要输出清单。脚本还应统计成功率、平均延迟、token 用量和可重试错误，作为 CI 报告附件。

### 45. 如果 Agent 错误调用了高风险工具，你如何排查？

参考答案：先查看系统指令、用户输入、工具 schema、权限配置、调用日志和模型中间步骤，确认是 prompt 注入、工具描述歧义还是权限控制缺失。修复上可以收紧工具权限、增加操作确认、改进工具描述、加入策略校验和补充对抗测试。

### 46. 讲一次你推动 AI 应用质量体系落地的经历。

参考答案：可以按 STAR 回答：背景是团队要上线 RAG 或 Agent 应用；任务是建立可衡量质量标准；行动包括构建评测集、自动化回归、性能压测、安全测试、可观测性和发布门禁；结果是上线前发现关键问题，降低事故率并缩短排查时间。重点体现你能把 AI 不确定性工程化。

## AI 相关加分点

- 能解释 LLM 输出不可完全确定时，如何用评测集、统计指标、规则断言和人工抽检组合保障质量。
- 熟悉 Agent 工具调用、安全沙箱、Prompt Injection、RAG 评测、Embedding 质量和批处理推理。
- 能设计模型版本发布门禁，覆盖准确率、安全性、延迟、成本、可用性和回滚。
- 能用日志和指标定位 LLM 生产问题，例如首 token 延迟、P99、token/s、工具调用失败和检索召回不足。
- 能把 AI 辅助测试用于生成用例、分析失败和总结报告，同时强调事实校验、权限隔离和审计。
- 有多语言、多模态、OCR、代码模型或企业 AI 治理经验，会明显加分。

## 复习建议

- 复习 LLM API 基础：chat、stream、tools、function calling、embedding、moderation、batch、fine-tuning 和模型版本。
- 练习设计 RAG 评测集：有答案、无答案、冲突答案、过期知识、引用错误和 Prompt Injection。
- 准备性能压测方案：首 token 延迟、P95/P99、吞吐、token/s、并发、限流和成本。
- 准备安全测试案例：越狱、间接注入、敏感信息泄露、工具误用和代码沙箱逃逸。
- 准备一个质量平台项目：自动化框架、评测报告、CI/CD 门禁、可观测性和事故回归。
- 面试回答中尽量量化：准确率提升、误报降低、P99 改善、事故减少、回归耗时缩短和成本下降。
