# DeepSeek 测试开发工程师面试题

## 公司名

DeepSeek

## 岗位方向

测试开发工程师 / SDET / 质量工程方向，重点面向大模型 API、推理服务、Agent 工具调用、结构化输出、上下文缓存、性能稳定性、数据评测平台、CI/CD 与 AI 辅助测试。

## 资料来源与整理依据

- [DeepSeek API Docs](https://api-docs.deepseek.com/)
- [Create Chat Completion](https://api-docs.deepseek.com/api/create-chat-completion)
- [Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode)
- [Tool Calls](https://api-docs.deepseek.com/guides/tool_calls)
- [JSON Output](https://api-docs.deepseek.com/guides/json_mode)
- [Context Caching](https://api-docs.deepseek.com/guides/kv_cache)
- [Token & Token Usage](https://api-docs.deepseek.com/quick_start/token_usage)
- [Rate Limit & Isolation](https://api-docs.deepseek.com/quick_start/rate_limit)
- [Error Codes](https://api-docs.deepseek.com/quick_start/error_codes)
- [Change Log](https://api-docs.deepseek.com/updates)

## 面试画像

DeepSeek 的测试开发岗位会更关注候选人是否能把传统质量工程能力迁移到大模型平台：不仅要会自动化、接口测试、性能压测和 CI/CD，还要理解推理模型、上下文、结构化输出、工具调用、成本、限流、缓存命中、评测集、线上观测与安全治理。回答时建议始终围绕“质量风险识别 -> 可测指标 -> 自动化方案 -> 数据闭环 -> 线上监控”展开。

## 题目分类

- 岗位理解与大模型平台质量
- Chat Completion、推理模式与 API 兼容
- Function Calling、JSON Output 与应用集成
- Context Caching、批量评测与数据质量
- 性能稳定性、成本与可观测性
- 安全、合规与企业治理
- CI/CD、测试平台与 AI 辅助测试
- 编程、数据与行为面模拟题

## 一、岗位理解与大模型平台质量

### 1. 你如何理解 DeepSeek 测试开发工程师和传统后端测试开发的差异？

参考答案：传统后端测试更偏接口契约、数据库一致性、并发、可用性和发布质量；DeepSeek 这类大模型平台还要测试概率性输出、推理链路、上下文窗口、Token 成本、模型版本变化、内容安全、工具调用、结构化输出和在线评测。测试开发不能只判断“接口是否返回 200”，还要建立语义正确率、格式合规率、幻觉率、拒答误伤率、工具调用成功率、延迟分位数、缓存命中率、单位请求成本等指标，并把离线评测、灰度监控和线上反馈打通。

### 2. 如果让你负责 DeepSeek API 的端到端质量，你会先建立哪些测试层级？

参考答案：我会分五层：第一层是协议与参数校验，覆盖鉴权、必填字段、非法参数、错误码；第二层是模型能力评测，覆盖通用问答、推理、代码、中文场景、长上下文；第三层是功能特性测试，覆盖流式输出、Thinking 模式、JSON Output、Tool Calls、Context Caching、Anthropic/OpenAI 兼容；第四层是非功能测试，覆盖吞吐、延迟、限流、降级、稳定性、成本；第五层是线上质量闭环，包含灰度、A/B、报警、人工抽检、用户反馈回流和回归集自动更新。

### 3. 大模型 API 的“正确性”很难定义，你会怎样设计可执行的质量标准？

参考答案：我会把正确性拆成可测维度：确定性任务用精确匹配或结构化断言，例如 JSON Schema、代码单测、数学最终答案；开放问答用 rubric 评分、事实核查、引用一致性和人工抽样；安全场景用策略命中率、误拒率、漏拦率；Agent 场景用任务完成率、工具调用参数正确率和副作用控制。为了降低评测波动，评测集要分层采样，固定模型参数，记录 seed 或请求上下文，并持续对评分器做一致性校准。

### 4. 面对模型版本升级，你会如何做回归测试？

参考答案：我会建立版本对比评测流水线。先维护稳定的黄金集、困难样本集、线上高频样本集和安全红队集；每次升级并行请求新旧模型，比较准确率、拒答率、格式合规率、平均 Token、P95/P99 延迟、错误率、工具调用成功率和成本。对明显退化的样本进入人工复核，再按业务重要性决定阻断发布、灰度放量或带风险上线。上线后继续用 shadow traffic 和线上指标确认没有隐性回归。

### 5. 如果业务说“模型效果变差了”，你会如何定位？

参考答案：我会先确认变化范围：是全部请求还是某个场景、某个模型、某个区域、某类用户、某个 prompt 版本。然后拉取请求样本，比较输入长度、系统提示词、模型参数、工具配置、缓存命中、错误码和响应分布。接着用历史基线重放样本，判断是模型版本、Prompt、数据、网关、限流、上游工具还是评测口径变化。最后把问题归因到可复现 case，并补充到回归集和监控规则。

### 6. 你会怎样设计 DeepSeek 这类模型服务的质量指标体系？

参考答案：核心指标包括可用性、错误率、首 Token 延迟、完整响应延迟、吞吐、限流命中、超时率、重试成功率、Token 使用量、缓存命中率、输出截断率、结构化输出合规率、工具调用成功率、安全拦截准确率和用户满意度。指标要按模型、接口、用户类型、场景、地区、SDK 版本和发布批次切分，避免平均值掩盖局部问题。

## 二、Chat Completion、推理模式与 API 兼容

### 7. 如何测试 Chat Completion 接口的基础契约？

参考答案：我会覆盖正常请求、最小请求、复杂多轮请求、空 messages、非法 role、缺少 model、超长 content、非法 response_format、非法 tools、stream 与非 stream 两种响应。断言包括 HTTP 状态码、错误码、响应 JSON 字段、choices、finish_reason、usage、message.role、content 类型、幂等性预期和错误信息可诊断性。还要检查 SDK 兼容路径，例如 OpenAI SDK 修改 base_url 后能否稳定调用。

### 8. DeepSeek 支持 OpenAI 兼容格式，你会重点测哪些兼容风险？

参考答案：要测 SDK 初始化、base_url、api_key、模型名、messages 格式、stream、tools、response_format、max_tokens、temperature、top_p、stop、错误码映射和异常处理。兼容测试不能只跑一个 curl 示例，而要覆盖 Python、Node.js、Java 等常用 SDK，以及历史版本 SDK。还要确认不支持或已废弃参数的处理方式是忽略、报错还是透传，避免客户迁移时出现静默失败。

### 9. 如果 DeepSeek 同时提供 OpenAI 与 Anthropic 风格接口，你会怎样设计测试矩阵？

参考答案：我会按协议维度拆分：OpenAI Chat Completions 和 Anthropic Messages 分别建立契约测试；再按能力维度覆盖非思考模式、思考模式、流式输出、工具调用、停止词、温度、system prompt、metadata/user_id 等字段。关键是验证同一业务意图在两种协议下的语义等价性，以及不支持字段是否有明确策略。最后用同一套评测样本做跨协议对照，发现映射差异。

### 10. 如何测试流式输出的稳定性？

参考答案：流式测试要覆盖 SSE 分块顺序、首包延迟、增量内容拼接、usage 统计、客户端中断、网络抖动、服务端超时、重复 chunk、缺失结束标记、长回答和并发流。自动化脚本应模拟慢消费者、断连重连和不同 SDK 的解析行为。断言既要验证最终拼接内容，也要验证每个 chunk 的 schema、finish_reason、错误传播和资源释放。

### 11. 如何测试 Thinking / reasoning 模式？

参考答案：要测试思考模式开关、推理输出字段、最终答案字段、max_tokens 对推理与最终答案的影响、多轮对话中是否错误拼接历史推理内容、流式输出中的增量顺序，以及不支持参数的处理。能力评测上要准备数学、代码、逻辑、长链路任务，比较思考模式和非思考模式在准确率、延迟和 Token 成本上的差异，避免只追求准确率而忽视成本和响应时间。

### 12. 如果官方文档提示某些旧模型名将在指定日期废弃，你会如何保障兼容迁移？

参考答案：我会建立模型名兼容测试和迁移预警。测试层面验证旧模型名到新模型能力的映射、日志告警、文档提示、SDK 示例和客户侧配置；平台层面提供用量统计，识别仍在使用旧模型名的客户；发布层面在废弃日期前设置灰度提醒、迁移指南和回滚预案。自动化中要加入“废弃日期前后”的配置测试，避免到期后出现大面积不可用。

### 13. 怎样测试 max_tokens、上下文长度和输出截断？

参考答案：我会构造短输入、接近窗口上限输入、超长输入、长输出和极小 max_tokens 场景。断言包括是否按预期返回 length 类 finish_reason、是否有清晰错误、usage 是否准确、JSON 输出被截断时是否能被检测、流式输出是否完整结束。对业务应用，还要验证客户端能识别截断并触发重试、续写或用户提示。

### 14. 多轮对话测试有哪些容易遗漏的点？

参考答案：容易遗漏 role 顺序、历史 assistant 内容、工具返回内容、系统提示词覆盖、长历史压缩、敏感信息残留、上下文缓存命中和多轮中的安全策略漂移。我会设计跨轮追问、纠错、引用前文、工具调用后继续推理、切换语言和越狱注入等样本，验证模型是否保持上下文一致，同时不会把不该进入上下文的中间推理或敏感字段带到下一轮。

## 三、Function Calling、JSON Output 与应用集成

### 15. 如何测试 JSON Output 功能？

参考答案：要同时测试 response_format 参数和 prompt 约束。用例包括简单对象、嵌套对象、数组、中文字段、特殊字符、空值、极长字段、非法 schema 预期、低 max_tokens 截断和偶发空内容。断言不是“看起来像 JSON”，而是用解析器解析、校验 schema、校验必填字段和业务类型，并记录格式合规率。失败时要能区分模型没按格式、响应被截断、客户端解析错误还是 prompt 不充分。

### 16. JSON Output 仍可能出现空内容或截断，你会如何让业务更稳？

参考答案：客户端要做三层防护：第一层是请求时明确写出 JSON 要求和示例，设置合理 max_tokens；第二层是响应后做 JSON parse 和 schema 校验；第三层是失败时根据原因选择重试、降级到普通文本、触发修复模型或返回可诊断错误。测试要覆盖这些降级路径，并把失败样本进入 prompt 和评测集优化。

### 17. 如何测试 Function Calling / Tool Calls？

参考答案：测试重点是工具选择、参数生成、schema 合规、tool_call_id 关联、工具返回后的二次回答和异常路径。用例包括无需调用工具、必须调用工具、多个候选工具、参数缺失、参数类型错误、工具执行失败、工具超时、工具返回恶意内容、并发工具调用和重复调用。断言包括工具名、参数 JSON、调用次数、业务幂等、最终回答是否正确引用工具结果。

### 18. 如果 Tool Calls 支持 strict schema，你会怎么测？

参考答案：我会准备严格 schema、宽松 schema、枚举值、嵌套对象、数组、oneOf 类结构和边界长度，验证 strict 模式下输出是否完全符合 JSON Schema。还要测试模型在信息不足时是否主动追问，而不是编造参数。负向用例要验证 schema 写错、字段名非法、必填字段缺失时的报错或降级策略。

### 19. 如何防止模型工具调用造成副作用事故？

参考答案：工具层要做权限隔离、参数白名单、审批流、幂等键、dry-run、限额和审计日志。测试时要模拟删除资源、转账、发消息、修改配置等高风险操作，确认模型不能绕过权限，不能把用户自然语言里的恶意指令直接传给工具。对于生产环境，建议先把高风险工具放到“建议模式”，由业务系统确认后再执行。

### 20. 如何测试模型输出对下游系统的可解析性？

参考答案：把下游解析规则前移到测试中，例如 JSON Schema、protobuf、SQL 参数化、枚举映射、时间格式、金额精度和多语言字符集。每个模型能力样本都要通过“模型输出 -> 解析器 -> 业务校验 -> 存储/消息队列”的链路测试。对解析失败要记录原始输入、模型输出、解析错误和修复策略，形成可回放样本。

### 21. 如何为 Agent 应用设计测试用例？

参考答案：Agent 测试要覆盖目标拆解、计划生成、工具选择、工具参数、观察结果利用、失败恢复、循环退出和最终验收。测试集可以分为单工具任务、多工具任务、长链路任务、信息不足任务、工具结果冲突任务和恶意工具结果任务。指标包括任务成功率、平均步数、无效调用率、成本、耗时、错误恢复率和安全违规率。

## 四、Context Caching、批量评测与数据质量

### 22. Context Caching 的质量测试重点是什么？

参考答案：重点验证缓存命中条件、缓存隔离、usage 字段、延迟收益和隐私边界。用例包括相同前缀、多轮对话前缀、长文档复用、轻微变更、不同 user_id、不同系统提示词和缓存过期。断言包括 prompt_cache_hit_tokens、prompt_cache_miss_tokens、响应语义一致性、命中后延迟下降，以及不该命中的请求不会错误复用缓存。

### 23. 如果缓存是默认开启的，你如何排查“缓存命中率低”？

参考答案：先看请求前缀是否稳定，系统提示词、文档内容、空格、时间戳、trace id 等动态字段都会破坏前缀复用。再看请求间隔、用户隔离、上下文拼接策略和缓存持久化时机。测试上可以构造固定前缀与动态前缀对照组，验证命中率、延迟和 usage 字段是否符合预期，并给业务输出 prompt 模板规范。

### 24. user_id 在大模型 API 质量中有什么作用？你会怎么测？

参考答案：user_id 可用于业务侧用户隔离、内容安全隔离、缓存隔离和调度隔离。测试要覆盖同一账号下不同 user_id 的请求是否互不污染、限流是否按预期计算、日志和审计是否可按 user_id 追踪，同时不能把手机号、身份证等隐私数据直接放入 user_id。负向用例包括超长 user_id、非法字符、空值和高并发热点用户。

### 25. 如何设计大规模离线评测平台？

参考答案：离线评测平台应包含样本管理、标签与版本、批量调度、模型路由、评分器、人工复核、统计分析和报告生成。样本要按场景、难度、语言、风险等级分层；调度要支持重试、限流、成本预算和断点续跑；评分要结合规则、模型裁判、单测和人工抽检。每次模型或 prompt 变更都生成可对比报告，显示提升、退化和置信区间。

### 26. 如何保证评测数据本身的质量？

参考答案：评测数据要有来源、标签人、更新时间、适用场景和预期答案。需要去重、脱敏、难度分层、污染检测、标签一致性检查和周期性复审。对于开放题，rubric 要明确优秀、合格、不合格的判断标准；对于代码题，要配套单元测试；对于安全题，要包含正负样本，避免只测攻击样本导致误拒率不可控。

### 27. 如果要做批量任务处理，你会关注哪些测试点？

参考答案：关注任务提交、任务排队、并发控制、部分失败、重试、结果顺序、幂等、成本预算、超时取消和结果导出。对于模型批量调用，还要关注限流退避、长尾请求、不同模型混跑、错误样本回放和任务级监控。自动化要模拟 1 条、1 万条、混合长度、异常输入和中途服务重启等场景。

## 五、性能稳定性、成本与可观测性

### 28. 如何设计 DeepSeek API 的性能压测？

参考答案：先定义目标场景：短问答、长上下文、推理模式、流式输出、工具调用、JSON 输出和缓存命中。指标包括 QPS、并发、首 Token 延迟、总延迟、P95/P99、错误率、429 比例、Token 吞吐、CPU/GPU 资源、队列等待和成本。压测要分阶梯加压、稳定性浸泡、突刺流量和混合模型流量，并用生产相似的输入长度分布。

### 29. 如何测试限流和退避策略？

参考答案：构造超过并发或速率限制的请求，验证是否返回明确的 429 或对应错误，客户端是否按指数退避、抖动和最大重试次数处理。还要测试不同 API key、不同 user_id、不同模型和不同租户之间的隔离。业务侧要避免“所有客户端同时重试”造成二次冲击，测试时可以模拟重试风暴。

### 30. DeepSeek API 返回 500、503、429 时，客户端应该如何处理？

参考答案：429 通常按限流处理，降低并发、排队或指数退避；500 和 503 按短暂服务异常处理，可有限重试并切换降级模型或提示用户稍后重试；400、401、402、422 一般属于请求、鉴权、余额或参数问题，不应盲目重试。测试要验证错误分类、重试次数、日志告警、用户提示和幂等保护。

### 31. 如何测试 Token 使用量和成本统计？

参考答案：用短文本、中文、英文、数字符号、长文本、流式和非流式样本验证 usage 字段是否稳定返回。成本侧要按模型、输入 Token、输出 Token、缓存命中 Token、重试 Token 和失败请求分类统计。测试还要覆盖日志采集丢失、流式最后一个 chunk 缺失、JSON 截断重试导致成本翻倍等场景。

### 32. 你会为模型服务建立哪些线上告警？

参考答案：基础告警包括可用性、5xx、429、超时、P95/P99 延迟、队列长度、流式中断率和错误码突增。模型质量告警包括 JSON 合规率下降、工具调用失败率上升、安全拒答率异常、平均输出 Token 异常、缓存命中率异常和用户负反馈突增。告警要按模型、接口、客户、场景维度聚合，并配套最近变更、样本链接和回放入口。

### 33. 如何定位 P99 延迟突然升高？

参考答案：先切分模型、输入长度、输出长度、stream、thinking、缓存命中、地区、客户和版本，判断是整体资源瓶颈还是局部场景。再看队列等待、上游网关、模型推理、工具调用、网络传输和客户端消费速度。对于流式接口，要单独看首 Token 和完整响应延迟，避免总延迟被长输出误判为服务异常。

### 34. 如何设计稳定性浸泡测试？

参考答案：选择接近生产的混合流量，持续运行 24 小时或更久，覆盖短请求、长上下文、思考模式、流式、工具调用和错误请求。监控内存、连接数、队列、错误率、延迟、Token 吞吐、缓存命中和日志量。浸泡测试不只看是否崩溃，还要看延迟是否缓慢劣化、错误是否聚集、缓存是否异常增长、日志是否导致磁盘压力。

## 六、安全、合规与企业治理

### 35. 如何测试 API key 安全？

参考答案：要覆盖无 key、错误 key、过期 key、权限不足、泄露 key、日志脱敏和多 key 隔离。自动化应检查请求日志、错误日志、trace、告警、前端页面和 crash dump 中是否出现明文 key。平台层要支持 key 轮换、最小权限、调用审计和异常用量告警。

### 36. 如何测试 prompt injection 对 Tool Calls 的影响？

参考答案：构造用户输入、网页内容、工具返回内容中的恶意指令，例如要求忽略系统提示、泄露密钥、调用高风险工具或伪造工具结果。验证模型是否把不可信内容当作数据处理，工具层是否有权限校验和参数白名单。通过红队样本评估攻击成功率，并把成功样本加入回归集。

### 37. 大模型应用日志如何兼顾排障和隐私？

参考答案：日志要记录 request id、模型、参数、延迟、错误码、usage、工具调用摘要和评测标签，但对用户输入、输出、API key、手机号、邮箱、证件号等敏感信息做脱敏或加密。测试要验证脱敏规则、采样策略、访问权限、数据保留周期和审计。排障时可以通过受控工单查看必要明文，避免默认全量暴露。

### 38. 如何测试内容安全策略的误伤和漏拦？

参考答案：建立安全正负样本集，包括违法违规请求、正常敏感讨论、教育科普、新闻引用、代码安全测试和多语言变体。指标同时看拦截率和误拦率，不能只追求高拦截。自动化要按策略版本生成报告，人工复核边界样本，并监控线上用户申诉和负反馈。

### 39. 企业客户接入模型 API 时，你会关注哪些合规测试？

参考答案：关注数据隔离、访问控制、审计日志、数据脱敏、区域与存储策略、模型输出可追溯、删除请求、权限审批和供应商风险。测试场景包括多租户隔离、越权访问、日志导出、数据保留到期、账号关闭后访问和异常调用告警。合规测试要和法务、安全、业务 SLA 一起定义验收标准。

## 七、CI/CD、测试平台与 AI 辅助测试

### 40. 如何把大模型评测接入 CI/CD？

参考答案：CI 中跑快速冒烟集，覆盖协议、JSON、工具调用和核心黄金样本；夜间跑完整评测集和性能基准；发布前跑新旧版本对比和安全红队集。CI 不应被少量概率波动频繁阻断，可以采用多次采样、置信区间和严重问题阻断策略。评测报告要能追踪模型版本、prompt 版本、样本版本和代码 commit。

### 41. AI 辅助测试在 DeepSeek 类岗位中可以怎么落地？

参考答案：可以用 LLM 辅助生成边界用例、变异 prompt、接口 mock、测试数据、日志摘要和失败归因；也可以用模型裁判做开放题初筛。但必须有人类规则兜底：关键断言用代码、schema、单测和统计指标验证，模型生成的用例要去重、审查和沉淀。AI 辅助测试的价值是提高覆盖率和分析效率，不是替代质量标准。

### 42. 如何设计一个模型输出自动评测平台？

参考答案：平台包括样本库、任务编排、模型网关、评分器、人工复核、报告和样本回流。评分器支持规则匹配、JSON Schema、代码执行、检索核查、模型裁判和人工标签。平台要支持多模型对比、版本对比、置信区间、失败样本聚类和趋势图。权限上要区分样本编辑、任务运行、结果查看和生产数据访问。

### 43. 遇到大模型测试的 flaky，你怎么处理？

参考答案：先区分模型随机性、网络不稳定、评测器不稳定、测试数据不稳定和环境资源波动。对于随机性，固定温度或多次采样取统计结果；对于开放题，用 rubric 和人工校准；对于性能测试，隔离环境并增加样本量。不要简单重跑掩盖问题，而要把 flaky 原因分类并设置不同的稳定性策略。

### 44. 如何做灰度发布和回滚验证？

参考答案：灰度前先完成离线评测和 shadow traffic；灰度中按客户、流量、场景逐步放量，监控错误率、延迟、成本、质量指标和用户反馈；出现退化时能按模型、prompt、路由策略或网关配置快速回滚。测试要提前演练回滚，验证缓存、会话、长连接和 SDK 客户端不会因为切换而异常。

## 八、编程、数据与行为面模拟题

### 45. 编程题：请设计一个接口客户端的重试策略，你会怎么写？

参考答案：我会先分类错误：400、401、402、422 不重试；429、500、503 可重试；网络超时可重试但要设置总超时。策略采用指数退避加随机抖动，限制最大次数和最大等待时间，并为有副作用请求设置幂等键。实现时输出结构化日志，记录 request_id、attempt、error_code、sleep_ms 和最终结果。测试覆盖成功、立即失败、重试后成功、重试耗尽、并发重试风暴和取消请求。

### 46. 行为面：如果研发认为模型质量问题“不可测”，你会如何推动？

参考答案：我会先承认开放式输出确实不能完全用传统断言覆盖，然后把问题拆成可测部分：格式、事实、任务完成、安全、成本、延迟和用户反馈。接着用少量高价值样本建立基线，让研发看到评测能发现真实退化，再逐步扩展到自动化平台。沟通上避免争论“能不能测”，而是用数据说明“哪些风险已经可量化，哪些风险需要人工复核”。

## AI 相关加分点

- 能把模型质量拆成准确率、格式合规率、工具调用成功率、幻觉率、安全误拒率、Token 成本、延迟和缓存命中率。
- 熟悉 OpenAI/Anthropic 兼容 API、流式输出、Thinking 模式、JSON Output、Tool Calls、Context Caching 和 user_id 隔离。
- 会设计离线评测平台、黄金集、红队集、模型对比报告和线上反馈回流。
- 能用 LLM 辅助生成测试用例、分析日志和聚类失败样本，但关键断言仍用规则、schema、单测和人工抽检兜底。
- 对性能稳定性有工程意识：P95/P99、首 Token 延迟、限流退避、重试风暴、成本预算、灰度回滚。
- 对安全有边界感：API key 脱敏、prompt injection、防工具副作用、日志隐私、多租户隔离。

## 复习建议

1. 熟悉 Chat Completion 基础字段、流式响应、错误码、usage 和常见 SDK 调用方式。
2. 准备 2 到 3 个大模型测试项目案例，最好覆盖评测集、自动化平台、性能压测和线上监控。
3. 练习把开放问题转成可测指标，例如“模型回答不好”如何拆成事实性、完整性、格式、安全和成本。
4. 重点复习 JSON Schema、接口自动化、pytest、压测、日志分析、SQL、CI/CD 和灰度发布。
5. 准备 Agent / Tool Calls 的测试思路，强调权限、幂等、参数校验和恶意工具返回。
6. 面试回答时尽量给出“测试策略 + 指标 + 自动化实现 + 线上闭环”，避免只罗列工具名。
