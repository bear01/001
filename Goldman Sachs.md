# Goldman Sachs 测试开发工程师面试题

## 公司名

Goldman Sachs

## 岗位方向

测试开发工程师 / SDET / QA Automation Engineer / 金融科技质量工程方向

## 资料来源与整理依据

- [Goldman Sachs Engineering Careers](https://www.goldmansachs.com/careers/our-firm/engineering)
- [Goldman Sachs Marquee](https://www.goldmansachs.com/our-firm/history/moments/2014-marquee)
- [Coding the Digital Transformation of Global Markets](https://www.goldmansachs.com/careers/blog/possibilities-marquee-a/)
- [Goldman Sachs Developer - API Standards](https://developer.gs.com/docs/services/ria-custody/api-standards/)
- [Goldman Sachs API Developer Portal](https://developer.xs2a.goldman.com/index.php/get-started)
- [GS Quant 文档](https://developer.gs.com/docs/gsquant/data/data-analytics/datagrid/overview/)
- 公开候选人面经中常见的 Goldman Sachs 工程面试方向：HackerRank/CoderPad、数据结构与算法、系统设计、Java/Python、SDLC、测试自动化、行为面。

> 说明：以下题目不是 Goldman Sachs 官方题库，而是结合公开资料、金融科技业务和测试开发岗位能力模型整理的高频准备题与模拟题。

## 面试画像

Goldman Sachs 的工程岗位通常围绕全球市场、Marquee、数据与分析平台、低延迟基础设施、网络安全、机器学习、开发者工具、交易风控和客户端数字化产品展开。测试开发工程师需要证明自己能在金融级系统中写代码、搭框架、做自动化门禁、验证数据质量、排查分布式故障，并理解权限、审计、低延迟、市场数据、模型风险和 AI 辅助测试的边界。

## 题目分类

### 一、岗位理解与金融科技质量

### 1. 你如何理解 Goldman Sachs 测试开发工程师的核心价值？

参考答案：核心价值是通过工程化质量体系保障金融平台的可靠、安全和可审计。Goldman Sachs 的工程系统可能服务交易、风险、市场数据、研究分析、客户 API 和内部平台，缺陷可能影响客户决策、交易执行和监管合规。测试开发不仅执行测试，还要参与需求评审、代码评审、自动化框架、数据校验、CI/CD 门禁、监控告警和故障复盘。

### 2. 金融市场系统测试和普通互联网系统测试有什么差异？

参考答案：金融市场系统更强调低延迟、准确性、权限隔离、时间一致性、审计追踪和数据血缘。普通互联网系统可以更多关注体验和流量转化，而金融系统还要验证价格、风险、订单状态、市场日历、时区、汇率、交易权限和监管记录。测试策略要包含回放测试、对账、延迟分布、故障注入和审计日志检查。

### 3. Marquee 这类面向机构客户的金融平台，测试重点是什么？

参考答案：重点包括市场数据准确性、分析结果一致性、交易或执行流程稳定性、权限控制、API 集成、实时刷新、移动/桌面/网页体验和可观测性。对于机构客户，平台还需要支持大规模数据查询、跨资产分析、风险视图和研究内容访问。测试不能只看 UI 是否可用，还要验证底层数据、模型和 API 契约。

### 4. 如果客户反馈某个风险分析结果和预期不一致，你会如何排查？

参考答案：先确认输入数据、模型版本、参数、市场数据时间点和用户权限是否一致。然后比较不同环境或不同计算路径的结果，查看数据源、缓存、计算日志和 trace。测试侧应补充 golden dataset、模型版本断言、数据快照、误差阈值和回归用例。金融分析结果通常需要可追溯，不能只用“本地无法复现”结束。

### 5. 如何测试低延迟交易或报价系统？

参考答案：要测端到端延迟、抖动、吞吐、P99、丢包、排队、序列号和故障恢复。测试场景包括正常流量、开盘高峰、突发市场事件、下游慢响应、网络抖动和批量重连。除了功能正确，还要验证系统在高压下是否按策略降级、限流或拒绝，且不会产生错误交易状态。

### 6. 面试中如何体现你对金融业务的学习能力？

参考答案：可以讲自己如何把复杂业务规则转成可测试模型，例如订单状态机、风险计算、账户权限、交易日历或数据对账。重点说明你会主动阅读文档、和业务确认边界、建立样本数据、设计自动化断言，并在故障复盘后补充回归。面试官更看重可学习和可落地，而不是背诵术语。

### 二、API、自动化与开发者平台

### 7. 如何测试 Goldman Sachs Developer API 的兼容性？

参考答案：先识别 API 标准中哪些变更属于向后兼容，哪些是 breaking change。测试要覆盖新增字段、可选参数、类型变化、必填参数、错误码和版本策略。自动化可以做 schema diff、契约测试和 consumer-driven contract，确保现有客户端不会因服务端变更而失败。

### 8. 如何测试 PSD2/Open Banking 类 API？

参考答案：覆盖第三方提供商身份、证书、用户授权、SCA、账户列表、余额、交易明细、consent 删除和错误场景。测试重点是权限范围、token 过期、授权撤销、证书异常、沙箱与生产契约一致性和敏感数据脱敏。还要验证审计日志记录了谁在什么时间访问了哪些账户数据。

### 9. 如何设计 REST API 自动化框架？

参考答案：框架应包含环境配置、鉴权封装、请求 client、测试数据、schema 校验、业务断言、报告、日志、并发执行和 CI 集成。金融 API 还要支持证书管理、敏感信息脱敏、trace id、权限模型、回放数据和审计字段断言。用例层应表达业务意图，底层 HTTP 细节应封装。

### 10. 如何测试 API 鉴权和证书？

参考答案：覆盖有效证书、过期证书、错误证书、吊销证书、错误私钥、token 过期、权限不足、请求签名错误和重放攻击。断言包括状态码、业务错误码、错误消息、日志、告警和审计记录。安全错误不能泄露内部堆栈、密钥或敏感账号信息。

### 11. 如何测试 API 限流和配额？

参考答案：先明确限流维度：客户、应用、用户、接口、IP 或时间窗口。用例覆盖阈值内、达到阈值、超过阈值、窗口重置、并发突刺和分布式节点一致性。还要验证错误码、重试建议、日志和监控，确保限流保护系统但不误伤其他客户。

### 12. 如何测试 API 文档和示例代码？

参考答案：把文档示例纳入自动化构建，在沙箱中验证请求能发起、响应符合文档、错误码一致。示例代码还要检查依赖版本、证书加载、超时配置和异常处理。开发者平台的文档质量直接影响客户集成效率，因此文档也应进入质量门禁。

### 13. 如何测试 GS Quant 这类 Python 工具包？

参考答案：覆盖数据获取、DataGrid 创建、时间序列处理、异常输入、网络错误、版本兼容和文档示例。Python 工具包还要测试依赖版本、pandas 行为差异、序列化、浮点精度和 API 认证。对于金融计算，应使用固定数据集和误差阈值，保证回归结果可解释。

### 14. 如何设计 API 契约测试？

参考答案：先把请求、响应、字段类型、枚举、错误码、分页、排序和权限规则固化为契约。服务端变更前跑 provider contract，客户端变更时跑 consumer contract。金融 API 还要验证金额精度、日期、时区、标识符、权限过滤和审计字段。契约测试能在集成前发现破坏性变更。

### 15. 如何测试 API 的错误处理？

参考答案：覆盖参数缺失、类型错误、权限不足、证书错误、资源不存在、限流、下游超时、服务内部错误和未知异常。错误响应应稳定、可诊断、不泄露敏感信息，并能指导客户端是否重试。测试还要验证日志和监控能帮助定位问题。

### 三、数据质量、市场数据与风险计算

### 16. 如何测试市场数据质量？

参考答案：从准确性、及时性、完整性、一致性和可追溯五个维度设计。测试要覆盖价格、成交量、收益率、曲线、波动率、证券标识、交易日历、币种和时间戳。自动化可以做数据源对账、异常值检测、缺失率统计、延迟分布和数据血缘校验。

### 17. 如何测试证券标识映射？

参考答案：覆盖 ticker、ISIN、CUSIP、SEDOL、FIGI、内部 id 和交易所代码之间的映射。重点是历史有效期、公司行动、合并拆分、跨市场同名证券和一对多关系。测试要断言当前映射和历史映射不能混用，并记录数据来源和更新时间。

### 18. 如何测试风险计算结果？

参考答案：准备固定投资组合、市场数据快照和模型版本，形成 golden dataset。测试要覆盖正常组合、空组合、极端市场、缺失价格、异常波动率、不同币种和不同估值日期。断言应使用合理误差阈值，并记录输入快照，便于复现。

### 19. 如何测试数据回放功能？

参考答案：回放要保证事件顺序、时间戳、速率、数据版本和外部依赖可控。测试覆盖正常回放、倍速回放、暂停恢复、乱序事件、重复事件和缺失事件。回放结果要和历史结果或预期状态对比，用于复现线上问题和验证修复。

### 20. 如何测试数据缓存对新鲜度的影响？

参考答案：测试缓存命中、失效、预热、回源失败、权限隔离和分布式一致性。市场数据场景中，新鲜度和正确性比命中率更关键。自动化应比较源时间戳和响应时间，验证 stale data 阈值、更新事件驱逐和缓存按用户权限隔离。

### 21. 如何发现数据源异常？

参考答案：建立基线指标：记录数、字段缺失率、延迟、价格跳变、重复率和源状态。通过规则和异常检测识别突降、突增或异常分布。发现后要自动生成影响范围、样本、数据源、最近变更和可能原因。关键数据源还应有替代源或降级策略。

### 22. 如何测试分析平台的权限控制？

参考答案：构造不同角色、客户、产品订阅、地区和数据授权，验证 UI、API、导出、缓存和日志都遵守同一权限规则。测试要覆盖越权访问、权限变更后缓存失效、共享仪表盘权限和审计日志。不能只验证入口按钮隐藏，必须验证后端权限。

### 23. 如何测试报表导出？

参考答案：覆盖不同格式、字段顺序、权限过滤、大数据量、编码、时区、金额精度、空值和异常字符。导出结果要和页面、API 或数据库对账。还要验证敏感字段脱敏、下载链接过期和审计记录。性能上要关注大报表生成时间和异步任务失败恢复。

### 四、测试平台、CI/CD 与工程实践

### 24. 如果为 Goldman Sachs 工程团队建设测试平台，你会提供哪些能力？

参考答案：平台应提供 API 自动化、契约测试、市场数据回放、数据对账、模型回归、mock 服务、环境健康检查、性能压测、报告看板、质量门禁和审计证据归档。金融场景还要支持权限模型、数据快照、交易日历、证书管理和敏感数据脱敏。

### 25. 如何把自动化测试接入 CI/CD？

参考答案：提交阶段跑单元测试、静态扫描和轻量契约；合并前跑 API、核心业务回归和安全扫描；部署测试环境后跑端到端、数据对账、模型回归和性能冒烟；灰度阶段跑生产探针和监控校验。高风险模块如交易、风险、权限和数据平台需要更严格门禁。

### 26. 如何治理 flaky test？

参考答案：先定位原因：数据不稳定、异步等待、环境波动、外部依赖、并发污染或断言过严。治理方法包括固定数据快照、mock 不稳定依赖、显式等待业务事件、用例隔离、失败上下文采集和稳定性看板。核心链路的 flaky test 不能长期留在门禁中。

### 27. 如何设计质量门禁？

参考答案：质量门禁应按风险分层，包含关键用例通过、契约兼容、数据质量、模型回归、安全扫描、性能阈值、错误预算和生产探针。低风险改动可以轻量，高风险改动必须严格。门禁失败要提供可诊断报告，否则会成为发布阻力。

### 28. 代码评审中测试开发会关注什么？

参考答案：关注边界条件、异常处理、并发安全、资源释放、日志、监控、权限、数据精度、时区、状态机、缓存失效和可测试性。金融系统还要检查审计字段、敏感信息脱敏、回滚方案和兼容性。好的测试开发能在代码合入前识别质量风险。

### 29. 如何设计生产探针？

参考答案：探针应模拟低风险关键路径，例如 API 鉴权、账户授权、市场数据查询、仪表盘加载、分析任务和只读导出。探针数据要可控，不应触发真实交易或敏感操作。指标包括成功率、延迟、数据新鲜度、错误码和权限校验，失败时关联最近发布和依赖状态。

### 30. 如何评估自动化测试 ROI？

参考答案：看回归时间下降、缺陷提前发现率、线上事故减少、门禁拦截有效性、发布频率、用例稳定性和维护成本。对于金融平台，少量覆盖交易、风险、权限和数据质量核心链路的自动化，比大量低价值 UI 用例更有价值。

### 31. 如何让测试报告适合故障定位？

参考答案：报告要包含用例意图、业务风险、请求响应摘要、trace id、数据快照、模型版本、环境版本、日志链接、差异样本和历史趋势。对数据质量失败，要展示字段差异、缺失率、异常样本和影响范围。这样开发可以直接定位问题。

### 五、AI、机器学习与智能测试

### 32. Goldman Sachs 工程场景中 AI/ML 测试重点是什么？

参考答案：重点包括数据质量、模型准确性、可解释性、稳定性、漂移、权限、隐私和审计。AI/ML 可能用于市场分析、风控、文档处理、开发效率和客户服务。测试不能只看离线指标，还要验证线上延迟、输入缺失、版本切换、阈值变化和人工复核流程。

### 33. 如何测试金融 LLM 助手？

参考答案：覆盖金融事实准确、引用可追溯、权限隔离、无答案拒答、敏感信息保护和提示注入防护。测试集应包含市场评论、研究摘要、客户问题、相似证券、过期信息和高风险建议。模型输出不能未经复核直接用于交易或投资建议。

### 34. 如何测试 RAG 金融问答系统？

参考答案：检索层验证召回率、权限过滤、时间有效性和文档去重；生成层验证答案忠实于来源、引用正确、不编造、不泄露。测试要覆盖有答案、无答案、权限不足、过期答案、相似实体和多跳问题。还要做提示注入和越权检索测试。

### 35. 如何用 AI 辅助测试用例生成？

参考答案：可以让 LLM 基于需求、API 文档、代码 diff、历史缺陷和生产事故生成候选测试点。测试开发需要审查、去重、风险分级，并转化为自动化。AI 生成用例要保留输入版本、prompt 和采纳记录，不能未经审查进入门禁。

### 36. 如何用 LLM 做日志分析？

参考答案：先通过检索、聚合和规则筛选出相关日志、trace、指标和变更，再让 LLM 总结异常模式和排查路径。输出必须包含证据，如 trace id、错误码、时间窗口和指标变化。高风险操作不能由模型自动执行，只能辅助工程师判断。

### 37. 如何测试模型漂移监控？

参考答案：建立特征分布、预测分布、业务指标和标签回流监控。测试要模拟市场环境变化、数据源缺失、特征异常、样本偏移和模型版本切换。漂移告警应包含影响范围、特征贡献和建议处理方式。还要验证告警不会因正常季节性波动频繁误报。

### 38. AI 生成自动化脚本有哪些风险？

参考答案：风险包括断言无效、等待不稳定、依赖不安全、误用生产数据、泄露密钥、删除环境数据和生成不可维护代码。应在沙箱运行，做代码审查、安全扫描、权限限制和小范围试点。核心金融系统不能直接信任 AI 生成脚本。

### 39. 如何测试 AI 生成的研究摘要？

参考答案：维度包括事实准确、数字正确、时间线正确、实体识别、风险提示、来源引用和语言清晰。测试集要包含财报、新闻、市场评论、监管文件和相似公司。高风险摘要必须支持引用追踪和人工复核，避免幻觉误导客户。

### 六、编码、SQL、Linux 与行为面

### 40. 编程题：实现一个最近 5 分钟请求计数器。

参考答案：低流量可以用队列保存时间戳，每次查询时移除 5 分钟外记录；高流量可用环形数组按秒聚合，每个槽保存时间戳和计数。要说明时间复杂度、空间上限、并发安全和时间回拨问题。分布式场景可以用 Redis 或流式聚合。

### 41. 编程题：给定行情更新流，保留每个证券的最新价格。

参考答案：使用哈希表 `symbol -> latest_event`，事件包含 sequence、event_time 和 price。若新事件 sequence 更大则更新，否则丢弃或记录重复。没有统一 sequence 时可用事件时间和来源优先级。测试要覆盖乱序、重复、缺失字段和同一时间多来源冲突。

### 42. 编程题：找出 1 分钟内波动超过阈值的证券。

参考答案：按证券分组维护滑动窗口，使用两个单调队列分别保存窗口最大值和最小值。每来一条价格，移除窗口外数据，计算 `(max - min) / min` 是否超过阈值。复杂度接近 O(n)。要考虑缺失价格、异常价格和停牌数据。

### 43. SQL 题：查询最近一小时 API 错误率最高的前 10 个客户。

参考答案：

```sql
SELECT client_id,
       COUNT(*) AS total_count,
       SUM(CASE WHEN status_code >= 500 THEN 1 ELSE 0 END) AS error_count,
       SUM(CASE WHEN status_code >= 500 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS error_rate
FROM api_logs
WHERE created_at >= CURRENT_TIMESTAMP - INTERVAL '1 hour'
GROUP BY client_id
HAVING COUNT(*) >= 100
ORDER BY error_rate DESC
LIMIT 10;
```

回答时要补充样本量阈值，否则低流量客户容易误报。还可以按接口、区域和错误码继续分析。

### 44. Linux 题：线上服务内存持续上涨，你如何排查？

参考答案：先用 `top`、`ps`、`free`、`pmap` 或容器指标确认进程，再结合语言工具查看 heap、GC、线程和 native memory。检查缓存、队列积压、连接池、批处理任务和最近发布。修复后要补充长稳测试、内存基线和告警阈值。

### 45. 系统设计题：设计一个市场数据质量监控系统。

参考答案：系统包括数据采集、规则校验、异常检测、指标存储、告警、样本留存和看板。校验维度包括延迟、缺失率、重复率、价格跳变、字段类型、权限和数据源状态。要支持回放、误报标注、规则灰度和影响范围分析。测试重点是数据正确性、告警准确性和故障恢复。

### 46. 行为题：你如何推动团队接受更严格的质量门禁？

参考答案：先用数据说明风险，例如线上缺陷、回归耗时、数据事故和发布回滚。然后从高价值核心链路试点，确保门禁执行快、失败可诊断、维护成本可控。推动过程中和开发共同优化框架和用例，让门禁成为交付保障而不是阻碍。最后用缺陷拦截率和回归时间下降证明价值。

## AI 相关加分点

- 能用 LLM 基于需求、API 文档、代码 diff 和历史缺陷生成测试点，并进行人工审查和自动化落地。
- 熟悉金融 RAG/LLM 测试，能验证权限过滤、引用忠实性、提示注入防护和无答案拒答。
- 能设计市场数据、风险模型和研究摘要的 AI 评测集，关注事实准确、时效性和可解释性。
- 能用日志、trace、指标和变更记录构建智能排障助手，缩短金融平台故障定位时间。
- 能用异常检测监控市场数据延迟、缺失、跳变、API 错误率和模型漂移。
- 理解金融 AI 的合规边界：隐私、审计、客户权限、人工复核和模型输出责任。
- 能将 AI 生成脚本纳入沙箱、代码审查、安全扫描和 CI/CD 门禁。

## 复习建议

- 编码重点练习哈希表、滑动窗口、队列、堆、树、字符串、并发和复杂度分析。
- 熟悉 Java/Python、API 自动化、契约测试、测试数据管理、mock、CI/CD 和报告平台。
- 金融业务重点准备市场数据、交易状态、风险计算、权限、审计、低延迟和数据对账。
- 系统设计重点准备市场数据监控、实时流处理、限流、缓存、消息队列、灾备和可观测性。
- AI 方向重点准备 RAG、LLM 评测、日志分析、模型漂移、AI 辅助测试生成和安全测试。
- 行为面用 STAR 法准备质量推动、线上事故、跨团队协作、复杂业务学习和平台落地案例。
