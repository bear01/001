# Cursor 测试开发工程师面试题

## 公司名

Cursor

## 岗位方向

测试开发工程师 / SDET / 质量工程方向，重点面向 AI 编程工具、Agent 模式、代码上下文检索、Rules、MCP、GitHub 集成、模型路由、企业安全、开发者体验、自动化测试平台、CI/CD、质量工程和 AI 辅助测试。

## 资料来源与整理依据

- [Cursor Docs](https://cursor.com/docs)
- [Agent](https://cursor.com/docs/agent)
- [Rules](https://cursor.com/docs/rules)
- [Model Context Protocol](https://cursor.com/docs/context/mcp)
- [Models & Pricing](https://cursor.com/docs/models-and-pricing)
- [GitHub Integration](https://cursor.com/docs/integrations/github)
- [Reviewing and Testing](https://cursor.com/learn/reviewing-testing)
- [Enterprise](https://cursor.com/docs/enterprise)
- [Security](https://cursor.com/security)
- [Privacy](https://cursor.com/privacy)
- [Trust Center](https://cursor.com/trust)

## 面试画像

Cursor 的测试开发岗位会更关注“AI 编程产品的质量闭环”：候选人既要懂传统 IDE、编辑器、插件、Git、CI/CD、自动化测试和性能稳定性，也要理解 LLM Agent、代码上下文、规则注入、MCP 工具、模型路由、代码生成正确性、安全边界和企业治理。回答时建议围绕“开发者工作流 -> AI 输出正确性 -> 工具调用安全 -> 自动化评测 -> 线上体验监控”展开。

## 题目分类

- 岗位理解与 AI 编程工具质量体系
- Agent、代码理解与上下文质量
- Rules、MCP、工具调用与集成测试
- 代码生成、测试生成与质量评测
- 性能稳定性、模型路由、成本与可观测性
- 企业安全、隐私与权限治理
- CI/CD、测试平台与 AI 辅助测试
- 编程、数据与行为面模拟题

## 一、岗位理解与 AI 编程工具质量体系

### 1. 你如何理解 Cursor 测试开发工程师和普通客户端测试开发的差异？

参考答案：普通客户端测试更关注 UI、兼容性、崩溃、性能和功能流程；Cursor 这类 AI 编程工具还要测试 Agent 是否理解代码库、生成代码是否正确、上下文检索是否命中、Rules 是否生效、MCP 工具是否安全、Git 集成是否可靠、模型响应是否稳定、企业策略是否可控。测试开发不能只判断按钮是否可点，还要把代码生成质量、任务完成率、误改率、测试通过率、开发者满意度和安全风险量化。

### 2. 如果让你负责 Cursor 的端到端质量，你会怎样分层？

参考答案：我会分六层：第一层是编辑器基础能力，覆盖安装、启动、文件编辑、索引、搜索和 Git；第二层是 AI 能力，覆盖 Chat、Inline Edit、Agent、代码理解和多文件修改；第三层是上下文与规则，覆盖 Rules、代码库检索、ignore、MCP、工具调用和外部集成；第四层是质量评测，覆盖生成代码编译、单测、lint、diff 审查和回滚；第五层是非功能，覆盖延迟、资源、稳定性、模型路由、成本和离线/弱网；第六层是企业安全，覆盖隐私、权限、审计、数据保留和团队策略。

### 3. AI 编程工具的“正确性”如何定义？

参考答案：正确性不能只看回答像不像。可以拆成任务完成率、编译通过率、测试通过率、lint 通过率、需求符合度、改动最小性、上下文引用准确率、无关文件误改率、安全问题引入率和用户接受率。确定性部分用编译、测试、静态分析和 diff 规则断言；开放性部分用人工评审、模型裁判和线上行为数据校准。

### 4. Cursor 这类产品的核心质量指标有哪些？

参考答案：核心指标包括请求成功率、首 token 延迟、端到端任务耗时、Agent 任务完成率、生成代码测试通过率、diff 接受率、撤销率、无关文件修改率、上下文命中率、MCP 工具调用成功率、崩溃率、索引耗时、CPU/内存占用、模型错误率、企业策略命中率和用户反馈。指标要按项目规模、语言、模型、操作系统、插件和网络环境切分。

### 5. 如果用户反馈“Cursor 最近变笨了”，你会如何定位？

参考答案：先确认影响范围：模型、功能入口、语言、项目规模、操作系统、版本、用户团队和时间段。再收集样本，查看 prompt、上下文文件、Rules、模型、工具调用、diff、编译结果和用户操作。通过旧版本、旧模型或固定评测集重放，判断是模型路由、上下文检索、规则注入、索引、提示词、工具失败还是产品交互变化。最后把样本加入回归评测。

### 6. 如何设计 Cursor 的模型版本回归测试？

参考答案：维护固定任务集，包括 bug 修复、功能开发、重构、测试生成、文档更新、跨文件修改、MCP 工具调用和安全边界。每次模型或 prompt 策略变更，自动在真实小仓库和合成仓库中运行，比较任务完成率、测试通过率、diff 大小、错误率、延迟和成本。对退化样本做人工复核，再决定阻断、灰度或回滚。

## 二、Agent、代码理解与上下文质量

### 7. 如何测试 Agent 模式的任务完成能力？

参考答案：准备可验证的开发任务，例如修复 failing test、增加 API 字段、重构函数、补充单测、修改 UI 状态和更新文档。每个任务有初始仓库、用户指令、预期测试和允许修改范围。Agent 执行后自动运行测试、lint、类型检查和 diff 检查，评估是否完成任务、是否误改无关文件、是否引入新问题。

### 8. 如何测试 Agent 的多文件修改能力？

参考答案：构造跨模块任务，例如新增后端接口并更新前端调用、修改类型定义和测试、重命名共享函数。断言 Agent 是否找到所有相关文件、保持接口一致、更新测试和文档、避免遗漏引用。自动化可以用编译、测试、静态分析和搜索旧符号验证。还要检查 diff 是否过大，避免过度重构。

### 9. 代码库索引和上下文检索怎么测？

参考答案：准备不同规模和语言的仓库，设置明确的目标问题，检查检索到的文件、符号和代码片段是否与任务相关。指标包括上下文命中率、无关上下文比例、索引耗时、增量索引延迟和大仓库内存占用。用例要覆盖新建文件、删除文件、重命名、分支切换、忽略文件和符号冲突。

### 10. 如何测试 Agent 不误改无关文件？

参考答案：每个任务定义允许修改文件范围或预期 diff 范围。执行后检查 changed files、diff 行数、关键文件保护规则和测试结果。对于不该动的配置、锁文件、生成文件和敏感文件，要设硬性断言。还可以注入诱导性指令，验证 Agent 不会为了通过任务而大范围改无关逻辑。

### 11. 如何测试长上下文项目中的回答质量？

参考答案：构造大仓库、重复命名、深层调用链和多语言项目，观察 Agent 是否能检索正确模块并引用最新代码。测试要覆盖仓库索引未完成、索引过期、分支切换、monorepo 子项目和 vendor 目录。指标包括上下文命中率、错误引用率、任务完成率和响应延迟。

### 12. 如何验证 Agent 的计划与执行一致？

参考答案：测试 Plan/Agent 任务时，可以记录计划步骤、实际工具调用、文件改动和最终结果。断言计划是否覆盖关键风险，执行是否偏离计划，遇到失败是否重新规划。好的 Agent 应该在任务复杂时分解步骤，在测试失败时定位原因，而不是盲目反复修改。

### 13. 如何测试 AI 生成测试用例的质量？

参考答案：准备已有代码和 bug 历史，让 Agent 生成单测、集成测试和边界测试。用 mutation testing、覆盖率、失败注入和人工评审评估测试是否能捕捉真实缺陷，而不是只追求覆盖率。还要检查测试是否稳定、可读、隔离、无网络依赖，并能在 CI 中可靠运行。

### 14. 如何测试“找 bug / 修 bug”工作流？

参考答案：准备带隐藏 bug 的仓库，提供失败日志、用户描述或测试失败信息。Agent 应能复现问题、定位根因、提出最小修复、运行验证并解释 diff。指标包括首次定位成功率、修复通过率、误改率、运行测试次数和总耗时。失败样本要按上下文不足、推理错误、工具失败和测试不足分类。

## 三、Rules、MCP、工具调用与集成测试

### 15. Cursor Rules 的测试重点是什么？

参考答案：Rules 用于把团队规范、项目约束和风格要求注入 AI 工作流。测试要覆盖全局规则、项目规则、文件作用域、规则优先级、规则冲突和规则更新。断言生成代码是否遵循命名、架构、测试、依赖和安全要求。负向用例要验证规则过长、规则冲突或恶意规则不会导致产品崩溃或越权。

### 16. 如何测试 Rules 是否真的生效？

参考答案：设计可观测规则，例如“所有新增函数必须有单测”“禁止引入某依赖”“API 返回类型必须用 Result”。让 Agent 在有规则和无规则两种环境下执行同一任务，比较 diff、测试和静态分析结果。还可以在任务中故意诱导违反规则，验证 Agent 是否拒绝或修正。

### 17. MCP 工具调用有哪些测试风险？

参考答案：MCP 把外部工具和数据源接入 AI 工作流，风险包括权限过大、工具参数错误、工具超时、返回恶意内容、数据泄露和副作用不可控。测试要覆盖工具发现、鉴权、参数 schema、调用失败、超时、重试、权限隔离和审计日志。高风险工具应有确认、dry-run 或权限边界。

### 18. 如何测试 MCP Server 集成？

参考答案：准备 mock MCP server 和真实 MCP server，覆盖连接、能力列表、参数校验、正常返回、错误返回、长耗时、断线重连和版本兼容。断言 Cursor 能正确展示工具、调用工具、处理错误和写入上下文。还要验证多个 MCP server 同时存在时不会串工具或泄露凭据。

### 19. 如何防止工具调用造成副作用事故？

参考答案：工具层要做最小权限、参数白名单、确认流、幂等键、审计和回滚。测试场景包括删除资源、发消息、创建 issue、修改数据库和触发部署。Agent 不应仅凭模型输出直接执行高风险动作；业务系统应二次校验用户身份、权限和参数。

### 20. GitHub 集成如何测试？

参考答案：覆盖登录授权、仓库读取、PR diff、评论、分支、权限不足、私有仓库、token 过期和网络失败。AI 场景要测试 Agent 是否正确理解 PR 上下文、定位评论行、生成修复、运行测试和避免修改无关代码。安全上要验证跨仓库访问隔离和 token 不出现在日志中。

### 21. 如何测试 Git 工作流中的 AI 修改？

参考答案：构造分支切换、未保存文件、冲突、stash、rebase、merge conflict、large diff 和二进制文件场景。断言 Agent 修改不会覆盖用户未提交更改，能正确处理冲突，生成 diff 可审查，可撤销。对危险命令和大范围删除要有保护和确认。

### 22. 如何测试第三方插件或扩展兼容性？

参考答案：Cursor 基于编辑器生态，需测试常见语言插件、格式化、lint、debugger、主题和 Git 插件。用例包括插件冲突、慢插件、崩溃插件、禁用插件和插件更新。AI 功能要验证上下文、诊断信息和格式化结果不会被插件干扰，性能指标也要按插件集合切分。

## 四、代码生成、测试生成与质量评测

### 23. 如何评估 AI 生成代码的质量？

参考答案：先运行编译、类型检查、lint、单测和集成测试；再做静态分析、安全扫描、复杂度和 diff 审查；最后人工评估可读性、架构一致性和维护性。指标包括测试通过率、bug 修复率、无关改动率、安全问题引入率和用户接受率。评估要按语言、任务类型、模型和项目规模分层。

### 24. 如何测试 AI 生成代码不会引入安全漏洞？

参考答案：准备安全敏感任务，如认证、权限、SQL、文件上传、命令执行和密钥处理。生成后运行 SAST、依赖扫描、secret scan、单元测试和攻击用例。还要在 Rules 中注入安全规范，验证 Agent 是否遵循。常见风险是 SQL 注入、路径穿越、XSS、明文密钥和权限绕过。

### 25. 如何测试代码补全和 Inline Edit？

参考答案：代码补全测试关注建议相关性、延迟、光标上下文、语法正确、去重和隐私；Inline Edit 测试关注选择范围、局部修改、格式保持、撤销、与 formatter 配合和多语言。自动化可用固定文件和光标位置生成建议，再用编译、测试和相似度评分验证。

### 26. 如何测试 AI 生成的 diff 可审查性？

参考答案：可审查性包括 diff 小、改动集中、命名清晰、测试同步、注释合理和无生成噪声。可以设定 diff 行数阈值、无关文件阈值、格式化噪声比例和测试覆盖要求。对大任务要求 Agent 分步骤提交或解释改动，避免一次性大 diff 让用户无法 review。

### 27. 如何设计代码生成评测集？

参考答案：评测集应包含真实 bug、功能开发、重构、测试生成、文档更新、API 迁移和安全修复。每个样本包括仓库快照、任务描述、验收测试、允许修改范围和评分规则。样本要覆盖多语言、多框架、小仓库和 monorepo，并定期加入线上失败样本。

### 28. 如何处理 AI 输出的非确定性？

参考答案：对确定性任务，用编译、测试和静态分析做硬断言；对开放任务，多次采样取统计结果，使用通过率和置信区间。评测时固定模型、参数和上下文，记录版本。不要要求每次 diff 完全相同，而要要求满足任务、测试通过、无安全问题且改动合理。

## 五、性能稳定性、模型路由、成本与可观测性

### 29. Cursor 的性能测试重点是什么？

参考答案：包括启动时间、文件打开、索引耗时、搜索、Agent 响应、首 token 延迟、完整任务耗时、CPU、内存、磁盘 IO、网络和大仓库体验。要按操作系统、项目规模、语言、插件数量和模型分层。AI 功能还要拆分上下文检索、模型调用、工具调用和 diff 应用耗时。

### 30. 如何测试大仓库和 monorepo 场景？

参考答案：准备百万行级仓库、多个 package、重复符号、生成文件和大依赖目录。测试索引耗时、内存、搜索准确率、上下文命中率、Agent 任务完成率和 UI 卡顿。还要验证 ignore 配置、增量索引、分支切换和文件监听不会造成资源飙升。

### 31. 如何测试模型路由和模型切换？

参考答案：覆盖不同模型、自动路由、手动选择、模型不可用、配额不足、降级和重试。断言用户选择被尊重，失败时有清晰提示，任务结果和日志能标识模型。模型切换还要跑固定评测集，对比任务完成率、延迟和成本，避免为了更快牺牲质量。

### 32. 如何做 AI 请求的成本治理测试？

参考答案：按用户、团队、模型、功能入口、token、工具调用和失败重试统计成本。测试要验证计量和计费一致，异常场景包括循环工具调用、重复重试、超长上下文、Agent 卡住和取消任务。企业版应支持预算、用量报表和策略限制。

### 33. 如何设计线上可观测性？

参考答案：记录 request id、用户匿名标识、模型、功能入口、上下文大小、延迟、错误码、工具调用、diff 大小、测试结果和用户反馈。隐私敏感内容要脱敏或采样。监控包括崩溃率、AI 请求失败率、P95/P99 延迟、上下文命中率、Agent 成功率、模型错误率和企业策略拦截。

### 34. 如何测试离线、弱网和服务异常？

参考答案：模拟断网、DNS 失败、高延迟、丢包、模型服务 5xx、限流和认证过期。断言编辑器基础功能仍可用，AI 功能失败提示清晰，可重试且不会丢失用户修改。Agent 长任务中断时要能保存状态或明确失败，不应出现半应用 diff 且无法回滚。

### 35. 如何做稳定性浸泡测试？

参考答案：用真实开发工作流持续运行，包括打开大仓库、索引、Chat、Agent、多文件修改、运行测试、Git 操作、MCP 工具和插件组合。监控内存、CPU、句柄、网络连接、崩溃、UI 卡顿和错误日志。浸泡测试重点发现资源泄漏、索引卡死、Agent 循环和插件冲突。

## 六、企业安全、隐私与权限治理

### 36. Cursor 企业版测试要关注哪些能力？

参考答案：关注团队管理、SSO、SCIM、权限、策略、用量、审计、数据隐私、模型配置和集成权限。测试包括成员加入离开、权限变更、团队策略下发、私有仓库访问、日志审计和管理员报表。企业场景最重要的是可控、可审计、可隔离。

### 37. 如何测试代码隐私和数据边界？

参考答案：验证哪些代码、prompt、上下文、日志和遥测会被发送，是否符合用户配置和企业策略。测试包括禁用遥测、隐私模式、敏感文件、ignore 配置、私有仓库、密钥文件和二进制文件。日志和错误报告不能包含明文 secret、私有代码或 token。

### 38. 如何测试 secret 泄露防护？

参考答案：在仓库中放置测试密钥、env 文件、证书、token 和私有 URL，验证上下文检索、AI 请求、日志、崩溃报告和 MCP 工具不会泄露。生成代码时也要运行 secret scan，防止 Agent 把密钥写入测试或文档。对高风险文件应默认排除或要求确认。

### 39. 如何防止 prompt injection 攻击代码工具？

参考答案：攻击可能藏在 README、issue、网页、代码注释或工具返回中，诱导 Agent 忽略规则、泄露密钥或执行命令。测试要构造恶意上下文，验证 Agent 能区分不可信内容和系统规则，高风险工具需要权限校验和确认。成功攻击样本要加入红队回归集。

### 40. MCP 和外部工具的权限如何治理？

参考答案：应按工具、workspace、用户和操作类型授予最小权限。测试包括未授权工具不可见、只读工具不能写入、写操作需确认、工具返回不能携带恶意指令、token 不进日志。企业管理员应能禁用不安全 MCP server，并查看工具调用审计。

## 七、CI/CD、测试平台与 AI 辅助测试

### 41. 如何把 AI 编程能力评测接入 CI/CD？

参考答案：PR 阶段跑快速 smoke，覆盖启动、基础 AI 请求、Rules、MCP mock、简单 bug 修复和生成测试；夜间跑完整评测集，包括多语言仓库、复杂 Agent 任务和性能基准；发布前跑新旧版本对比和企业策略回归。报告要展示任务通过率、测试通过率、diff 质量、延迟和成本。

### 42. 如何设计 Cursor 的自动评测平台？

参考答案：平台包括仓库样本库、任务描述、环境构建、Agent 执行器、测试运行器、diff 分析、静态扫描、评分器、人工复核和报告。每个任务要可重复执行，输出日志、模型、上下文、工具调用和最终 diff。平台要支持失败样本聚类和一键回放。

### 43. AI 辅助测试在 Cursor 自身研发中如何落地？

参考答案：可以用 AI 生成 IDE 交互用例、代码仓库样本、Rules 变体、MCP mock、bug 复现脚本和日志摘要；也可以让 Agent 辅助写单测和 E2E 测试。但关键断言仍要用编译、测试、UI 自动化、静态分析和人工评审兜底。AI 辅助测试是提效工具，不应替代质量门槛。

### 44. 如何治理 AI 编程评测中的 flaky？

参考答案：先区分模型随机性、环境不稳定、依赖下载、测试本身 flaky、评分器不稳定和资源波动。对模型随机性固定参数或多次采样；对环境问题使用容器和锁定依赖；对评分器做人类校准；对性能测试增加样本量。所有 flaky 要分类记录，避免简单重跑掩盖真实问题。

## 八、编程、数据与行为面模拟题

### 45. 编程题：如何实现一个 AI 生成代码评测器？

参考答案：评测器可以接收仓库快照、任务描述和 Agent 生成 diff；先应用 diff，再运行格式化检查、lint、类型检查、单测和安全扫描；随后计算 changed files、diff 行数、无关文件、测试覆盖和失败日志；最后输出结构化评分。测试覆盖编译失败、测试失败、无关改动、secret 泄露、超大 diff 和部分成功场景。

### 46. 行为面：研发认为“AI 输出不可测”，你会如何推动？

参考答案：我会先承认 AI 输出有非确定性，然后把问题拆成确定性工程质量和开放式体验质量。确定性部分用编译、测试、静态分析、状态机和权限断言；开放式部分用固定任务集、人工评审、模型裁判和线上用户反馈做趋势评估。先用高价值样本证明评测能发现真实退化，再逐步把评测结果接入发布门禁和灰度。

## AI 相关加分点

- 能把 AI 编程质量拆成任务完成率、测试通过率、上下文命中率、diff 可审查性、误改率、安全问题和用户接受率。
- 熟悉 Agent、Rules、MCP、GitHub 集成、模型路由、上下文检索和企业策略的测试风险。
- 会设计真实仓库评测集、bug 修复评测、测试生成评测、红队样本和线上失败样本回放。
- 能用编译、单测、lint、SAST、secret scan、diff 分析和人工复核组合评价 AI 生成代码。
- 对性能和成本敏感：大仓库索引、首 token 延迟、Agent 总耗时、上下文大小、模型切换和 token 成本。
- 对安全边界有意识：私有代码、secrets、prompt injection、MCP 权限、Git 操作保护和企业审计。

## 复习建议

1. 熟悉 Cursor Docs 中的 Agent、Rules、MCP、Models & Pricing、GitHub Integration、Enterprise 和 Security/Privacy 页面。
2. 准备一个 AI 编程工具测试项目案例，最好能讲清任务评测、真实仓库、自动运行测试和 diff 评分。
3. 复习 Git、CI/CD、单测、E2E、静态分析、secret scan、IDE 插件兼容和大仓库性能。
4. 准备 Agent 工具调用安全话题：MCP 权限、prompt injection、外部工具返回恶意内容和高风险操作确认。
5. 练习把“AI 写得不好”拆成可测指标：任务完成率、测试通过率、误改率、上下文命中率和人工评分。
6. 面试回答尽量体现“开发者工作流 + 工程断言 + AI 评测 + 企业安全 + 线上反馈”的完整闭环。
