# Block 测试开发工程师面试题

## 公司名

Block

## 岗位方向

测试开发工程师 / SDET / QA Automation Engineer / 支付与金融科技质量工程方向

## 资料来源与整理依据

- [Square Developer Documentation](https://developer.squareup.com/docs/)
- [Square API Reference](https://developer.squareup.com/reference)
- [Square Sandbox](https://developer.squareup.com/docs/devtools/sandbox/overview)
- [Square Authentication](https://developer.squareup.com/docs/auth)
- [Square Webhooks](https://developer.squareup.com/docs/webhooks/overview)
- [Block Investor Day 2025](https://block.xyz/investor-day-2025)
- [Inside Block’s 2025 Investor Day](https://block.xyz/inside/inside-blocks-2025-investor-day-building-for-profitable-growth-through-connected-ecosystems)
- 公开候选人面经中常见的 Block/Square/Cash App 工程面试方向：实用编码、系统设计、API 测试、支付链路、移动端质量、风控安全、数据分析和行为面。

> 说明：以下题目不是 Block 官方题库，而是结合公开资料、Square/Cash App/Afterpay 等业务场景和测试开发岗位能力模型整理的高频准备题与模拟题。

## 面试画像

Block 的技术生态覆盖 Square 商家支付和经营工具、Cash App 个人金融服务、Afterpay 分期支付、Bitkey/Bitcoin 相关产品和内部 AI 工程效率平台。测试开发岗位通常要求候选人既能写可靠自动化框架，也能理解支付、商户订单、退款、webhook、OAuth、移动端、风控、数据平台和 AI 辅助测试。面试更偏实用工程：如何把业务风险转成自动化、如何让 CI/CD 门禁可信、如何排查线上支付或数据问题。

## 题目分类

### 一、岗位理解与支付生态质量

### 1. 你如何理解 Block 测试开发工程师的核心价值？

参考答案：核心价值是用工程化方式保障 Block 多业务生态的交易正确性、系统稳定性和交付效率。Square 面向商户处理支付、订单、目录、库存和 webhook，Cash App 面向消费者处理转账、卡、投资和风控，Afterpay 涉及分期和信用风险。SDET 需要把这些业务风险转化为自动化测试、质量门禁、数据校验、生产探针和故障复盘，而不是只做发布前验收。

### 2. Block 的质量挑战和普通互联网产品有什么不同？

参考答案：Block 的核心业务涉及资金、商户经营、个人金融和安全风控，因此需要特别关注金额精度、支付状态、幂等、权限、合规、审计、反欺诈和数据隐私。普通互联网应用更侧重体验和增长，而支付金融产品还要验证失败补偿、重复请求、异步 webhook、退款、对账和生产稳定性。测试策略必须兼顾业务正确性和工程韧性。

### 3. Square 商家支付场景中，测试重点是什么？

参考答案：重点包括创建支付、授权、捕获、退款、订单、目录、库存、收据、商户位置、OAuth 授权、webhook 和对账。测试不仅要检查 API 返回成功，还要验证业务副作用，例如订单状态、支付流水、退款状态、库存变化和 webhook 通知。还要覆盖沙箱和生产契约一致性、限流、错误码和安全边界。

### 4. Cash App 场景中，测试开发应关注哪些质量风险？

参考答案：Cash App 可能涉及用户身份、转账、余额、卡、投资、Bitcoin、提醒和风控。质量风险包括账户安全、重复转账、余额不一致、欺诈交易、通知延迟、隐私泄露、移动端兼容和模型误判。测试要覆盖端到端状态、后端数据、移动端体验、异常恢复和生产监控。

### 5. Afterpay 这类 BNPL 业务的测试重点是什么？

参考答案：BNPL 关注分期计划、授信、还款、退款、逾期、商户结算和风控。测试要验证分期金额、手续费、还款时间、退款拆分、风控拒绝、状态流转和通知。还要关注模型或规则的公平性、可解释性和异常回滚，避免用户被错误授信或错误收费。

### 6. 面试中如何展示你适合 Block 的工程文化？

参考答案：可以强调实用工程、端到端责任和用户/商户影响。回答时结合项目说明你如何发现质量风险、搭建自动化框架、推动 CI/CD 门禁、用数据定位问题，并和开发、产品、风控或运营协作。Block 面向多生态产品，候选人要展示跨业务学习能力和主动推动质量改进的能力。

### 二、Square API、Sandbox 与 Webhook 测试

### 7. 如何测试 Square API 的支付接口？

参考答案：覆盖创建支付、幂等键、金额币种、支付方式、商户位置、授权失败、退款、取消、错误码和权限。自动化要验证响应 schema、业务状态、支付流水、订单关联和对账结果。支付接口测试必须检查业务副作用，不能只看 HTTP 200。

### 8. 如何测试 Square Sandbox？

参考答案：Sandbox 是隔离测试环境，可模拟支付和业务活动而不影响真实数据。测试要验证沙箱账号、测试支付方式、API base URL、默认 API 版本、示例请求、错误码和文档一致性。还要确认沙箱不会误连生产，测试数据稳定可复现，并能覆盖成功、失败和边界场景。

### 9. 如何测试 Square OAuth 授权流程？

参考答案：覆盖授权页跳转、seller 授权、scope 选择、authorization code 交换 token、refresh、revoke、过期 token、错误 redirect uri 和权限不足。还要测试 reduced scope token、短期 token 和多 location 场景。断言包括 token 权限、API 访问结果、错误码和审计日志。

### 10. 如何测试 scoped access token？

参考答案：构造不同 scope 的 token，验证只能访问授权资源。例如只读订单 token 不应能创建退款，有限时效 token 过期后必须失效。还要测试 scope 缩减、token 刷新、撤销后的缓存失效和错误响应。安全测试要确认日志不会泄露 token。

### 11. 如何测试 Square Webhooks？

参考答案：覆盖订阅创建、事件类型、通知 URL、HTTPS、JSON payload、2xx ACK、非 2xx 重试、重复通知、乱序通知和签名验证。Square 文档说明 webhook 事件不保证顺序，因此系统必须用事件 id、时间戳和业务状态机做幂等处理。测试要断言最终业务状态和重试记录。

### 12. Webhook 不保证顺序时，如何设计测试？

参考答案：构造先收到退款再收到支付更新、重复支付通知、延迟通知和旧事件覆盖新状态等场景。系统应根据事件时间、版本、状态优先级或查询源系统确认最终状态。测试重点不是每条通知都按顺序处理，而是最终状态正确、幂等、可追踪。

### 13. 如何验证 webhook 来源可信？

参考答案：验证签名、请求来源、HTTPS、payload 完整性和时间戳。测试要覆盖签名缺失、签名错误、body 被篡改、重放请求和过期请求。安全错误要可诊断但不泄露内部密钥。还要在日志中记录验证结果和 event id，便于排查。

### 14. 如何测试订单、目录和库存 API 的一致性？

参考答案：构造商品目录、订单创建、库存扣减、退款回补和多渠道销售场景。断言订单总价、税费、折扣、库存数量和状态一致。并发测试要覆盖多个终端同时下单，避免超卖或库存回滚错误。还要验证 webhook 与实际库存状态一致。

### 15. 如何测试 API 版本兼容？

参考答案：Square API 支持版本选择，测试要验证不同版本的字段、行为和 webhook 事件兼容性。发布新版本时跑契约测试和示例代码测试，确认旧客户端不被破坏。对 breaking change 要有迁移文档、错误提示和灰度策略。

### 三、自动化框架、CI/CD 与质量平台

### 16. 如何设计 Block 支付生态的 API 自动化框架？

参考答案：框架应包含环境配置、OAuth/token 管理、请求 client、schema 校验、业务断言、测试数据、幂等键、webhook 模拟、报告、日志脱敏和 CI 集成。支付场景还要支持沙箱数据、商户位置、订单状态、退款状态和对账校验。用例层只表达业务意图，底层 HTTP 和鉴权逻辑应封装。

### 17. 如何把自动化测试接入 CI/CD？

参考答案：提交阶段跑单元测试、静态扫描和轻量契约；合并前跑 API 回归、关键支付链路、webhook 和安全扫描；部署测试环境后跑端到端、数据校验和性能冒烟；灰度后跑生产只读探针和监控校验。高风险支付、风控、OAuth 和隐私相关变更要设置硬门禁。

### 18. 如何设计测试标签和回归选择？

参考答案：按业务域、风险等级、接口、支付方式、执行耗时、数据依赖和自动化层级打标签。回归选择应基于代码 diff、依赖图、历史缺陷和线上事故。AI 可以辅助推荐测试范围，但需要可解释依据，避免黑盒地跳过关键链路。

### 19. 如何治理 flaky test？

参考答案：先定位原因：异步 webhook、数据污染、沙箱波动、固定等待、并发冲突或断言过严。治理方法包括测试数据隔离、mock 不稳定依赖、显式等待业务状态、失败证据采集、稳定性看板和持续清理。支付核心链路的 flaky test 不应长期留在门禁中。

### 20. 如何设计测试数据管理？

参考答案：使用沙箱商户、测试支付方式、合成用户、固定目录、可重置库存和隔离订单。每次测试前创建数据，测试后清理或归档。涉及真实用户、卡、账户、token 和交易记录时必须脱敏并限制访问。测试数据要有版本，便于复现失败。

### 21. 如何测试移动端 Cash App 的自动化？

参考答案：覆盖登录、余额、转账、卡、通知、异常网络、深链、系统权限和多设备同步。移动端自动化要结合 API/服务层验证，避免只测 UI。还要关注弱网、后台恢复、推送延迟、设备兼容、隐私遮罩和安全校验。高风险资金操作应使用沙箱或模拟账户。

### 22. 如何让测试报告更有诊断价值？

参考答案：报告应包含用例意图、业务风险、请求响应摘要、trace id、payment id、order id、event id、webhook 历史、沙箱账号、环境版本、日志链接和历史趋势。支付失败要展示状态机轨迹和业务副作用，帮助开发判断是代码、数据、沙箱还是下游问题。

### 23. 代码评审中测试开发会关注什么？

参考答案：关注幂等、金额精度、状态机、异常处理、重试退避、权限校验、敏感日志、并发安全、监控指标、缓存失效和可测试性。对于 webhook 和消息处理，还要关注重复、乱序和死信。好的 SDET 会在代码合入前发现质量风险，而不是等测试执行才发现。

### 四、支付风控、数据安全与稳定性

### 24. 如何测试 Cash App 或 Square 的反欺诈模型？

参考答案：构造正常交易、可疑交易、设备异常、账号异常、商户异常、金额边界和历史欺诈样本，评估召回率、误报率、漏报率、延迟和原因码。还要测试特征缺失、模型服务超时、规则兜底、版本切换和线上漂移监控。反欺诈测试要平衡用户体验和风险拦截。

### 25. 如何测试身份验证和账户安全？

参考答案：覆盖登录、MFA、设备绑定、密码重置、风险登录、会话过期、token 撤销和账户锁定。还要测试弱网、重复请求、异常设备和安全通知。断言包括用户可见结果、后台风险事件、审计日志和敏感信息脱敏。

### 26. 如何测试 P2P 转账幂等？

参考答案：同一幂等键重复提交相同转账请求，只应产生一次业务效果；同一幂等键不同请求应拒绝或冲突；超时重试不应重复扣款。测试要检查余额、交易流水、通知、风控记录和审计日志，而不是只看响应码。

### 27. 如何测试退款流程？

参考答案：覆盖全额退款、部分退款、多次退款、超额退款、支付失败后退款、退款回调和商户权限。断言订单状态、支付状态、退款流水、库存回补、客户通知和对账记录。还要测试下游超时、重复退款请求和状态不同步。

### 28. 如何测试商户结算和对账？

参考答案：对账要比较支付、退款、手续费、税费、结算批次、商户账单和内部流水。覆盖跨时区、节假日、部分失败、重复记录和缺失记录。自动化要输出差异类型、交易样本和影响范围，帮助快速定位。

### 29. 如何测试限流和风控拦截？

参考答案：构造单用户、单商户、单 IP、单接口的突发流量，验证限流阈值、错误码、重试建议和监控。风控拦截要验证原因码、人工复核、白名单、黑名单和误杀样本。限流和风控都不能影响其他租户。

### 30. 如何测试生产监控和告警？

参考答案：对支付成功率、退款失败率、webhook 延迟、OAuth 错误、API P99、风控超时、队列积压和沙箱健康设置告警。通过故障注入验证告警能触发、路由正确并包含上下文。告警要控制噪声，避免长期误报。

### 31. 如何排查 API P99 延迟突然升高？

参考答案：先按接口、区域、商户、版本和错误码切分，再检查网关、服务、数据库、缓存、队列、下游依赖和最近发布。用 trace 样本定位慢环节，并用回放或压测复现。修复后要补充性能回归和监控阈值。

### 32. 如何测试故障恢复？

参考答案：构造下游超时、数据库故障、消息队列积压、webhook 消费失败、缓存不可用和区域故障。验证系统是否按策略重试、降级、死信、补偿和恢复。支付系统恢复后要检查状态一致和对账结果，防止隐性资金错误。

### 五、AI、机器学习与智能测试

### 33. Block 强调 AI 与工程效率时，测试开发可以怎么落地？

参考答案：可以用 AI 辅助需求分析、测试点生成、代码 diff 风险识别、日志分析、自动化脚本草稿和报告总结。落地时要有人工审查、沙箱运行、安全扫描和采纳记录。AI 应提升效率，但不能替代对支付、风控、隐私和合规风险的工程判断。

### 34. 如何测试 MoneyBot 或 ManagerBot 这类 AI 产品？

参考答案：测试重点包括事实准确、用户权限、隐私保护、引用来源、无答案拒答、多轮上下文、提示注入和高风险操作限制。Cash App 金融建议或 Square 商家经营建议都不能编造数据或越权访问。高风险建议应有免责声明、来源和人工或规则兜底。

### 35. 如何测试 AI 客服或支持助手？

参考答案：覆盖账户问题、支付失败、退款、欺诈申诉、商户设置、无权限请求和敏感数据。断言助手不能泄露隐私、不能绕过认证、不能给出不合规建议，遇到复杂问题应转人工。还要评估幻觉率、解决率、客户满意度和升级率。

### 36. 如何测试 RAG 金融问答系统？

参考答案：检索层验证召回率、权限过滤、时间有效性和文档去重；生成层验证答案忠实、引用正确、不编造、不泄露。测试集包含有答案、无答案、权限不足、过期文档、相似问题和提示注入。金融场景必须确保回答可追溯。

### 37. 如何用异常检测提升支付质量？

参考答案：对支付成功率、退款失败率、webhook 延迟、风控拒绝率、商户异常、用户转账频率和 API 错误率建模。测试要评估误报率、漏报率、检测延迟和解释能力。模型要考虑节假日、促销、开店高峰等正常波动，避免误报过多。

### 38. AI 生成自动化脚本有哪些风险？

参考答案：风险包括断言无效、等待不稳定、依赖不安全、误用真实数据、泄露密钥、删除环境数据和不可维护。应在沙箱运行，做代码审查、安全扫描和小范围试点。涉及资金或真实账户的脚本绝不能直接由 AI 生成后运行。

### 39. 如何评估 AI 辅助测试的收益？

参考答案：可以看测试点采纳率、自动化落地率、缺陷发现率、review 通过率、用例稳定性和节省时间。对 LLM 产品还要看事实准确率、拒答正确率、权限违规率、提示注入防护和引用准确率。指标要能指导改进，而不是只展示 AI 使用量。

### 六、编码、SQL、Linux 与行为面

### 40. 编程题：实现一个最近 5 分钟请求计数器。

参考答案：低流量可用队列保存时间戳，每次查询清理 5 分钟外数据；高流量可用环形数组按秒聚合，每个槽保存时间戳和计数。需要考虑并发、时间回拨、内存上限和多实例聚合。分布式场景可使用 Redis sorted set 或流式计算。

### 41. 编程题：给定交易列表，找出疑似重复支付。

参考答案：优先使用幂等键或 payment id；没有唯一键时，可按 buyer、seller、amount、currency、order_id 和短时间窗口分组。使用哈希表记录已见请求，重复出现时标记。真实系统不能只按金额判断，必须结合状态、来源和时间窗口。

### 42. 编程题：实现 webhook 事件去重。

参考答案：维护事件 id 或业务 id 的去重表，记录处理状态和过期时间。收到事件先检查是否已处理，已处理则跳过；处理中则根据锁或状态等待；未处理则执行业务并写入完成状态。要考虑并发、重试、数据库事务和失败恢复。

### 43. SQL 题：查询过去一小时失败率最高的商户。

参考答案：

```sql
SELECT merchant_id,
       COUNT(*) AS total_count,
       SUM(CASE WHEN status = 'FAILED' THEN 1 ELSE 0 END) AS failed_count,
       SUM(CASE WHEN status = 'FAILED' THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS fail_rate
FROM payments
WHERE created_at >= CURRENT_TIMESTAMP - INTERVAL '1 hour'
GROUP BY merchant_id
HAVING COUNT(*) >= 100
ORDER BY fail_rate DESC
LIMIT 10;
```

要补充样本量阈值，避免低流量商户误判。还可以按错误码、地区、应用版本和支付方式继续分析。

### 44. Linux 题：线上服务 CPU 飙高，你如何排查？

参考答案：先用 `top`、`pidstat` 找到进程和线程，再用 `jstack`、`perf`、`async-profiler` 或 APM 定位热点。检查最近发布、异常重试、日志打印、序列化、加密、正则和线程池。修复后补充压测场景和 CPU 监控阈值。

### 45. 系统设计题：设计一个 Square webhook 质量监控系统。

参考答案：系统包括事件接收、签名校验、幂等去重、处理状态、重试、死信、指标聚合、告警和看板。指标包括通知成功率、处理延迟、重复率、乱序率、失败原因和商户影响范围。测试重点是重复、乱序、签名错误、消费者失败和最终业务状态一致。

### 46. 行为题：你如何推动团队从手工回归转向自动化门禁？

参考答案：先用数据说明手工回归的问题，如耗时长、漏测、线上缺陷和发布回滚。再从高价值核心链路试点，例如支付创建、退款、webhook、OAuth 和商户订单。推动时保证门禁执行快、失败可诊断、维护成本可控。最后用缺陷拦截率和回归时间下降证明价值。

## AI 相关加分点

- 能用 LLM 基于 API 文档、代码 diff、历史缺陷和线上事故生成测试点，并进行人工审查和自动化落地。
- 熟悉支付和金融 AI 测试，能验证隐私、权限、事实准确、提示注入、高风险拒答和引用追溯。
- 能测试 AI 客服、MoneyBot、ManagerBot、风控模型和异常检测系统。
- 能用日志、trace、event id、payment id 和指标构建智能排障流程，缩短支付链路故障定位时间。
- 能用异常检测发现支付失败率突增、webhook 延迟、商户异常、欺诈风险和沙箱不稳定。
- 理解 AI 生成脚本的风险，能建立沙箱、代码审查、安全扫描和 CI 门禁流程。
- 能将 AI 辅助测试收益量化为采纳率、缺陷发现率、自动化落地率和节省时间。

## 复习建议

- 编码重点练习哈希表、滑动窗口、队列、幂等去重、字符串和复杂度分析。
- 熟悉 API 自动化、OAuth、webhook、沙箱测试、契约测试、测试数据管理和 CI/CD。
- 支付业务重点准备支付、退款、订单、库存、商户位置、Cash App 转账、Afterpay 分期和风控。
- 稳定性重点准备 P99 延迟、重试、熔断、死信、对账、生产探针和故障注入。
- AI 方向重点准备 AI 辅助测试、RAG、智能客服、风控模型、异常检测和日志分析。
- 行为面用 STAR 法准备质量推动、线上事故、跨团队协作、复杂业务学习和自动化平台落地案例。
