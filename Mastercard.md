# Mastercard 测试开发工程师面试题

## 公司名

Mastercard

## 岗位方向

测试开发工程师 / SDET / Software Engineer in Test / 支付网络质量工程方向

## 资料来源与整理依据

- [Mastercard Developers](https://developer.mastercard.com/)
- [Mastercard Payment Gateway Developer Integration](https://www.mastercard.com/gateway/index.html)
- [Mastercard AI and Security Solutions - Brighterion AI](https://b2b.mastercard.com/ai-and-security-solutions/risk-decisioning/technology/)
- [Mastercard 生成式 AI 反欺诈公告](https://www.mastercard.com/news/press/2024/may/mastercard-accelerates-card-fraud-detection-with-generative-ai-technology/)
- [Mastercard Consumer Fraud Risk 新闻稿](https://newsroom.mastercard.com/news/press/2024/april/mastercard-transforms-the-fight-against-scams-with-latest-ai-tech/)
- [Mastercard Engineering Careers - Software Engineer in Test](https://careers.mastercard.com/us/en/job/R-274181/Software-Engineer-II-Software-Engineer-in-Test)
- [Mastercard Engineering Careers - Test Automation/Java](https://careers.mastercard.com/us/en/job/R-274425/Senior-Software-Engineer-Test-Automation-Java)

> 说明：以下题目不是 Mastercard 官方题库，而是结合公开招聘信息、开发者平台资料、支付网络业务和测试开发岗位能力模型整理的高频准备题与模拟题。

## 面试画像

Mastercard 的测试开发岗位更偏“软件工程质量”而不是传统手工测试。公开岗位信息强调 Java 自动化、API/UI/分布式系统测试、CI/CD、DevTestOps、代码评审、云原生应用、核心授权、清算、借记、反欺诈和开发者平台。候选人需要证明自己能用代码、平台和数据方法提升质量，同时理解支付网络的幂等、低延迟、风控评分、令牌化、对账、安全合规和生产稳定性。

## 题目分类

### 一、岗位理解与支付网络质量

### 1. 你如何理解 Mastercard Software Engineer in Test 的核心价值？

参考答案：核心价值是通过软件工程能力提升支付网络和业务平台的质量、韧性和交付效率。Mastercard 的测试开发不仅写自动化脚本，还需要参与需求评审、代码评审、测试框架建设、CI/CD 门禁、环境自动化、生产监控和故障复盘。支付系统涉及授权、清算、风控、令牌化和开发者 API，测试开发要把资金正确性、安全性、低延迟和可审计性转化为自动化检查。

### 2. Mastercard 的测试开发和普通 QA 有什么不同？

参考答案：公开岗位描述中强调 Quality Engineering 要有接近软件开发工程师的编码和代码评审能力，能构建测试基础设施并嵌入开发流程。普通 QA 可能更偏执行用例，而 Mastercard 的 SDET 需要设计可维护框架、写 Java 自动化、分析分布式系统问题、推动 DevTestOps 和质量门禁。回答时可以强调“测试即工程”，不是只做末端验收。

### 3. 支付网络测试为什么特别关注低延迟和高可用？

参考答案：支付授权和风控评分通常发生在用户结账路径上，延迟过高会导致交易失败、用户放弃或商户损失。高可用也很关键，因为支付网络服务大量金融机构、商户和消费者。测试要关注 P95/P99 延迟、错误率、吞吐、降级策略、灾备切换和重复处理，而不是只验证功能正确。

### 4. 如何设计信用卡授权链路的测试策略？

参考答案：先建模交易状态和参与方：商户、收单机构、网络、发卡机构、风控服务和持卡人。测试覆盖正常授权、拒绝、超时、重复请求、撤销、部分授权、币种、限额、风控命中和网络异常。自动化要验证请求字段、响应码、交易 id、幂等性、状态机、审计日志和对账记录。

### 5. 如何测试清算和授权结果的一致性？

参考答案：授权是交易批准阶段，清算是资金结算阶段。测试要验证授权成功的交易能进入清算，撤销或失败交易不会错误清算，金额、币种、商户、手续费和交易日期一致。可以通过批处理文件、流水表、对账任务和异常报告做自动校验。对账差异要能定位到字段、交易和处理阶段。

### 6. 如何向面试官说明你适合支付行业质量工程？

参考答案：可以强调自己具备三类能力：工程能力，比如 Java/Python 自动化、API 测试、CI/CD 和分布式排障；质量方法，比如风险分析、契约测试、性能测试和质量门禁；支付意识，比如金额精度、幂等、状态机、风控、令牌化、数据安全和审计。最好结合项目案例说明如何把复杂业务规则落成自动化。

### 二、API 自动化、开发者平台与网关

### 7. 如何测试 Mastercard Developers 平台上的 API？

参考答案：需要覆盖文档示例、沙箱请求、鉴权、证书、参数校验、响应 schema、错误码、限流、版本兼容和 SDK 示例。开发者平台质量不只是 API 本身，还包括示例代码能否运行、文档与行为是否一致、沙箱数据是否稳定、错误信息是否可诊断。自动化应结合契约测试和端到端沙箱测试。

### 8. 如何测试支付网关接口？

参考答案：覆盖创建订单、授权、捕获、撤销、退款、状态查询、3DS、回调通知、重复请求和异常错误。还要验证不同支付方式、币种、商户配置和国家地区规则。接口测试必须断言业务副作用，例如交易状态、金额、回调、对账记录，而不是只看 HTTP 200。

### 9. 如何设计 REST API 自动化框架？

参考答案：框架应包含环境配置、鉴权封装、请求 client、数据驱动、schema 校验、业务断言、报告、日志、重试、并行执行和 CI 集成。支付场景要额外支持幂等键、证书、签名、敏感字段脱敏、交易 id 追踪和测试账户隔离。用例层应表达业务意图，避免重复拼接底层 HTTP 细节。

### 10. 如何测试 API 鉴权、签名和证书？

参考答案：覆盖有效证书、过期证书、错误私钥、证书被吊销、签名字段缺失、签名顺序错误、请求体篡改、时间戳过期和重放攻击。断言包括 HTTP 状态、业务错误码、错误消息、审计日志和安全告警。安全错误不能暴露内部密钥、堆栈或敏感配置。

### 11. 如何做 API 契约测试？

参考答案：先定义请求字段、响应字段、枚举、错误码、必填规则、兼容性和版本策略。服务端变更前跑 provider contract，客户端或 SDK 变更时跑 consumer contract。支付 API 还要校验金额精度、币种、国家地区规则、状态枚举和脱敏规则。契约测试能提前发现破坏开发者集成的问题。

### 12. 如何测试 API 幂等？

参考答案：用同一幂等键重复发送相同请求，系统应返回同一业务结果且只产生一次交易；同一幂等键但请求体不同，应拒绝或返回冲突；幂等记录过期后应按规则处理。还要测试并发重复请求、网络超时后重试、服务重启和幂等存储故障。最终断言必须检查交易流水和状态。

### 13. 如何测试支付回调？

参考答案：构造成功、失败、处理中、重复、乱序、延迟、签名错误和商户端不可达。系统应验证回调来源和签名，按状态机幂等更新，不允许终态被旧消息覆盖。回调失败要有重试和死信处理。测试报告应展示回调历史、状态变化和重试次数。

### 14. 如何测试 API 限流？

参考答案：明确限流维度：商户、应用、IP、用户、接口或区域。测试覆盖阈值内、达到阈值、超过阈值、窗口重置、并发突刺和多节点一致性。还要验证错误码、重试建议、日志和监控。支付平台限流要保护核心服务，同时不能误伤其他租户。

### 15. 如何测试 SDK 示例代码？

参考答案：将文档中的 Java、Python、Node.js 等示例纳入自动化构建，在沙箱环境验证可以编译、鉴权、发送请求并处理响应。还要测试依赖版本、异常处理、超时配置、证书加载和日志脱敏。示例代码如果不可运行，会直接影响开发者体验。

### 三、质量平台、CI/CD 与 DevTestOps

### 16. 如果为 Mastercard 团队建设测试平台，你会提供哪些能力？

参考答案：平台应提供 API 自动化、UI 自动化、契约测试、支付状态模拟、mock 下游、回调模拟、测试数据工厂、性能压测、环境健康检查、对账校验、报告看板和质量门禁。还要支持证书管理、敏感数据脱敏、审计证据归档和按风险选择回归集。

### 17. 如何理解 DevTestOps？

参考答案：DevTestOps 是把测试活动嵌入开发和运维全流程。它要求测试不只在发布前执行，而是在需求、代码、构建、部署、监控和复盘阶段持续发生。具体实践包括左移测试、自动化门禁、环境即代码、生产探针、灰度验证、指标驱动质量和故障复盘用例化。

### 18. 如何把自动化测试接入 CI/CD？

参考答案：提交阶段跑单元测试、静态扫描和轻量契约；合并阶段跑 API 回归、关键状态机和安全扫描；部署测试环境后跑端到端、回调和对账；灰度阶段跑生产探针和监控校验。高风险模块如授权、清算、风控和令牌化要设置硬门禁。

### 19. 如何设计质量门禁？

参考答案：质量门禁可包含关键用例通过、覆盖率、契约兼容、漏洞扫描、性能阈值、错误预算、对账一致和生产探针通过。门禁要按风险分层，核心支付链路应严格，低风险页面改动可轻量。门禁失败必须有清晰诊断信息，否则会变成团队阻力。

### 20. 如何治理 flaky test？

参考答案：先分类原因：测试数据污染、异步等待、环境不稳定、外部依赖波动、并发冲突或断言过严。治理方法包括数据隔离、mock 不稳定依赖、显式等待业务事件、失败上下文采集、稳定性看板和定期淘汰低价值用例。核心支付链路的 flaky test 不能长期留在门禁里。

### 21. 如何做测试环境自动化？

参考答案：使用基础设施即代码、容器化、配置模板、数据初始化脚本和健康检查。支付测试环境要支持证书、沙箱账户、mock 下游、交易日历、风控规则版本和敏感配置隔离。每次环境创建后自动跑冒烟测试，确保环境问题不会伪装成代码缺陷。

### 22. 如何设计测试报告？

参考答案：报告应包含用例意图、业务风险、请求响应摘要、trace id、交易 id、错误码、环境版本、测试数据、日志链接、状态机路径和历史趋势。对支付失败，还应展示回调历史、幂等记录和对账差异。报告要让开发能直接定位问题。

### 23. 代码评审中你会从测试角度关注什么？

参考答案：关注边界条件、幂等、异常处理、事务一致性、状态机、金额精度、权限校验、敏感日志、监控指标、并发安全和可测试性。对于 Java 微服务，还要关注线程池、连接池、重试退避、序列化兼容和错误码设计。测试开发参与评审能在代码合入前发现质量风险。

### 四、风控、AI 与安全测试

### 24. Mastercard 的 AI 风控场景测试重点是什么？

参考答案：重点是准确性、低延迟、误报率、漏报率、可解释性、数据隐私和线上漂移。Mastercard 的公开资料提到 AI 用于欺诈检测、诈骗识别和风险决策，这类模型通常位于交易或风控链路上。测试要覆盖正常交易、欺诈样本、边界分数、特征缺失、模型超时、版本切换和降级策略。

### 25. 如何测试 Decision Intelligence 或类似风控评分服务？

参考答案：构造不同金额、商户、国家、设备、账户历史和异常模式，验证风险分数、原因码、阈值策略和响应时间。还要测试特征服务异常、模型服务不可用、规则兜底、灰度模型和结果审计。测试不能只看接口成功，要验证分数是否在可解释范围内。

### 26. 如何评估反欺诈模型的误报和漏报？

参考答案：使用标注样本和线上回流数据计算 precision、recall、false positive rate、false negative rate、AUC 和业务成本。支付风控中误报会伤害正常用户，漏报会造成资金损失。测试要按地区、商户、金额、支付方式分层评估，避免整体指标掩盖局部风险。

### 27. 如何测试生成式 AI 反欺诈助手？

参考答案：如果生成式 AI 用于解释风险、总结欺诈案例或辅助调查，要验证事实准确、引用证据、隐私保护、无过度推断和提示注入防护。测试集应包含证据充分、证据不足、相似实体、恶意提示和敏感数据场景。模型输出不能直接作为最终拒绝交易的唯一依据。

### 28. 如何测试 Consumer Fraud Risk 这类实时诈骗识别能力？

参考答案：测试要覆盖正常转账、疑似诈骗、边界风险、账户接管、社工诈骗模式和实时拦截流程。关键指标包括识别延迟、拦截准确性、原因解释、用户提示和误拦率。还要验证不同银行或渠道接入时的数据权限、字段缺失和回调处理。

### 29. 如何测试支付令牌化？

参考答案：覆盖 token 创建、绑定、使用、暂停、恢复、删除、过期、设备迁移和授权失败。测试重点是 token 与原始卡号映射安全、权限隔离、生命周期状态、交易授权和审计记录。日志、API 响应和报告中不得暴露敏感卡数据。

### 30. 如何测试敏感数据脱敏？

参考答案：定义 PAN、token、证件号、手机号、邮箱、密钥等敏感字段清单，扫描 API 响应、日志、错误消息、监控标签、测试报告和导出文件。自动化可以识别常见卡号模式和未脱敏字段。异常堆栈和 debug 日志也要纳入检查。

### 31. 如何做支付系统安全测试？

参考答案：覆盖鉴权、授权、签名、重放、越权、注入、敏感数据泄漏、证书管理、速率限制、回调来源验证和审计日志。还要验证依赖漏洞、容器镜像安全和密钥管理。高危安全问题应进入 CI/CD 硬门禁。

### 五、性能、稳定性与可观测性

### 32. 如何设计核心授权系统的性能测试？

参考答案：先建立真实流量模型，包括商户类型、地区、交易金额、支付方式、风控命中比例和峰值倍数。压测要覆盖正常峰值、突刺流量、下游慢响应、模型超时、队列积压和批量重试。指标包括 TPS、P95/P99 延迟、错误率、线程池、连接池、CPU、内存和恢复时间。

### 33. 如何测试系统灾备和多活？

参考答案：构造节点故障、可用区故障、区域切换、数据库故障、消息队列故障和配置中心故障。验证 RTO、RPO、流量切换、重复处理、数据一致性和客户可感知影响。支付系统的灾备演练还要验证交易不会重复授权或错误丢失。

### 34. 如何排查生产错误率突然升高？

参考答案：先按错误码、地区、商户、接口、版本和支付方式切分，判断是否与发布、配置、流量或下游故障相关。然后用 trace、日志、指标和样本请求定位根因。测试开发要把事故样本沉淀为回归用例，并补充监控和探针。

### 35. 如何测试消息队列可靠性？

参考答案：覆盖重复消息、乱序、丢失、延迟、消费者重启、死信和积压。系统应通过幂等、状态机和补偿任务保证最终一致。测试断言要关注最终交易状态、日志、告警和对账，而不只是消息消费成功。

### 36. 如何设计对账测试？

参考答案：对账测试比较授权记录、清算记录、退款、撤销、商户账单和内部流水。覆盖成功、失败、冲正、跨币种、手续费、重复记录和缺失记录。自动化要能指出差异类型和样本，帮助定位是金额、状态、时间窗口还是数据源问题。

### 37. 如何测试生产监控和告警？

参考答案：对交易成功率、授权延迟、风控超时、错误码突增、回调失败、队列积压和对账差异设置告警。测试要通过故障注入确认告警能触发、路由正确并包含上下文。告警还要控制噪声，避免长期误报让团队麻木。

### 38. 如何测试熔断和降级？

参考答案：模拟下游超时、错误率升高、模型服务不可用和数据库慢响应。验证熔断是否保护核心系统、降级路径是否符合业务规则、恢复后是否自动半开探测。支付系统降级要谨慎，必须明确是拒绝、排队、使用规则兜底还是人工复核。

### 六、编码、SQL、Linux 与行为面

### 39. 编程题：实现一个按商户维度的滑动窗口限流器。

参考答案：可以用哈希表保存 `merchant_id -> queue/timestamp bucket`。每次请求清理窗口外数据，若窗口内计数低于阈值则允许，否则拒绝。高并发分布式场景可用 Redis sorted set 或令牌桶。回答要说明并发安全、时钟偏差、内存清理和多节点一致性。

### 40. 编程题：给定交易流，找出疑似重复授权。

参考答案：使用哈希表按幂等键、交易 id 或业务唯一字段分组。如果没有唯一键，可按持卡人、商户、金额、币种和短时间窗口生成候选重复。真实系统不能只按金额判断，需结合授权码、订单号、状态和时间窗口。测试要覆盖同金额不同订单和同订单重试。

### 41. 编程题：实时统计每分钟失败率最高的前 N 个商户。

参考答案：按分钟窗口维护商户总数和失败数，用哈希表聚合，再用小顶堆保留失败率最高的 N 个。需要设置最小样本量，避免低流量商户误排第一。流式系统中可用滑动窗口和延迟数据处理，输出时带样本数、失败数和错误码分布。

### 42. SQL 题：查询过去 1 小时授权失败率最高的支付通道。

参考答案：

```sql
SELECT channel,
       COUNT(*) AS total_count,
       SUM(CASE WHEN status = 'DECLINED' THEN 1 ELSE 0 END) AS declined_count,
       SUM(CASE WHEN status = 'DECLINED' THEN 1 ELSE 0 END) * 1.0 / COUNT(*) AS decline_rate
FROM authorization_events
WHERE created_at >= CURRENT_TIMESTAMP - INTERVAL '1 hour'
GROUP BY channel
HAVING COUNT(*) >= 100
ORDER BY decline_rate DESC
LIMIT 10;
```

要补充样本量阈值和错误码维度，避免小样本误判。也可按地区、商户和发卡机构进一步分析。

### 43. SQL 题：如何找出有授权但缺少清算记录的交易？

参考答案：

```sql
SELECT a.transaction_id, a.amount, a.currency, a.created_at
FROM authorizations a
LEFT JOIN settlements s
  ON a.transaction_id = s.transaction_id
WHERE a.status = 'APPROVED'
  AND s.transaction_id IS NULL
  AND a.created_at < CURRENT_TIMESTAMP - INTERVAL '1 day';
```

实际场景要考虑清算延迟、撤销、退款和批处理时间窗口，不能简单把所有缺失都判为异常。

### 44. Linux 题：线上 Java 服务 CPU 飙高，你如何排查？

参考答案：先用 `top`、`pidstat` 找到进程和线程，再用 `jstack`、`async-profiler` 或 APM 定位热点方法。检查最近发布、异常重试、日志打印、正则、加密运算、序列化和线程池。修复后要补充压测场景和 CPU 监控阈值。

### 45. 系统设计题：设计一个支付风控评分服务。

参考答案：系统包括特征采集、规则引擎、模型推理、评分输出、原因码、审计日志、监控和降级策略。关键要求是低延迟、高可用、可解释、可灰度和可回滚。测试重点是特征缺失、模型超时、版本切换、边界分数、并发性能、数据安全和线上漂移。

### 46. 行为题：你如何推动团队接受更严格的自动化门禁？

参考答案：先用数据说明当前风险，比如回归耗时、线上缺陷、回滚次数和重复事故。然后从高价值核心链路试点，如授权、回调、幂等和对账，确保门禁执行快、失败可诊断。推动过程中和开发一起优化框架和环境，让门禁成为交付加速器。最后用缺陷拦截率和回归时间下降证明价值。

## AI 相关加分点

- 能用 LLM 基于 API 文档、代码 diff 和历史缺陷生成测试点，并进行人工审查和自动化落地。
- 熟悉反欺诈 AI 测试，能评估误报率、漏报率、低延迟、模型漂移和原因码可解释性。
- 能测试生成式 AI 风控助手的事实一致性、引用证据、隐私保护和提示注入防护。
- 能用异常检测发现授权失败率突增、风控服务超时、回调失败、队列积压和对账差异。
- 能将日志、trace、错误码、交易状态输入智能排障流程，缩短支付事故定位时间。
- 理解支付 AI 合规边界：模型建议需要证据链、审计和人工或规则兜底。
- 能把 AI 生成脚本纳入沙箱运行、代码审查、安全扫描和 CI/CD 门禁。

## 复习建议

- 编码重点练习哈希表、滑动窗口、队列、堆、字符串、并发和复杂度分析。
- 熟悉 Java、REST Assured、Selenium/WebDriver、SQL、Oracle、API 自动化、契约测试和 CI/CD。
- 支付业务重点准备授权、清算、幂等、状态机、回调、风控、令牌化、限流和对账。
- 稳定性重点准备 P99 延迟、故障注入、消息队列、熔断降级、灾备演练和可观测性。
- AI 方向重点准备反欺诈模型、生成式 AI 风控助手、异常检测、日志分析和 AI 辅助测试生成。
- 行为面用 STAR 法准备质量推动、跨团队协作、线上事故复盘、平台落地和快速学习支付业务的案例。
