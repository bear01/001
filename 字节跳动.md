# 字节跳动测试开发工程师面试题整理

生成时间：2026-05-31  
岗位方向：测试开发工程师 / SDET / 质量工程 / 自动化测试 / 测试平台  
适用场景：社招与校招均可复习，偏向字节跳动内容生态、本地生活、电商、TikTok/抖音等高并发业务。

## 一、面试画像

字节跳动测开面试通常不会只考“会不会点点点”，更看重候选人是否具备开发基础、自动化落地能力、质量体系思维、问题拆解能力和业务理解能力。公开面经中常见轮次包括基础知识、项目深挖、算法题、场景测试设计、自动化/CI/CD、测试平台与稳定性保障。近期岗位描述也强调 Go/Python/Java 编码能力、测试工具/平台/框架/监控建设、分级发布、服务稳定性、效率提升和质量改进。

复习优先级建议：

1. 项目经历：能讲清背景、职责、难点、指标收益和复盘。
2. 编码与算法：字符串、数组、链表、哈希表、滑动窗口、双指针、排序、复杂度分析。
3. 自动化测试：接口自动化、UI 自动化、数据构造、断言设计、用例分层、稳定性治理。
4. 质量工程：CI/CD、灰度发布、监控告警、故障演练、质量门禁、线上问题闭环。
5. AI 能力：AI 辅助用例生成、日志分析、缺陷聚类、智能回归选择、LLM 测试平台、AI 输出评测。

## 二、计算机基础

### 1. 进程和线程有什么区别？测试开发为什么要理解它？

**参考答案：**  
进程是资源分配的基本单位，拥有独立地址空间；线程是 CPU 调度的基本单位，多个线程共享同一进程的内存资源。进程切换开销通常比线程大，线程共享数据时需要处理同步与并发安全。测试开发需要理解这些概念，是因为自动化执行器、并发压测工具、测试平台任务调度、服务端并发问题定位都会涉及进程、线程、协程、锁、队列等机制。

**AI 相关加分点：**  
如果用 AI Agent 执行自动化测试，要限制并发任务的资源消耗，例如浏览器实例数、接口调用频率、日志采集线程数，避免 AI 任务把测试环境打满。

### 2. 线程同步有哪些方式？如何排查并发测试中的偶现失败？

**参考答案：**  
常见同步方式包括互斥锁、读写锁、条件变量、信号量、CAS、线程安全队列和数据库事务锁。偶现失败要先确认是否存在共享状态、测试数据污染、接口幂等问题、异步任务未完成、环境资源不足或断言时机不稳定。排查时可以增加请求链路 trace、唯一测试数据、重试前后日志、线程 dump、数据库快照和事件时间线。

**AI 相关加分点：**  
可以让 LLM 辅助聚类失败日志，但不能直接相信结论；要让模型输出“证据链”，例如异常堆栈、请求 ID、时间窗口和影响用例列表。

### 3. 堆和栈有什么区别？堆内存可能带来哪些问题？

**参考答案：**  
栈通常保存函数调用帧、局部变量和返回地址，分配释放快，由运行时自动管理。堆用于动态对象分配，生命周期更灵活，但可能出现内存泄漏、碎片、GC 压力、悬挂引用或对象过多导致的性能下降。Java/Python 等语言虽然有自动内存管理，但仍可能因为缓存未释放、全局引用、循环引用、连接池泄漏导致内存持续增长。

**AI 相关加分点：**  
对 AI 服务压测时，要特别关注上下文缓存、向量检索结果、模型响应缓存和批处理队列的内存峰值。

### 4. HTTP 和 HTTPS 的区别是什么？HTTPS 请求的大致过程是什么？

**参考答案：**  
HTTP 明文传输，HTTPS 在 HTTP 与 TCP 之间加入 TLS 加密层，提供身份认证、加密传输和完整性保护。HTTPS 大致过程包括客户端发起 TLS 握手、服务端返回证书、客户端校验证书、双方协商密钥，然后使用对称密钥加密后续数据。测试时要覆盖证书过期、域名不匹配、弱 TLS 配置、重定向、混合内容、抓包代理和网关转发等场景。

**AI 相关加分点：**  
AI 测试工具在调用线上接口时不能绕过证书校验；如果接入 LLM 日志分析，也要避免把 token、cookie、证书和用户隐私上传到不合规的模型服务。

### 5. GET、POST、PUT、DELETE 的语义区别是什么？接口测试如何设计断言？

**参考答案：**  
GET 通常用于读取资源，POST 常用于创建或提交，PUT 常用于整体更新，DELETE 用于删除资源。接口测试不能只断言状态码，还要断言响应结构、关键字段、业务状态、数据库落库、消息队列事件、幂等性、权限控制、异常返回、耗时和链路日志。对于写接口，还要验证重复请求、并发请求和回滚逻辑。

**AI 相关加分点：**  
可以让 AI 根据 OpenAPI 文档生成接口用例草案，但测试开发必须补充业务规则、权限矩阵、幂等和数据一致性校验。

### 6. TCP 三次握手和四次挥手分别解决什么问题？

**参考答案：**  
三次握手用于确认客户端和服务端的发送、接收能力正常，并同步初始序列号。四次挥手用于双方分别关闭发送方向的数据流，因为 TCP 是全双工连接。测试开发在压测和稳定性测试中需要关注连接复用、TIME_WAIT、连接池耗尽、半连接队列、超时重传和负载均衡连接策略。

**AI 相关加分点：**  
AI 生成压测脚本时容易忽略连接池配置和真实用户连接模型，要明确要求它区分短连接、长连接和 HTTP/2 多路复用。

### 7. 关系型数据库和非关系型数据库如何选择？

**参考答案：**  
关系型数据库适合强事务、结构化数据、复杂查询和一致性要求高的场景；非关系型数据库适合高吞吐、灵活 schema、缓存、文档、宽表、图关系或时序数据等场景。测试时要关注事务一致性、索引命中、慢查询、分库分表、缓存穿透、主从延迟、最终一致性和数据修复能力。

**AI 相关加分点：**  
AI 应用常同时用关系库保存业务状态、向量库保存语义索引、缓存保存热点上下文。测试方案要覆盖“检索结果正确但业务状态过期”这类跨存储一致性问题。

### 8. HashMap 的底层原理是什么？为什么面试常问它？

**参考答案：**  
HashMap 通过哈希函数把 key 映射到桶位，冲突时使用链表或树化结构处理。面试常问它，是因为它能考察数据结构、哈希冲突、扩容、时间复杂度、线程安全和工程场景选择。测试开发写用例平台、缓存、去重、统计、覆盖率分析时都会频繁使用哈希结构。

**AI 相关加分点：**  
做缺陷聚类或日志聚合时，可以先用哈希规则进行粗分组，再用向量相似度或 LLM 做语义合并，兼顾成本和效果。

## 三、算法与编码

### 9. 写一个函数，统计字符串中出现次数最多的字符和次数。

**参考答案：**  
核心思路是用哈希表统计字符频次，再遍历哈希表找最大值。时间复杂度 O(n)，空间复杂度 O(k)，k 为字符集大小。需要考虑空字符串、大小写是否敏感、中文/emoji 等 Unicode 字符、多个字符并列第一时如何返回。

```python
def most_frequent_char(s: str):
    if not s:
        return None, 0
    counter = {}
    for ch in s:
        counter[ch] = counter.get(ch, 0) + 1
    return max(counter.items(), key=lambda item: item[1])
```

**AI 相关加分点：**  
回答时可补一句：让 AI 写完代码后，我会补边界测试和属性测试，例如随机字符串校验统计总和是否等于原字符串长度。

### 10. 如何判断链表是否有环？复杂度是多少？

**参考答案：**  
使用快慢指针，慢指针每次走一步，快指针每次走两步。如果链表有环，快慢指针最终会相遇；如果快指针走到空节点，则无环。时间复杂度 O(n)，空间复杂度 O(1)。

**AI 相关加分点：**  
可以让 AI 生成链表构造器和测试样例，但要人工检查是否覆盖空链表、单节点自环、尾节点成环和无环长链表。

### 11. 最小覆盖子串怎么解？

**参考答案：**  
使用滑动窗口。先统计目标串字符需求，再移动右指针扩大窗口，满足条件后移动左指针收缩窗口并记录最短答案。关键是维护窗口内字符计数和已满足字符数量。时间复杂度 O(n)，空间复杂度 O(k)。

**AI 相关加分点：**  
AI 生成的滑动窗口代码常在重复字符需求上出错，例如目标串包含两个相同字符；面试中主动说出这个风险，会显得更稳。

### 12. 两个大整数用字符串表示，如何实现相加？

**参考答案：**  
从两个字符串末尾开始逐位相加，维护进位 carry，把每一位结果放入数组，最后反转。要处理长度不同、进位、前导零和非法字符。时间复杂度 O(max(m, n))。

**AI 相关加分点：**  
回答测试用例时可以覆盖：`"0"+"0"`、不同长度、有连续进位、超长输入、前导零、非法输入策略。

### 13. 给定一个数组，如何找出“左侧都小于它，右侧都大于它”的元素？

**参考答案：**  
可以预处理两个数组：`left_max[i]` 表示 i 左侧最大值，`right_min[i]` 表示 i 右侧最小值。若 `nums[i] > left_max[i]` 且 `nums[i] < right_min[i]`，则满足条件。时间复杂度 O(n)，空间复杂度 O(n)。如果只需要一个元素，可以进一步优化空间。

**AI 相关加分点：**  
让 AI 生成测试数据时，要提醒它构造不存在答案、多个答案、边界位置和重复元素场景。

### 14. 如何测试一个排序函数？

**参考答案：**  
不仅要检查输出是否有序，还要检查元素集合是否未丢失、未新增。如果是稳定排序，还要验证相等元素的相对顺序。用例包括空数组、单元素、已排序、逆序、重复元素、负数、大量随机数、极值、局部有序和超大输入。也可以用性质测试：结果长度不变、结果有序、排序前后频次一致。

**AI 相关加分点：**  
可以用 AI 生成初始用例，再用 property-based testing 自动生成更多随机输入，提高覆盖面。

## 四、测试理论与场景设计

### 15. 如何测试“电脑上线销售前”的质量？

**参考答案：**  
可以从功能、硬件、兼容性、性能、稳定性、安全、可用性、生产交付和售后监控展开。功能包括开机、系统安装、驱动、网络、蓝牙、摄像头、音频、键盘触控板；硬件包括屏幕、接口、电池、温度、风扇；兼容性包括外设、系统版本和常用软件；性能包括启动耗时、续航、压力、散热；稳定性包括长时间运行、睡眠唤醒、异常断电；安全包括系统权限、固件、隐私；交付包括包装、序列号、说明书、保修。

**AI 相关加分点：**  
可以用 AI 把需求文档、历史缺陷和用户评论转成测试点，但最终要按风险优先级筛选，不能只追求数量。

### 16. 如何为抖音/短视频 Feed 流设计测试用例？

**参考答案：**  
要覆盖推荐刷新、上滑下滑、视频播放、暂停、点赞、评论、关注、分享、弱网、无网、缓存、预加载、广告插入、青少年模式、内容安全、账号状态、地域策略、性能耗电、崩溃率和数据埋点。服务端还要关注推荐接口稳定性、降级策略、重复内容、冷启动、AB 实验和召回/排序策略变更。

**AI 相关加分点：**  
AI 可以辅助生成场景矩阵，例如用户画像、网络状态、内容类型、推荐策略组合；但推荐质量评估还需要指标体系，如停留时长、负反馈率、重复率、投诉率。

### 17. 如何测试直播间发弹幕功能？

**参考答案：**  
功能上验证发送、展示、排序、撤回、屏蔽词、频控、权限、礼物联动和多端同步。性能上验证高并发发送、消息延迟、丢消息、重复消息和服务降级。安全上验证 XSS、敏感词绕过、刷屏、机器人攻击。兼容性上覆盖不同端、横竖屏、弱网、切后台和重连。

**AI 相关加分点：**  
可用 LLM 生成敏感词变体、谐音、拆字和 emoji 混淆样例，但必须结合规则引擎和人工审核数据做评估。

### 18. 如何测试一个优惠券发放系统？

**参考答案：**  
核心是资格判断、库存扣减、并发一致性、幂等、防刷、过期、核销、退款返还、活动配置、灰度和财务对账。并发场景要验证超卖、重复领取、库存回滚、消息延迟和缓存数据库一致性。异常场景包括支付失败、订单取消、服务超时、MQ 重投和配置错误。

**AI 相关加分点：**  
可以让 AI 根据规则生成决策表，但金额、库存、财务类规则必须做精确断言和对账，不能只做自然语言判断。

### 19. 如何设计接口自动化用例分层？

**参考答案：**  
底层封装请求客户端、鉴权、日志、重试、环境配置；中间层封装业务 API 和数据构造；上层编排测试场景和断言。用例可以分为冒烟、核心链路、异常、权限、兼容、回归和契约测试。好的分层能降低维护成本，使接口变化时只修改少量封装。

**AI 相关加分点：**  
AI 很适合从接口文档生成 API client 和基础断言，但业务场景编排、数据清理和幂等校验仍需要测试开发主导。

### 20. 自动化测试为什么会不稳定？如何治理？

**参考答案：**  
不稳定原因包括环境波动、数据依赖、异步等待、元素定位脆弱、接口限流、并发污染、第三方依赖、测试顺序耦合和断言过弱或过强。治理手段包括独立测试数据、显式等待、稳定定位、服务 mock、失败截图/日志/trace、重试分级、失败归因、用例隔离、定期清理和稳定性指标看板。

**AI 相关加分点：**  
可以建立 flaky case 自动归因系统，用规则加 LLM 分类失败原因，例如环境、脚本、产品缺陷、数据问题、超时。

## 五、自动化、CI/CD 与测试平台

### 21. 你如何理解 CI/CD？测试在其中起什么作用？

**参考答案：**  
CI 是持续集成，核心是代码提交后自动构建、静态检查、单元测试、接口测试等，尽早发现问题。CD 是持续交付或持续部署，核心是把变更安全、可控地发布到测试、预发或生产环境。测试在 CI/CD 中承担质量门禁，包括用例执行、覆盖率、扫描、契约校验、性能基线、灰度监控和回滚判断。

**AI 相关加分点：**  
AI 可以根据代码 diff 推荐需要回归的用例，但上线门禁必须有可解释规则，例如改动模块、历史缺陷、覆盖率、风险等级和核心链路影响。

### 22. 如何设计一个接口自动化测试平台？

**参考答案：**  
核心模块包括项目管理、环境管理、接口管理、用例管理、数据管理、执行调度、报告、权限、通知、Mock、CI 集成和质量看板。平台应支持参数化、前后置步骤、依赖提取、断言模板、并发执行、失败重跑、历史趋势和接口变更对比。架构上要把执行引擎与平台管理解耦，避免平台 UI 与测试执行强绑定。

**AI 相关加分点：**  
可以加入 AI 用例生成、断言建议、失败原因摘要、接口变更影响分析和自然语言查询测试报告，但每个 AI 能力都要保留人工确认入口和可追溯日志。

### 23. 如何把 UI 自动化接入流水线？

**参考答案：**  
先筛选稳定且收益高的核心路径，不要把全部 UI 用例都放进主干门禁。流水线中准备浏览器/移动设备环境、测试账号和测试数据，执行后保存截图、视频、日志、网络请求和失败 DOM。主干可以跑少量冒烟用例，夜间任务跑完整回归，失败要自动归因并通知责任人。

**AI 相关加分点：**  
AI 可以辅助定位页面变化导致的元素失败，但要控制自动修复脚本的权限，避免误改定位后掩盖真实产品缺陷。

### 24. 质量看板应该包含哪些指标？

**参考答案：**  
可以包含需求测试进度、用例通过率、自动化覆盖率、缺陷密度、线上缺陷数、P0/P1 缺陷、构建失败率、平均修复时长、回归耗时、接口成功率、性能基线、稳定性 SLO、灰度异常和用户反馈。指标要服务决策，避免只堆数据。每个指标都应有口径、负责人、阈值和行动策略。

**AI 相关加分点：**  
LLM 可以将看板指标转成日报和风险摘要，但原始指标必须可追溯，不能只展示模型的自然语言结论。

### 25. 如何做测试数据管理？

**参考答案：**  
测试数据管理要解决构造、隔离、复用、清理、脱敏和一致性。常见方案包括工厂方法生成数据、接口造数、数据库快照、Mock 服务、影子库、数据标签和自动清理任务。关键是避免用例之间互相污染，避免使用真实敏感数据，避免数据过期导致用例失败。

**AI 相关加分点：**  
AI 可以生成数据组合，但涉及隐私、支付、订单和身份信息时要先脱敏，并确保生成数据不违反业务约束。

## 六、稳定性与质量保障

### 26. 如何做服务稳定性测试？

**参考答案：**  
稳定性测试包括容量评估、压测、长稳、故障注入、限流降级、依赖治理、灰度验证和监控告警。要先梳理核心链路、强弱依赖、容量模型、峰值流量和降级预案，再设计压测脚本和观测指标。核心指标包括 QPS、P95/P99 延迟、错误率、CPU、内存、GC、线程池、连接池、队列堆积和下游依赖耗时。

**AI 相关加分点：**  
AI 可以从历史故障复盘中提取风险清单，生成混沌测试候选场景，但故障注入必须在可控环境中逐步扩大范围。

### 27. 灰度发布如何测试？

**参考答案：**  
灰度测试要覆盖灰度规则、用户命中、配置下发、版本兼容、数据兼容、回滚、监控和异常扩散控制。测试时要构造灰度用户和非灰度用户，对比功能、接口、埋点和数据写入是否符合预期。发布后观察核心指标，如果错误率、延迟、转化率或投诉异常，应触发暂停或回滚。

**AI 相关加分点：**  
可以用 AI 总结灰度期间的日志和用户反馈，但回滚判断要绑定硬指标和阈值。

### 28. 线上出现 P0 故障，测试开发应该怎么参与？

**参考答案：**  
首先协助止血，确认影响范围、版本、链路、用户群和关键指标，推动降级或回滚。然后保留现场数据，包括日志、trace、监控、配置和变更记录。故障恢复后参与复盘，补充自动化用例、监控告警、发布门禁和故障演练，确保同类问题不再发生。

**AI 相关加分点：**  
AI 可用于快速摘要大量日志和工单，但在故障现场应避免让模型直接执行高风险操作，建议只读分析和人工确认。

### 29. 如何测试消息队列相关业务？

**参考答案：**  
要覆盖消息生产、消费、顺序、重复、丢失、延迟、重试、死信、堆积、幂等、事务消息和消费者扩缩容。业务断言不能只看接口返回，还要检查最终状态、消费日志、数据库结果和补偿任务。异常场景包括消费者宕机、网络抖动、消息重复投递、下游失败和消息格式变更。

**AI 相关加分点：**  
AI 可以帮助从链路日志中还原消息流转过程，但测试用例应明确消息 ID 和业务 ID 的对应关系。

### 30. 性能测试结果不达标，你如何分析？

**参考答案：**  
先确认压测模型是否真实，包括并发、数据量、请求比例、连接模型、缓存状态和环境规格。再看瓶颈指标：应用 CPU、内存、GC、线程池、数据库慢查询、锁等待、缓存命中率、网络 IO、队列堆积和下游耗时。最后定位到代码、配置、架构或容量问题，给出优化建议并复测。

**AI 相关加分点：**  
AI 可以根据监控指标生成初步假设，但要通过实验验证，例如调整线程池、索引、缓存或限流参数后对比结果。

## 七、AI + 测试开发专项模拟题

### 31. 如何用 AI 辅助生成测试用例？它的风险是什么？

**参考答案：**  
可以把需求文档、接口文档、PRD、历史缺陷和业务规则输入给 AI，让它生成场景清单、边界值、异常路径、决策表和自动化脚本草案。风险包括幻觉、遗漏隐性规则、生成重复低价值用例、误解业务术语、泄露敏感数据和断言不准确。因此要建立模板化提示词、上下文脱敏、人工评审、规则校验和覆盖率反馈机制。

**AI 相关加分点：**  
回答时突出“AI 不是替代测试设计，而是提高初稿速度和覆盖提醒；最终质量由风险模型和评审闭环保证”。

### 32. 你会如何测试一个 LLM 聊天机器人？

**参考答案：**  
要从功能、正确性、安全、稳定性、性能、成本和体验评估。功能包括多轮对话、上下文记忆、工具调用、知识库检索；正确性包括事实一致性、引用来源、拒答策略；安全包括越权、提示注入、隐私泄露、敏感内容；稳定性包括超时、重试、降级；性能包括首 token 延迟、总耗时、并发能力；成本包括 token 消耗和缓存命中率。评测方式可以结合规则断言、人工评审、LLM-as-judge 和线上反馈。

**AI 相关加分点：**  
要强调 LLM-as-judge 也需要校准，最好有人审样本、黄金集、反例集和一致性评估。

### 33. 什么是提示注入？测试时如何覆盖？

**参考答案：**  
提示注入是用户或外部内容试图覆盖系统指令、诱导模型泄露信息或执行越权行为。测试应覆盖直接指令覆盖、角色扮演、编码混淆、多语言、Markdown/HTML 注入、检索文档中的恶意指令、工具调用越权和数据外泄。断言重点是模型是否遵守系统边界、是否拒绝敏感请求、是否保留审计日志。

**AI 相关加分点：**  
可以建立红队 prompt 集，并持续从线上异常对话中沉淀新样本。

### 34. 如何评估 AI 生成自动化脚本的质量？

**参考答案：**  
可以从可运行性、可维护性、断言有效性、数据隔离、等待策略、失败诊断、代码风格和安全性评估。不能只看脚本能不能跑通，还要看它是否真的验证业务结果，是否有清晰日志和截图，是否避免硬编码账号、token、环境地址。最好让脚本经过 lint、单测、dry run、人工 review 和失败回放。

**AI 相关加分点：**  
可以把 AI 生成脚本当作候选实现，由测试平台自动运行并给出质量评分，而不是直接合入主干。

### 35. 如何做“智能回归测试选择”？

**参考答案：**  
输入包括代码 diff、模块依赖、接口变更、历史缺陷、用例覆盖关系、线上风险等级和近期失败记录。规则层先做确定性筛选，例如改动模块关联用例、核心链路必跑；模型层再做风险排序，推荐额外回归用例。输出要包含推荐原因，便于测试负责人判断。上线后要对推荐命中率、漏测率和节省时间做持续评估。

**AI 相关加分点：**  
强调可解释性和保守策略：核心链路不能完全交给模型裁剪，AI 只做排序和补充建议。

### 36. 如何用 AI 分析线上缺陷和用户反馈？

**参考答案：**  
可以把缺陷标题、描述、日志摘要、用户反馈、版本号、设备信息和业务标签输入模型，做聚类、摘要、严重级别建议、责任模块推荐和复现步骤提取。落地时要先做脱敏，统一字段结构，再让模型输出结构化 JSON，便于进入缺陷平台。模型结论要和规则、历史负责人、监控指标交叉验证。

**AI 相关加分点：**  
可以补充“用向量检索找相似历史缺陷，再让 LLM 总结差异”，这样比单纯让模型猜更可靠。

## 八、项目深挖题

### 37. 介绍一个你做过的自动化测试项目。

**参考答案：**  
建议按 STAR 结构回答：背景是什么，质量痛点是什么；你负责了哪些模块；用了什么技术方案；遇到什么难点；最后收益如何。比如接口回归从手工 2 天缩短到 2 小时，核心接口覆盖率从 30% 提升到 80%，线上缺陷下降，失败定位时间减少。不要只说“我写了很多用例”，要说平台能力、稳定性治理和业务收益。

**AI 相关加分点：**  
可以说自己如何尝试用 AI 生成初版用例、分析失败日志或总结报告，并说明人工校验机制。

### 38. 你如何理解测试开发岗位？

**参考答案：**  
测试开发不是单纯执行测试，也不是纯开发，而是用工程化能力提升质量效率。它既要理解业务风险，也要能开发工具、框架和平台；既要发现问题，也要推动研发流程改进。优秀测开要能从需求、开发、测试、发布、监控、复盘全链路建立质量保障体系。

**AI 相关加分点：**  
AI 时代的测开更像质量工程效率放大器：把重复分析、用例初稿、报告总结交给 AI，把风险判断、体系设计和关键验证掌握在自己手里。

### 39. 如果让你从 0 到 1 建设团队自动化体系，你怎么做？

**参考答案：**  
先做现状评估：业务风险、发布频率、历史缺陷、手工回归耗时、技术栈和人员能力。再选择收益最高的核心链路做接口自动化和冒烟测试，接入 CI，建立报告和通知。随后逐步建设数据管理、Mock、并发执行、失败归因、质量看板和回归选择。过程中要持续度量收益和稳定性，避免一开始追求大而全。

**AI 相关加分点：**  
可以把 AI 能力放在第二阶段：用例生成、日志摘要、报告总结、变更影响分析，先保证基础自动化可靠。

### 40. 你最近学习过哪些 AI 测试相关内容？

**参考答案：**  
可以回答 LLM 应用测试、RAG 评测、提示注入、安全红队、AI 生成用例、智能回归、日志聚类、LLM-as-judge、Agent 工具调用测试等。重点是结合测试开发职责，说清楚自己如何把 AI 用在质量效率提升上，而不是泛泛说“会用 ChatGPT”。

**AI 相关加分点：**  
建议准备一个小 demo，例如“输入需求文档，自动生成接口测试点和 pytest 草案，并生成覆盖率检查清单”。

## 九、反问面试官

1. 团队当前质量保障最主要的痛点是什么，是效率、稳定性、自动化覆盖率，还是线上缺陷？
2. 测试开发在团队中更偏业务质量保障，还是偏平台工具建设？
3. 团队是否已经在使用 AI 做测试用例生成、日志分析或智能回归？
4. 对新人前三个月的期望是什么？更希望先补业务，还是先交付工具能力？
5. 团队发布节奏如何？质量门禁和灰度回滚机制是否完善？

## 十、复习建议

1. 每天刷 2-3 道中等难度算法，重点练字符串、数组、链表、哈希、滑动窗口和双指针。
2. 准备 2 个项目故事：一个偏自动化/平台，一个偏业务质量/稳定性。
3. 把常见场景题练成结构化回答：功能、异常、兼容、性能、安全、数据、监控、灰度。
4. 熟悉 pytest/requests/Appium/Selenium/Playwright 至少一条技术线，能讲清框架设计。
5. 准备 AI + 测试的实际落地方案，不要只停留在“让 AI 生成用例”。
6. 回答每道题都尽量带“风险意识”和“指标收益”，这很贴近大厂测开评价方式。

## 十一、参考来源

- 字节跳动测试开发实习面经，一二三面常见基础、算法、项目问题：https://ceshiren.com/t/topic/18760
- 字节跳动测试开发岗位信息，强调稳定性、自动化、分级发布、平台框架和监控能力：https://teamedupchina.com/job/test-development-engineer-life-services-marketing-direction-bytedance-shanghai/
- 2026 字节测开岗面经，涉及 pytest、requests、CI/CD、基础知识、场景测试和代码题：https://cj.sina.com.cn/articles/view/7879848900/1d5acf3c401902sbou
- 牛客字节测试开发面经，涉及 Appium、HTTP、UI 自动化收益、CI/CD、算法和测试平台：https://www.nowcoder.com/discuss/353156654583717888

